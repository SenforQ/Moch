
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_joinTitle:[UInt8] = [0x2b,0x30,0x2b,0x36,0xea,0x25,0x31,0x26,0x27,0x34,0xfc,0xeb,0xe2,0x2a,0x23,0x35,0xe2,0x30,0x31,0x36,0xe2,0x24,0x27,0x27,0x30,0xe2,0x2b,0x2f,0x32,0x2e,0x27,0x2f,0x27,0x30,0x36,0x27,0x26]

fileprivate func toVideo(tag num: UInt8) -> UInt8 {
    let value = Int(num) + 62
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Voice greeting" :*/
fileprivate let show_managerData:String = "Voice gif label if manager"
fileprivate let kNamePath:String = "ritemitemting"

/*: "Add" :*/
fileprivate let showTheFormat:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let showLabelPath:String = "typscript"

/*: "addStatus" :*/
fileprivate let appThanFormat:String = "addSwith view"

/*: "list" :*/
fileprivate let app_fileName:String = "colorst"

/*: "audio" :*/
fileprivate let constMiniData:String = "audmakeo"

/*: "length" :*/
fileprivate let dataWithKey:String = "LENGTH"

/*: "url" :*/
fileprivate let app_addContent:String = "worldl"

/*: "Successfully added, please wait for approval" :*/
fileprivate let app_kindTitle:[UInt8] = [0x6c,0x61,0x76,0x6f,0x72,0x70,0x70,0x61,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x64,0x64,0x61,0x20,0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53]

/*: "Are you sure to delete this message？" :*/
fileprivate let constPartyFormat:[UInt8] = [0x7a,0x49,0x5e,0x1b,0x42,0x54,0x4e,0x1b,0x48,0x4e,0x49,0x5e,0x1b,0x4f,0x54,0x1b,0x5f,0x5e,0x57,0x5e,0x4f,0x5e,0x1b,0x4f,0x53,0x52,0x48,0x1b,0x56,0x5e,0x48,0x48,0x5a,0x5c,0x5e,0xd4,0x87,0xa4]

private func backgroundText(key num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "Cancel" :*/
fileprivate let main_levelId:[Character] = ["C","a","n","c","e"]
fileprivate let dataToKey:String = "position"

/*: "Delete" :*/
fileprivate let show_labelName:String = "true self view constraintDelet"
fileprivate let k_centerMsg:[Character] = ["e"]

/*: "id" :*/
fileprivate let noti_indexText:String = "ierror"

/*: "Please wait for the audit result" :*/
fileprivate let appPresentationMessage:[UInt8] = [0x74,0x6c,0x75,0x73,0x65,0x72,0x20,0x74,0x69,0x64,0x75,0x61,0x20,0x65,0x68,0x74,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "The upper limit has been reached" :*/
fileprivate let k_blockPath:[UInt8] = [0x6a,0x56,0x5b,0x1e,0x4b,0x4e,0x4e,0x5b,0x4c,0x1e,0x52,0x57,0x53,0x57,0x4a,0x1e,0x56,0x5f,0x4d,0x1e,0x5c,0x5b,0x5b,0x50,0x1e,0x4c,0x5b,0x5f,0x5d,0x56,0x5b,0x5a]

/*: "filePath" :*/
fileprivate let const_shareStr:[Character] = ["f","i","l","e","P","a","t","h"]

/*: "time" :*/
fileprivate let app_imagePath:String = "timput"

/*: "#FF506D" :*/
fileprivate let noti_videoText:[Character] = ["#","F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let mainMessageValue:String = "view row self mobilebtn_me"
fileprivate let k_toName:String = "_deleguard height"

/*: "Currently mute" :*/
fileprivate let showViewName:String = "self nowCurre"
fileprivate let notiMediumStr:String = "uusere"

/*: "Edit Title" :*/
fileprivate let appModelTitle:[Character] = ["E","d","i"]
fileprivate let show_bagKey:[Character] = ["t"," ","T","i","t","l","e"]

/*: "OK" :*/
fileprivate let main_progressMsg:[Character] = ["O","K"]

/*: "remark" :*/
fileprivate let dataSexTitleModelMsg:[Character] = ["r","e","m","a","r","k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let show_makeName:[UInt8] = [0xeb,0xc4,0xc1,0xcb,0xc3,0x88,0xdc,0xc0,0xcd,0x88,0xca,0xdd,0xdc,0xdc,0xc7,0xc6,0x88,0xca,0xcd,0xc4,0xc7,0xdf,0x88,0xdc,0xc7,0x88,0xc9,0xcc,0xcc,0xa2,0xcb,0xc7,0xc6,0xdc,0xcd,0xc6,0xdc]

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let data_kitValue:[UInt8] = [0x11,0x34,0x34,0xf0,0x3d,0x45,0x3c,0x44,0x39,0x40,0x3c,0x35,0xf0,0x46,0x3f,0x39,0x33,0x35,0xf0,0x37,0x42,0x35,0x35,0x44,0x39,0x3e,0x37,0x43,0xfc,0xf0,0x31,0x3e,0x34,0xf0,0x44,0x38,0x35,0xf0,0x43,0x49,0x43,0x44,0x35,0x3d,0xf0,0x47,0x39,0x3c,0x3c,0xf0,0x43,0x35,0x3e,0x34,0xf0,0x46,0x3f,0x39,0x33,0x35,0xf0,0x3d,0x35,0x43,0x43,0x31,0x37,0x35,0x43,0xf0,0x32,0x49,0xf0,0x44,0x45,0x42,0x3e,0x43,0xb,0xda,0x13,0x3c,0x39,0x33,0x3b,0xf0,0x44,0x38,0x35,0xf0,0x32,0x45,0x32,0x32,0x3c,0x35,0xf0,0x44,0x3f,0xf0,0x40,0x3c,0x31,0x49,0xf0,0x44,0x38,0x35,0xf0,0x46,0x3f,0x39,0x33,0x35,0xfc,0xf0,0x43,0x3c,0x39,0x34,0x35,0xf0,0x44,0x3f,0xf0,0x44,0x38,0x35,0xf0,0x46,0x3f,0x39,0x33,0x35,0xf0,0x31,0x42,0x35,0x31,0xf0,0x44,0x3f,0xf0,0x34,0x35,0x3c,0x35,0x44,0x35,0xf0,0x39,0x44,0xfc,0xf0,0x31,0x3e,0x34,0xf0,0x44,0x38,0x35,0xf0,0x46,0x3f,0x39,0x33,0x35,0xf0,0x44,0x39,0x44,0x3c,0x35,0xf0,0x33,0x31,0x3e,0xf0,0x32,0x35,0xf0,0x35,0x34,0x39,0x44,0x35,0x34,0xfe]

fileprivate func layerData(view num: UInt8) -> UInt8 {
    let value = Int(num) + 48
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DeleteViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class DeleteViewController: TailRecognizerDelegate {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: OrientationGreetBoundaryWrittenRecordAudioView?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: OfReactiveCompatible?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: DeIonateViewCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_joinTitle.map{toVideo(tag: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(show_managerData.prefix(7)) + kNamePath.replacingOccurrences(of: "item", with: "e")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.objectVideoBurn()
        //: designView()
        planBlock()
        //: getAudioListData()
        preemptData()
        //: setMutedDetection()
        changeDetection()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(sharedNotification(notification:)), name: userListName, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(quantityNotification(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(executeLoad),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: userListName,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.playSettle()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [ValueGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [ValueGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: user_deviceId - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(showTheFormat)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.photographicEquipment(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.gradientMore(colors: UIColor.showCorner(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fillIn), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension DeleteViewController {
    //: func getAudioListData() {
    func preemptData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(showLabelPath.replacingOccurrences(of: "script", with: "e"))] = 1
        //: ProgressHUD.show()
        DeleteReactiveCompatible.fileAdd()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ValueThen.cellNext(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            DeleteReactiveCompatible.deflector()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.modelView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(appThanFormat.prefix(4)) + "tatus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(app_fileName.replacingOccurrences(of: "color", with: "li"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [ValueGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<ValueGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func colorTime(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(showLabelPath.replacingOccurrences(of: "script", with: "e"))] = 1
        //: dict["audio"] = data
        dict[(constMiniData.replacingOccurrences(of: "make", with: "i"))] = data
        //: dict["length"] = Int(time)
        dict[(dataWithKey.lowercased())] = Int(time)

        //: ProgressHUD.show()
        DeleteReactiveCompatible.fileAdd()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        ValueThen.wish(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            DeleteReactiveCompatible.deflector()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(app_addContent.replacingOccurrences(of: "world", with: "ur"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.beforeDemonstrate(showMsg: String(bytes: app_kindTitle.reversed(), encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.preemptData()
                //: self.recordView?.dismiss()
                self.recordView?.expiryDismiss()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func tip(index: Int) {
        //: let config = ShowAlertConfig()
        let config = MagnitudeAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.photographicEquipment(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        PileThen.refuseLay(message: String(bytes: constPartyFormat.map{backgroundText(key: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(main_levelId) + dataToKey.replacingOccurrences(of: "position", with: "l")).localized, rightBtnTitle: (String(show_labelName.suffix(5)) + String(k_centerMsg)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            PileThen.obstruct()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            PileThen.obstruct()
            //: self.stopPlay()
            self.executeLoad()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: ValueGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ValueThen.allow(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.preemptData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension DeleteViewController {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func sharedNotification(notification _: NSNotification) {
        //: stopPlay()
        executeLoad()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func quantityNotification(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            executeLoad()
        }
    }

    //: @objc func addButtonClick() {
    @objc func fillIn() {
        //: guard TalkingSocketManager.shared.isTarget == false else {
        guard BankManagerSocketDelegate.shared.isTarget == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.addMsg(showMsg: dataMessageName)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LengthPermissionTool.actionCheckStub(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.addMsg(showMsg: const_limitTitle)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.addMsg(showMsg: String(bytes: appPresentationMessage.reversed(), encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.addMsg(showMsg: String(bytes: k_blockPath.map{$0^62}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.executeLoad()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = OrientationGreetBoundaryWrittenRecordAudioView(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.theDismiss()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(const_shareStr))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.colorTime(data: data!, time: dict[(app_imagePath.replacingOccurrences(of: "put", with: "e"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension DeleteViewController {
    //: func setMutedDetection() {
    func changeDetection() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        Equipment.shared.showBy()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        Equipment.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension DeleteViewController: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = DeIonateViewCell.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: DeIonateViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! DeIonateViewCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.move(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.playingData(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.playingData(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            Equipment.shared.showBy()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.section(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.executeLoad()
            //: self.editTitle(index: indexPath.section)
            self.ofIndex(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(show_labelName.suffix(5)) + String(k_centerMsg)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.tip(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(noti_videoText)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.tip(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.atomicCount85(name: (String(mainMessageValue.suffix(6)) + "ssage" + String(k_toName.prefix(5)) + "te_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = RemoveViewThen(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func section(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isTarget else {
        guard !BankManagerSocketDelegate.shared.isTarget else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.addMsg(showMsg: dataMessageName)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.playSettle()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? DeIonateViewCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.playingData(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.addMsg(showMsg: (String(showViewName.suffix(5)) + "ntly m" + notiMediumStr.replacingOccurrences(of: "user", with: "t")).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = OfReactiveCompatible.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.playSettle()
        //: self.player?.play(urlStr: model.content)
        self.player?.listSystem(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? DeIonateViewCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.playingData(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func ofIndex(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = FaceAlertView(title: (String(appModelTitle) + String(show_bagKey)).localized, leftBtnTitle: (String(main_levelId) + dataToKey.replacingOccurrences(of: "position", with: "l")).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.accumulation()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            DeleteReactiveCompatible.fileAdd()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: ValueGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(String(dataSexTitleModelMsg))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            ValueThen.targetIn(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DeleteReactiveCompatible.deflector()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func executeLoad() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.playSettle()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.playingData(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension DeleteViewController: TailReactiveCompatible {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func improvizeStatus(status: ConstraintWidthQuantity) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            executeLoad()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            executeLoad()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension DeleteViewController {
    //: private func setupEmptyDataView() {
    private func modelView() {
        //: var style = EmptyStyle()
        var style = ScreenEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: show_makeName.map{$0^168}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .objectVideoBurn()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func planBlock() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(DeIonateViewCell.self, forCellReuseIdentifier: DeIonateViewCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.someRegard()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.labelWith(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: data_kitValue.map{layerData(view: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
