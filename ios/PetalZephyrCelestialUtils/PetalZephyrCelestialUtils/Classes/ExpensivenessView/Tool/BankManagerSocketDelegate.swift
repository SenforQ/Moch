
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let show_nameMessage:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_"]
fileprivate let app_sourceValue:String = "viewEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let showTopData:[UInt8] = [0xa8,0xb4,0xb8,0xb0,0xbe,0xaf,0xa4,0xad,0xb2,0xbf,0xbe,0xb4,0xb8,0xb3,0xba,0xaf,0xa4,0xa9,0xbe,0xaa,0xae,0xbe,0xa8,0xaf,0xb8,0xba,0xb7,0xb7,0xa4,0xb5,0xb4,0xaf,0xb2,0xbd,0xb2,0xb8,0xba,0xaf,0xb2,0xb4,0xb5]

private func sectionText(cell num: UInt8) -> UInt8 {
    return num ^ 251
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let main_donationUrl:[UInt8] = [0x5f,0x48,0x4e,0x44,0x5b,0x48,0x52,0x4b,0x41,0x42,0x4c,0x59,0x5e,0x4e,0x5f,0x48,0x48,0x43,0x52,0x43,0x42,0x59,0x44,0x4b,0x44,0x4e,0x4c,0x59,0x44,0x42,0x43]

/*: "isCalling" :*/
fileprivate let constHomeText:String = "isCafalse medium icon"
fileprivate let dataAppText:String = "observe"
 
/*: "SOCKET 连接成功." :*/
fileprivate let userHiddenId:String = "SOCKbackground"
fileprivate let userResponseUrl:[Character] = [" ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let app_maxKey:[Character] = ["S","O","C","K","E","T"," ","连","接","失","\u{8d25}",","]
fileprivate let dataHiddenTitle:String = " error:result voice break white return"

/*: ." :*/
fileprivate let main_buttonData:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let show_cellKey:String = "SOerrorK"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let show_upName:String = "\u{6b21}, 切换过"
fileprivate let data_userMsg:String = "go useret域名重新尝"
fileprivate let dataViewMessage:String = "试，location："

/*: "SOCKET 已关闭, code: :*/
fileprivate let showSucceedUrl:[Character] = ["S","O","C","K","E","T"," ","已","关","闭",","," ","c","o","d"]
fileprivate let appKitValue:String = "click user imagee:"

/*: , reason: :*/
fileprivate let notiOfKey:String = "type data selected equal, rea"
fileprivate let const_messageMsg:[Character] = ["s","o","n",":"]

/*: "ws:// :*/
fileprivate let const_userFormat:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let noti_infoMessage:String = "height view view make color/?sid="

/*: &uid= :*/
fileprivate let user_mixtureStr:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let noti_toId:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let noti_barKey:[Character] = ["&"]
fileprivate let noti_modeKey:[Character] = ["p","a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let app_customTitle:String = "scale show1v1se"

/*: .com:7766" :*/
fileprivate let dataEqualName:String = ".comcancel label send view for"
fileprivate let user_constraintTitle:String = ":button66"

/*: "43.129.185.194:7767" :*/
fileprivate let user_insideData:String = "43.1live"
fileprivate let data_userManagerId:[Character] = ["9",".","1","8","5",".","1","9","4",":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let main_taskMsg:[UInt8] = [0xda,0xc6,0xca,0xc2,0xcc,0xdd,0xa9,0x6e,0x34,0x18,0x6e,0x32,0x15,0xfb,0xec,0xe8,0xea,0xe1,0xe8,0xeb,0xe0,0xe5,0xe0,0xfd,0xf0,0xca,0xe1,0xe8,0xe7,0xee,0xec,0xed,0x6e,0x12,0x18,0x6c,0x19,0x25,0x66,0x35,0x5,0x6f,0x1f,0x24,0x6e,0x34,0x18,0x6c,0xc,0x3a,0x60,0x1e,0x24,0xa7]

private func tingHiddenSelected(lab num: UInt8) -> UInt8 {
    return num ^ 137
}

/*: "cmd" :*/
fileprivate let notiTopMessage:String = "sized"

/*: "ping" :*/
fileprivate let noti_gameData:String = "pinimage"

/*: "callHeartbeat" :*/
fileprivate let show_fadeTitle:String = "show ifcallH"
fileprivate let constQuantityFormat:String = "makertbmaket"

/*: "recievedDate" :*/
fileprivate let show_ofStr:String = "rarrayieve"
fileprivate let noti_monthValue:String = "left manager on var makedDate"

/*: "liveHeartBeat" :*/
fileprivate let constFrameMsg:String = "liuser"
fileprivate let main_sexStr:[Character] = ["e","H","e","a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let userLikeValue:String = "🔗 关闭 "
fileprivate let user_pathFormat:String = "soclabelet"

/*: "The user logs out" :*/
fileprivate let userMakeValue:[Character] = ["T","h","e"," ","u","s","e","r"," ","l"]
fileprivate let k_needMessage:String = "ogs outview var open"

/*: "SOCKET  :*/
fileprivate let const_sizeFormat:[Character] = ["S","O","C","K","E"]
fileprivate let main_appearUrl:String = "T identity background"

/*: 重连. 重试次数： :*/
fileprivate let const_cameraName:String = "重add. 重"

/*: , 地址： :*/
fileprivate let user_succeedRawTitle:[Character] = [","," ","\u{5730}","址","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let notiToContent:String = "true view burn image makeSOCKET "
fileprivate let show_makeMessage:String = "\u{8fde}接正常"

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let kBottomUrl:String = "sock"
fileprivate let user_pointUrl:String = "接 kit"
fileprivate let dataImagePath:[Character] = ["需","\u{8981}","重","\u{65b0}","连","接","."]

/*: "&resume=1" :*/
fileprivate let show_countId:[Character] = ["&","r","e","s","u","m","e"]
fileprivate let app_infoLabMsg:String = "=1"

/*: "&lang= :*/
fileprivate let data_equalName:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let noti_personId:String = "socket"
fileprivate let const_sessionValue:String = "："

/*: "onNetDisabled" :*/
fileprivate let noti_labData:[Character] = ["o","n","N","e"]
fileprivate let dataBlockMainMsg:String = "tDisaequal size no with text"

/*: "errno" :*/
fileprivate let appCellStr:String = "ERRNO"

/*: "data" :*/
fileprivate let showRefreshPartyStr:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let dataAtKey:[Character] = ["S","O","C","K","E","T"," ","连","接","保","活","c","m","d",":"]
fileprivate let dataAppearEqualUrl:String = "title dynamic var height time ping."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let app_liveMessage:[UInt8] = [0x3d,0x21,0x2d,0x25,0x2b,0x3a,0x4e,0x1a,0x1c,0x1a,0xd,0x8a,0xd1,0xf3,0x88,0xda,0xd5,0xd,0x3,0xa,0x54,0x4e,0xd,0xf,0x2,0x2,0x26,0xb,0xf,0x1c,0x1a,0xc,0xb,0xf,0x1a,0x40]

private func valueOf(frame num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let main_playerPath:[UInt8] = [0x4,0x0,0xf4,0xfc,0xf6,0x5,0xd1,0x1d,0x1a,0x27,0x16,0x95,0x70,0x4e,0x97,0x65,0x6c,0x14,0x1e,0x15,0xeb,0xd1,0x1d,0x1a,0x27,0x16,0xf9,0x16,0x12,0x23,0x25,0xf3,0x16,0x12,0x25,0xdf]

fileprivate func returnTo(than num: UInt8) -> UInt8 {
    let value = Int(num) + 79
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let kSourceUrl:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let notiInsideMsg:String = "msizeg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let k_valueFormat:String = "🔗 处理消\u{606f}"
fileprivate let notiBeautyMsg:String = "\u{ff1a}cmd"
fileprivate let const_rawKey:[Character] = ["N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let main_styleGiftMessage:[Character] = [","," ","e","r","r","N","o",":"]

/*: , msg: :*/
fileprivate let dataBackgroundName:String = "path cell, msg:"

/*: , data: :*/
fileprivate let show_layerValue:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let app_nameToFormat:String = "goldOCKET"
fileprivate let constMakePositionData:String = "\u{6d88}息：cmd"
fileprivate let userManagerStr:[Character] = ["N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let noti_customPath:String = "add useronRe"
fileprivate let mainNameKey:[Character] = ["G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let kPathStr:String = "onVidetime current make curve"

/*: "onRequestCall" :*/
fileprivate let mainGuideText:[Character] = ["o","n","R","e","q"]
fileprivate let userEquipmentContent:String = "uelivet"

/*: "onCancelCall" :*/
fileprivate let userWorkName:[Character] = ["o","n","C","a","n","c","e","l","C","a","l"]
fileprivate let appTimeIndexUrl:String = "filter"

/*: "onRefuseCall" :*/
fileprivate let appAllKey:String = "onRefguard shared array empty data"
fileprivate let mainMakeTitle:[Character] = ["u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let userDataCountoText:String = "hidden resultonAcce"
fileprivate let noti_aspectSmallLabValue:String = "viewview"

/*: "onStopCall" :*/
fileprivate let showClickMsg:[Character] = ["o","n","S","t","o","p","C","a"]
fileprivate let show_effectFormat:String = "user"

/*: "freeCallTimes" :*/
fileprivate let kCellKey:String = "FRE"
fileprivate let showMainName:String = "eCallview false var name local"

/*: "mfCoin" :*/
fileprivate let user_specialUrl:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let noti_fileKey:String = "oa"
fileprivate let data_modelPath:String = "type contrastMissCoin"

/*: "acceptCall" :*/
fileprivate let kManagerTitleMessage:String = "infoccep"

/*: "stopCall" :*/
fileprivate let noti_countName:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let main_sourceStr:String = "rvoicequ"

/*: "[Invite you to a video call...]" :*/
fileprivate let appSizeStr:[UInt8] = [0x39,0x2b,0xc,0x14,0xb,0x16,0x7,0x42,0x1b,0xd,0x17,0x42,0x16,0xd,0x42,0x3,0x42,0x14,0xb,0x6,0x7,0xd,0x42,0x1,0x3,0xe,0xe,0x4c,0x4c,0x4c,0x3f]

private func layerRange(round num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "type" :*/
fileprivate let userParentContent:String = "ttope"

/*: "[Invite you to a voice call...]" :*/
fileprivate let main_recordData:[UInt8] = [0xdf,0xcd,0xea,0xf2,0xed,0xf0,0xe1,0xa4,0xfd,0xeb,0xf1,0xa4,0xf0,0xeb,0xa4,0xe5,0xa4,0xf2,0xeb,0xed,0xe7,0xe1,0xa4,0xe7,0xe5,0xe8,0xe8,0xaa,0xaa,0xaa,0xd9]

private func managingDirector(all num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "uid" :*/
fileprivate let user_toName:String = "umodel"

/*: "nickname" :*/
fileprivate let mainUserMsg:String = "NICKNAME"

/*: "headPic" :*/
fileprivate let appColorId:String = "hsection"
fileprivate let mainMakeToId:[Character] = ["a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let app_tapVideoPath:String = "onCallconstraint true let gesture"
fileprivate let notiActualButtonValue:[Character] = ["R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let user_progressContent:String = "cancelCself self small action regular"
fileprivate let kAwakeData:String = "acollectioncollection"

/*: "refuseCall" :*/
fileprivate let main_emptyLabelFormat:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let main_topSizeFormat:String = "down transfer for show viewonCall"
fileprivate let constMakeUrl:String = "view barMotiv"

/*: "onLiveWarning" :*/
fileprivate let show_objectValue:String = "onLiveheight view view leading view"
fileprivate let user_meData:String = "moment let mode viewWarning"

/*: "reason" :*/
fileprivate let kDateSecondName:String = "requestason"

/*: "time" :*/
fileprivate let showContentNeedMessage:String = "TIME"

/*: "onStopLive" :*/
fileprivate let app_commonUrl:String = "allow return typeonSto"
fileprivate let data_modelMessage:[Character] = ["e"]

/*: "onSendProgramGift" :*/
fileprivate let showLabPath:[Character] = ["o","n","S","e","n","d","P","r","o","g"]
fileprivate let notiBottomPath:[Character] = ["r","a"]
fileprivate let constSendUrl:[Character] = ["m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let k_iconUrl:String = "mid border view quoteonGa"

/*: "broadStatus" :*/
fileprivate let showEachKey:String = "broadStequal view error"
fileprivate let app_fadeValue:[Character] = ["a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let constTitleCountMessage:String = "onCallChicon lab share container"
fileprivate let show_addGroupPath:String = "apply botatMsg"

/*: "onNotificationPopupWin" :*/
fileprivate let app_handleKey:String = "onNopresent add progress let"
fileprivate let data_viewMsg:String = "false equipment with user ontionPo"

/*: "onUserBlock" :*/
fileprivate let mainVideoUrl:String = "model user perform import gestureonUse"
fileprivate let user_bottomMsg:[Character] = ["r","B","l","o","c","k"]

/*: "freeCallWin" :*/
fileprivate let notiAddId:[Character] = ["f","r","e","e","C","a","l","l","W"]
fileprivate let mainKitUpPath:String = "IN"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BankManagerSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let app_bottomTotalData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let appCollectionMaxFormat = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let showTransformKey = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let notiVideoTitle = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let notiReplaceUrl = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let showBusyViewMessage = (String(show_nameMessage) + app_sourceValue.replacingOccurrences(of: "view", with: "K"))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let notiButtonId = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let showObjectIndexText = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let noti_infoStr = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let appLineTitle = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let userListName = NSNotification.Name(rawValue: String(bytes: showTopData.map{sectionText(cell: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let noti_fileFormat = NSNotification.Name(rawValue: String(bytes: main_donationUrl.map{$0^13}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol BotchErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func startAccountingData(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol CommentObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func locationData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func beTitle(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol NameObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func session(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func modelPhone(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func eigenvalueOfASquareMatrix(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func filterOfRequest(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol ActionVideoPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func doTarget(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func sprechgesang(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func moveCellData(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class BankManagerSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum CookieAdd: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: WithLanguageManager.shared.direction == .rightToLeft ? 0 : constExplainViewMsg,
        //: y: StatusBarHeight+190
        y: appImageLimitPath + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: WithLanguageManager.shared.direction == .rightToLeft ? 0 : constExplainViewMsg,
        //: y: StatusBarHeight+190
        y: appImageLimitPath + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: WithLanguageManager.shared.direction == .rightToLeft ? 0 : constExplainViewMsg,
        //: y: StatusBarHeight+500
        y: appImageLimitPath + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: BotchErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: CommentObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: NameObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: ActionVideoPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTarget = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = BankManagerSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(theCurrent(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(itemExamine),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(constHomeText.prefix(4)) + "llin" + dataAppText.replacingOccurrences(of: "observe", with: "g")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (SocialKingfisherCompatible.shared as! SocialKingfisherCompatible).appeal()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, "isTarget")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (SocialKingfisherCompatible.shared as! SocialKingfisherCompatible).appeal()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension BankManagerSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        WoodLogTool.knowHow(msg: (userHiddenId.replacingOccurrences(of: "background", with: "ET") + String(userResponseUrl)))
        //: sendPing()
        tableGroup()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: userDetailName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: showBusyViewMessage)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        recordStraddleView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        WoodLogTool.knowHow(msg: (String(app_maxKey) + String(dataHiddenTitle.prefix(7))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = NowInfoThen.share.reachability?.connection, status != .unavailable else {
        guard let status = NowInfoThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == notiVideoTitle {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = momentRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            WoodLogTool.knowHow(msg: (show_cellKey.replacingOccurrences(of: "error", with: "C") + "ET 失败") + "\(notiVideoTitle)" + (show_upName + "Sock" + String(data_userMsg.suffix(7)) + dataViewMessage.replacingOccurrences(of: "location", with: "地址")) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.colorResponse(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.dealWith(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        WoodLogTool.knowHow(msg: (String(showSucceedUrl) + String(appKitValue.suffix(2))) + "\(code)" + (String(notiOfKey.suffix(5)) + String(const_messageMsg)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.modelSuccessfully(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == appLineTitle {
            //: return
            return
        }
        //: if let status = NowInfoThen.share.reachability?.connection, status != .unavailable {
        if let status = NowInfoThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.colorResponse(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func momentRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = NowInfoThen.share.appConfigMode.ws
        self.linkAddrArr = NowInfoThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = appEqual()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: showBusyViewMessage)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = NowInfoThen.share.loginUid
        let uid = NowInfoThen.share.loginUid
        //: let sid = NowInfoThen.share.loginSessionId
        let sid = NowInfoThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(noti_infoMessage.suffix(6))) + "\(sid)" + (String(user_mixtureStr)) + "\(uid)" + (String(noti_toId)) + "\(appFileMsg)" + (String(noti_barKey) + String(noti_modeKey)) + "\(main_maxTitle)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func appEqual() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return main_closedTitle ? [(String(app_customTitle.suffix(5)) + "rver.") + "\(main_bottomStr)" + (String(dataEqualName.prefix(4)) + user_constraintTitle.replacingOccurrences(of: "button", with: "77"))] : [(user_insideData.replacingOccurrences(of: "live", with: "2") + String(data_userManagerId))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension BankManagerSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func theCurrent(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            WoodLogTool.knowHow(msg: String(bytes: main_taskMsg.map{tingHiddenSelected(lab: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.modelSuccessfully(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.colorResponse(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func itemExamine() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.colorResponse(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.buttonDrag()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension BankManagerSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func tableGroup() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: app_bottomTotalData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.appFrom(info: [(notiTopMessage.replacingOccurrences(of: "size", with: "cm")): (noti_gameData.replacingOccurrences(of: "image", with: "g"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func discourse() {
        //: endTalkingPing()
        itemPing()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: appCollectionMaxFormat, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTarget || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTarget || (self.buttonController()?.isKind(of: BeautyOfViewController.self) == false && self.buttonController()?.isKind(of: ViewEqualObserverDelegate.self) == false) && (self.currTalkingVC?.isKind(of: BeautyOfViewController.self) == false && self.currTalkingVC?.isKind(of: ViewEqualObserverDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.appFrom(info: [(notiTopMessage.replacingOccurrences(of: "size", with: "cm")): (String(show_fadeTitle.suffix(5)) + constQuantityFormat.replacingOccurrences(of: "make", with: "ea"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func itemPing() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func buttonDrag() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(show_ofStr.replacingOccurrences(of: "array", with: "ec") + String(noti_monthValue.suffix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < notiScreenLimitMessage {
                //: NowInfoThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                NowInfoThen.share.equalToView(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension BankManagerSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func belowKeyCrop() {
        //: endLivePing()
        inhabitEndPing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: showTransformKey, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.appFrom(info: [(notiTopMessage.replacingOccurrences(of: "size", with: "cm")): (constFrameMsg.replacingOccurrences(of: "user", with: "v") + String(main_sexStr))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func inhabitEndPing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension BankManagerSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func recordStraddleView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = InheritanceReactiveCompatible()
        //: let keyWindow = LineThen.getWindow()
        let keyWindow = LineThen.getName()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTarget
        self.containerView?.isHidden = !self.isTarget
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func mentalRepresentation() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard ProduceViewPushListener.decide().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard ConstraintEventHandler.bindTo().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension BankManagerSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func modelSuccessfully(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (userLikeValue.capitalized + user_pathFormat.replacingOccurrences(of: "label", with: "k")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: appLineTitle, reason: (String(userMakeValue) + String(k_needMessage.prefix(7))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        itemPing()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            mentalRepresentation()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func blackLabel() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = BankManagerSocketDelegate.shared.momentRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.colorResponse(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func colorResponse(timeInterval: TimeInterval, type: CookieAdd) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            WoodLogTool.knowHow(msg: (String(const_sizeFormat) + String(main_appearUrl.prefix(2))) + "\(type.rawValue)" + (const_cameraName.replacingOccurrences(of: "add", with: "连") + "试\u{6b21}\u{6570}：") + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.fountainheadStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func fountainheadStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                WoodLogTool.knowHow(msg: (String(notiToContent.suffix(7)) + show_makeMessage + " 不需要\u{91cd}\u{65b0}连接."))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                WoodLogTool.knowHow(msg: (kBottomUrl.uppercased() + "ET \u{6b63}在连" + user_pointUrl.replacingOccurrences(of: "kit", with: "不") + String(dataImagePath)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTarget {
        if self.isTarget {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(show_countId) + app_infoLabMsg.capitalized))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(data_equalName)) + "\(OfDetailAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        modelSuccessfully(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = notiReplaceUrl
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        WoodLogTool.knowHow(msg: (noti_personId.uppercased() + " 开始连接, 地址" + const_sessionValue.capitalized) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func appFrom(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if NowInfoThen.share.reachability?.connection == nil || NowInfoThen.share.reachability?.connection == .unavailable {
        if NowInfoThen.share.reachability?.connection == nil || NowInfoThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(notiTopMessage.replacingOccurrences(of: "size", with: "cm")): (String(noti_labData) + String(dataBlockMainMsg.prefix(5)) + "bled"),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (appCellStr.lowercased()): "\(noti_infoStr)",
                                       //: "data": [:]]
                                       (String(showRefreshPartyStr)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            dealWith(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(notiTopMessage.replacingOccurrences(of: "size", with: "cm"))] as? String == (noti_gameData.replacingOccurrences(of: "image", with: "g")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                WoodLogTool.knowHow(msg: (String(dataAtKey) + String(dataAppearEqualUrl.suffix(6))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(notiTopMessage.replacingOccurrences(of: "size", with: "cm"))] as? String == (String(show_fadeTitle.suffix(5)) + constQuantityFormat.replacingOccurrences(of: "make", with: "ea")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                WoodLogTool.knowHow(msg: String(bytes: app_liveMessage.map{valueOf(frame: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(notiTopMessage.replacingOccurrences(of: "size", with: "cm"))] as? String == (constFrameMsg.replacingOccurrences(of: "user", with: "v") + String(main_sexStr)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                WoodLogTool.knowHow(msg: String(bytes: main_playerPath.map{returnTo(than: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(notiTopMessage.replacingOccurrences(of: "size", with: "cm"))] as? String == (noti_gameData.replacingOccurrences(of: "image", with: "g")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(notiTopMessage.replacingOccurrences(of: "size", with: "cm")): (String(kSourceUrl)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (appCellStr.lowercased()): "\(notiButtonId)",
                                           //: "data": [:]]
                                           (String(showRefreshPartyStr)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                dealWith(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.colorResponse(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func dealWith(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(notiTopMessage.replacingOccurrences(of: "size", with: "cm"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(appCellStr.lowercased())].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(notiInsideMsg.replacingOccurrences(of: "size", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(showRefreshPartyStr))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (k_valueFormat + notiBeautyMsg + String(const_rawKey)) + "\(cmdName)" + (String(main_styleGiftMessage)) + "\(errorNo)" + (String(dataBackgroundName.suffix(6))) + "\(msg)" + (String(show_layerValue)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        chemicalElement(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            WoodLogTool.knowHow(msg: (app_nameToFormat.replacingOccurrences(of: "gold", with: "S") + " 接\u{6536}异常" + constMakePositionData + String(userManagerStr)) + "\(cmdName)" + (String(main_styleGiftMessage)) + "\(errorNo)" + (String(dataBackgroundName.suffix(6))) + "\(msg)" + (String(show_layerValue)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: NowInfoThen.share.loginUserMode.freeCallTimes = 0
                NowInfoThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !NowInfoThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !NowInfoThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                PopupReactiveCompatible.shared.changeShape()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.startAccountingData(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(noti_customPath.suffix(4)) + "ceived" + String(mainNameKey)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: data_tabPath, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(kPathStr.prefix(6)) + "oMatch") { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.locationData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(mainGuideText) + userEquipmentContent.replacingOccurrences(of: "live", with: "s") + "Call") { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.beTitle(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(userWorkName) + appTimeIndexUrl.replacingOccurrences(of: "filter", with: "l")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(appAllKey.prefix(5)) + String(mainMakeTitle)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(userDataCountoText.suffix(6)) + "ptCa" + noti_aspectSmallLabValue.replacingOccurrences(of: "view", with: "l")) { // 接收方发送acceptCall回执
            //: self.isTarget = true
            self.isTarget = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            discourse()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.sprechgesang(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(showClickMsg) + show_effectFormat.replacingOccurrences(of: "user", with: "ll")) { // 接收方发送stopCall回执
            //: self.isTarget = false
            self.isTarget = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            itemPing()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(kCellKey.lowercased() + String(showMainName.prefix(5)) + "Times")] as? Int {
                //: NowInfoThen.share.loginUserMode.freeCallTimes = freeCallTimes
                NowInfoThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: NowInfoThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            NowInfoThen.share.loginUserMode.mf_coin = json[(String(showRefreshPartyStr))][(String(user_specialUrl))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: app_intimateKey, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SocialKingfisherCompatible.shared as! SocialKingfisherCompatible).ofReport()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (noti_fileKey.replacingOccurrences(of: "a", with: "n") + String(data_modelPath.suffix(8))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.filterOfRequest(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (kManagerTitleMessage.replacingOccurrences(of: "info", with: "a") + "tCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTarget = true
            self.isTarget = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            discourse()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.session(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(noti_countName)) { // 接收方/拨打方 挂断了通话
            //: self.isTarget = false
            self.isTarget = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            itemPing()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.modelPhone(data: data)
            // 更新余额
            //: NowInfoThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            NowInfoThen.share.loginUserMode.mf_coin = json[(String(showRefreshPartyStr))][(String(user_specialUrl))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            giftEnableData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: app_intimateKey, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SocialKingfisherCompatible.shared as! SocialKingfisherCompatible).ofReport()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (main_sourceStr.replacingOccurrences(of: "voice", with: "e") + "estCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: userListName, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: NowInfoThen.share.recieve1v1TalkRequest(info: data)
                NowInfoThen.share.equalToView(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(show_ofStr.replacingOccurrences(of: "array", with: "ec") + String(noti_monthValue.suffix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: appSizeStr.map{layerRange(round: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(showRefreshPartyStr))][(userParentContent.replacingOccurrences(of: "to", with: "y"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: main_recordData.map{managingDirector(all: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                CellThen.view(uid: String(json[(String(showRefreshPartyStr))][(user_toName.replacingOccurrences(of: "model", with: "id"))].intValue),
                                       //: title: data["nickname"] as? String,
                                       title: data[(mainUserMsg.lowercased())] as? String,
                                       //: body: body,
                                       body: body,
                                       //: imageUrl: data["headPic"] as? String)
                                       imageUrl: data[(appColorId.replacingOccurrences(of: "section", with: "e") + String(mainMakeToId))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                CellThen.shared.deadWeight()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(app_tapVideoPath.prefix(6)) + String(notiActualButtonValue)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTarget = true
            self.isTarget = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            discourse()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(app_tapVideoPath.prefix(6)) + String(notiActualButtonValue))] = "1"
            //: NowInfoThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            NowInfoThen.share.titleInfo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (SocialKingfisherCompatible.shared as! SocialKingfisherCompatible).appeal()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(user_progressContent.prefix(7)) + kAwakeData.replacingOccurrences(of: "collection", with: "l")) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTarget = false
            self.isTarget = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            itemPing()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.eigenvalueOfASquareMatrix(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                CellThen.addPublicTransport(identifier: String(json[(String(showRefreshPartyStr))][(user_toName.replacingOccurrences(of: "model", with: "id"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                CellThen.shared.option()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(main_emptyLabelFormat)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTarget = false
            self.isTarget = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            itemPing()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.doTarget(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(main_topSizeFormat.suffix(6)) + String(constMakeUrl.suffix(5)) + "ation") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if NowInfoThen.share.loginUserMode.sex == Gender.female.rawValue {
            if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.moveCellData?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(show_objectValue.prefix(6)) + String(user_meData.suffix(7))) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            ProduceViewPushListener.nowadays(content: msg, reason: json[(String(showRefreshPartyStr))][(kDateSecondName.replacingOccurrences(of: "request", with: "re"))].stringValue, time: json[(String(showRefreshPartyStr))][(showContentNeedMessage.lowercased())].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(app_commonUrl.suffix(5)) + "pLiv" + String(data_modelMessage)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ProduceViewPushListener.decide().blockFemale()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(showLabPath) + String(notiBottomPath) + String(constSendUrl)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.beforeDemonstrate(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(k_iconUrl.suffix(4)) + "meFs") { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(showEachKey.prefix(7)) + String(app_fadeValue))] as? Int ?? -1
            //: if NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: noti_fileFormat, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: data_tabPath, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: noti_fileFormat, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(constTitleCountMessage.prefix(8)) + String(show_addGroupPath.suffix(5))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TargetThen.successInvite().standIn(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(app_handleKey.prefix(4)) + "tifica" + String(data_viewMsg.suffix(6)) + "pupWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            PopupReactiveCompatible.shared.ascertain(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(mainVideoUrl.suffix(5)) + String(user_bottomMsg)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ProduceViewPushListener.decide().blockFemale()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ConstraintEventHandler.bindTo().userRender()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: mainAddTitle, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension BankManagerSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func chemicalElement(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (main_sourceStr.replacingOccurrences(of: "voice", with: "e") + "estCall") || cmdName == (String(mainGuideText) + userEquipmentContent.replacingOccurrences(of: "live", with: "s") + "Call") {
            //: if TalkingLiveManager.shared().isLive == true {
            if ProduceViewPushListener.decide().isLive == true {
                //: NEED_RESTORE_LIVE = true
                show_detailIndexScreenValue = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                ProduceViewPushListener.nearSubCompletion(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                ProduceViewPushListener.decide().blockFemale()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard show_detailIndexScreenValue == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(user_progressContent.prefix(7)) + kAwakeData.replacingOccurrences(of: "collection", with: "l")) || cmdName == (String(userWorkName) + appTimeIndexUrl.replacingOccurrences(of: "filter", with: "l")) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(main_emptyLabelFormat)) || cmdName == (String(appAllKey.prefix(5)) + String(mainMakeTitle)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(noti_countName)) || cmdName == (String(showClickMsg) + show_effectFormat.replacingOccurrences(of: "user", with: "ll"))
        {
            //: NEED_RESTORE_LIVE = false
            show_detailIndexScreenValue = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = CharacteristicRootOfASquareMatrixView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                ProduceViewPushListener.decide().conversationPath()
            }
            //: alert.show()
            alert.drogue()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func giftEnableData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(kCellKey.lowercased() + String(showMainName.prefix(5)) + "Times")] as? Int {
            //: NowInfoThen.share.loginUserMode.freeCallTimes = freeCallTimes
            NowInfoThen.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(notiAddId) + mainKitUpPath.lowercased())] as? Bool
        //: if freeCallWin == true && NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ContainerEnableThen()
            //: view.show()
            view.toPop()
            //: view.setShowData(data: data)
            view.dataWithModel(data: data)
        }
    }
}
