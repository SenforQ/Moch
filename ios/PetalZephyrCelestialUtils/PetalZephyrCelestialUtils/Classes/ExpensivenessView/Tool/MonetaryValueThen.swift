
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let kBlockMessage:String = "mf/butrue table selected"
fileprivate let data_lineHiddenFormat:String = "ss/giftLdefine margin"
fileprivate let constValueMsg:[Character] = ["i","s","t"]

/*: "type" :*/
fileprivate let user_viewLiveModelValue:[UInt8] = [0x8d,0x92,0x89,0x7e]

fileprivate func outsideDisk(text num: UInt8) -> UInt8 {
    let value = Int(num) + 231
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let appBackPath:String = "mf/cguard your title"
fileprivate let const_playerPopTitle:[Character] = ["h","a","t","R","o","o","m","/","g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let noti_addValue:[Character] = ["l","i","v","e","/","g","i","f","t","L","i","s"]
fileprivate let main_kitMakeFormat:[Character] = ["t"]

/*: "party/giftList" :*/
fileprivate let user_eventLabMsg:[Character] = ["p","a","r","t","y","/","g","i","f","t","L","i","s","t"]

/*: "IM/getMessageList" :*/
fileprivate let showFrameData:[Character] = ["I","M","/","g","e","t","M","e","s","s","a"]
fileprivate let k_momentValue:String = "geListname message"

/*: "mf/business/sendMsg" :*/
fileprivate let const_titleUrl:String = "mf/buin scale text to background"
fileprivate let constEqualPath:String = "title open views/sen"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let show_numberName:String = "result"
fileprivate let showEffectId:String = "equal make effect head/chatR"
fileprivate let userValueId:String = "endMsgfalse play range"

/*: "mf/business/sendGift" :*/
fileprivate let notiPressAppValue:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","G"]
fileprivate let notiActualFormat:String = "ifblock"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let noti_pathMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","G","i"]
fileprivate let appNoValue:String = "fstatus"

/*: "live/sendGift" :*/
fileprivate let const_decideUrl:String = "black height push none oflive/"
fileprivate let showInputContent:String = "color"

/*: "party/sendGift" :*/
fileprivate let data_screenTargetMsg:[Character] = ["p","a","r","t","y","/","s","e","n","d","G","i","f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let user_prepareTitle:String = "mf/useme pop"
fileprivate let app_effectStr:[Character] = ["C"]
fileprivate let kSizeStr:[Character] = ["h","a","t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let show_styleData:String = "IM/gecase self make"
fileprivate let k_makeValue:String = "model extension modelageUserIn"
fileprivate let dataTableLayerId:[Character] = ["f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let showViewMsg:[Character] = ["m","f","/","c","r"]
fileprivate let appJoinId:String = "view in varush/w"
fileprivate let userSupportData:String = "text leadingeMeList"

/*: "mf/crush/ignore" :*/
fileprivate let notiModeText:String = "component video make whitemf/c"
fileprivate let data_gestureStr:String = "/ignoreplayer size"

/*: "mf/crush/like" :*/
fileprivate let dataLiveTitle:String = "mf/cby data section shadow"
fileprivate let userMentionMsg:String = "/likecell list content color match"

/*: "mf/user/getRelation" :*/
fileprivate let app_playerValue:String = "title object infomf/us"
fileprivate let dataTitleStr:String = "pic next color ifetRe"

/*: "user/attention" :*/
fileprivate let showListStr:[UInt8] = [0x16,0x10,0x6,0x11,0x4c,0x2,0x17,0x17,0x6,0xd,0x17,0xa,0xc,0xd]

/*: "user/removeAttention" :*/
fileprivate let const_modeResultMsg:String = "uschanger"
fileprivate let show_imageMessage:String = "oveAttdisable player class sample"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let appDataId:[UInt8] = [0x48,0x66,0x73,0x2c,0x79,0x25,0x6a,0x66,0x77,0x73,0x25,0x75,0x74,0x6e,0x73,0x79,0x78,0x25,0x6e,0x6b,0x25,0x7e,0x74,0x7a,0x25,0x6b,0x74,0x71,0x71,0x74,0x7c,0x25,0x6a,0x66,0x68,0x6d,0x25,0x74,0x79,0x6d,0x6a,0x77,0x83]

fileprivate func arrayPath(after num: UInt8) -> UInt8 {
    let value = Int(num) - 5
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "follow Success" :*/
fileprivate let notiAppUrl:String = "FOLL"
fileprivate let app_sharedTitle:String = "ccactual"
fileprivate let constSaveLevelValue:String = "border"

/*: "unfollow Success" :*/
fileprivate let user_arrayFormat:String = "unshowo"
fileprivate let app_countDirectionStr:[Character] = [" ","S","u","c","c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let kInputStr:String = "tranframe"
fileprivate let noti_normalKey:String = "mlabg"

/*: "msgId" :*/
fileprivate let app_modelFormat:[UInt8] = [0x67,0x79,0x6d,0x43,0x6e]

private func waterRank(block num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "targetLng" :*/
fileprivate let app_locationAddStr:[UInt8] = [0xa0,0xb5,0xa6,0xb3,0xb1,0xa0,0x98,0xba,0xb3]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let noti_userName:String = "index guard data actionmf/g"
fileprivate let notiOfSuiteFormat:String = "app any var handlegetAu"
fileprivate let kSharedFormat:String = "eetCequal extension"
fileprivate let noti_cellUrl:String = "image"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let appEventNamePath:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","I","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let showClearMsg:[UInt8] = [0xc8,0xd3,0xe9,0xd5,0xd8]

private func equalPairData(player num: UInt8) -> UInt8 {
    return num ^ 188
}

/*: "mf/chatRoom/info" :*/
fileprivate let constViewValue:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let data_styleKey:String = "let first make explain/info"

/*: "mf/chatRoom/list" :*/
fileprivate let appToFormat:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let k_labelData:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let data_belowUrl:[UInt8] = [0x5a,0x51,0x51,0x50,0x31,0x64,0x60,0x5e,0x4d,0x35,0x5a,0x52,0x5b]

fileprivate func kitTitle(layer num: UInt8) -> UInt8 {
    let value = Int(num) - 236
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/check" :*/
fileprivate let kTextTitle:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let data_dismissName:String = "video string equal without titleom/"
fileprivate let showStyleName:String = "cpopeck"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let show_bottomPriceName:String = "mf/chatRself error color"
fileprivate let constCareFormat:[Character] = ["o","o","m","/","j","o","i","n","O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let dataViewText:String = "mf/chaview color size key type"
fileprivate let k_enterData:[Character] = ["/","m","e","m","b","e","r","s"]

/*: "translate/text" :*/
fileprivate let showEmptyData:String = "TRANSL"
fileprivate let k_hiddenFormat:String = "fileexfile"

/*: "text" :*/
fileprivate let userViewUrl:[UInt8] = [0x74,0x78,0x65,0x74]

/*: "mf/user/queryStatus" :*/
fileprivate let mainListStr:[Character] = ["m","f","/","u","s","e","r","/","q","u"]
fileprivate let show_tempDropMessage:String = "epath"
fileprivate let user_accountData:String = "yStatusview type path in"

/*: "toUids" :*/
fileprivate let user_modelName:[UInt8] = [0x8e,0x95,0xaf,0x93,0x9e,0x89]

private func sharedOf(custom num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "call/getCallTab" :*/
fileprivate let userGestureTitle:String = "call/gblack return"
fileprivate let user_keyMsg:[Character] = ["e","t","C","a","l","l","T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let app_regularUrl:[Character] = ["m","f","/","u","s","e","r","/","r","e","m","a","r","k"]

/*: "content" :*/
fileprivate let k_valueId:[UInt8] = [0xf5,0xf9,0xf8,0xe2,0xf3,0xf8,0xe2]

private func sharedEqual(array num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "top" :*/
fileprivate let kRawStr:[UInt8] = [0x9e,0x99,0x9a]

fileprivate func windowRefresh(view num: UInt8) -> UInt8 {
    let value = Int(num) - 42
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MonetaryValueThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class MonetaryValueThen: NSObject {
public class MonetaryValueThen: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [LengthThen]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [LengthThen]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func completionDown(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(kBlockMessage.prefix(5)) + "sine" + String(data_lineHiddenFormat.prefix(8)) + String(constValueMsg))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: user_viewLiveModelValue.map{outsideDisk(text: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func afterTo(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(appBackPath.prefix(4)) + String(const_playerPopTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func followCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(noti_addValue) + String(main_kitMakeFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func errorVariantCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(user_eventLabMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func headPushDownStack(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(showFrameData) + String(k_momentValue.prefix(6)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func throughGift(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(const_titleUrl.prefix(5)) + "sines" + String(constEqualPath.suffix(5)) + "dMsg")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func holograph(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (show_numberName.replacingOccurrences(of: "result", with: "mf") + String(showEffectId.suffix(6)) + "oom/s" + String(userValueId.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func centerCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(notiPressAppValue) + notiActualFormat.replacingOccurrences(of: "block", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func futurismCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(noti_pathMsg) + appNoValue.replacingOccurrences(of: "status", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func clickAcrossTag(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(const_decideUrl.suffix(5)) + "sendGif" + showInputContent.replacingOccurrences(of: "color", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func field(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(data_screenTargetMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func scale(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(user_prepareTitle.prefix(6)) + "r/get" + String(app_effectStr) + String(kSizeStr))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func makeCompletionShared(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(show_styleData.prefix(5)) + "tMess" + String(k_makeValue.suffix(9)) + String(dataTableLayerId))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<LengthThen>.deserialize(from: result as? Array) {
            if let datas = Array<LengthThen>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [LengthThen])
                completion(succeed, datas as! [LengthThen])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func exaggerateCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        dataVoiceKey.product(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(showViewMsg) + String(appJoinId.suffix(5)) + "hoLik" + String(userSupportData.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func prototypical(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(notiModeText.suffix(4)) + "rush" + String(data_gestureStr.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func dataCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(dataLiveTitle.prefix(4)) + "rush" + String(userMentionMsg.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tableOfNames(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(app_playerValue.suffix(5)) + "er/g" + String(dataTitleStr.suffix(4)) + "lation")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func qualityUp(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: showListStr.map{$0^99}, encoding: .utf8)! : (const_modeResultMsg.replacingOccurrences(of: "change", with: "e") + "/rem" + String(show_imageMessage.prefix(6)) + "ention")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if NowInfoThen.share.loginUserMode.sex == Gender.female.rawValue {
                    if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        dataNameMessage.beforeDemonstrate(showMsg: String(bytes: appDataId.map{arrayPath(after: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        dataNameMessage.beforeDemonstrate(showMsg: (notiAppUrl.lowercased() + "ow Su" + app_sharedTitle.replacingOccurrences(of: "actual", with: "es") + constSaveLevelValue.replacingOccurrences(of: "border", with: "s")).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    dataNameMessage.beforeDemonstrate(showMsg: (user_arrayFormat.replacingOccurrences(of: "show", with: "f") + "llow" + String(app_countDirectionStr)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                dataNameMessage.addMsg(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func positionCompletion(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (kInputStr.replacingOccurrences(of: "frame", with: "sl") + "ate/" + noti_normalKey.replacingOccurrences(of: "lab", with: "s"))
        //: reqModel.params = ["msgId": msgId, "targetLng": LineThen.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: app_modelFormat.map{waterRank(block: $0)}, encoding: .utf8)!: msgId, String(bytes: app_locationAddStr.map{$0^212}, encoding: .utf8)!: LineThen.can(), String(bytes: user_viewLiveModelValue.map{outsideDisk(text: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func address(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(noti_userName.suffix(4)) + "reet/" + String(notiOfSuiteFormat.suffix(5)) + "toGr" + String(kSharedFormat.prefix(4)) + "onten" + noti_cellUrl.replacingOccurrences(of: "image", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func requestService(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(appEventNamePath))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: showClearMsg.map{equalPairData(player: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func plat(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(constViewValue) + String(data_styleKey.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func listImage(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(appToFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func selectExecuteSize(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: k_labelData.reversed(), encoding: .utf8)!: roomId, String(bytes: data_belowUrl.map{kitTitle(layer: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(kTextTitle) + String(data_dismissName.suffix(3)) + showStyleName.replacingOccurrences(of: "pop", with: "h"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func sessionCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(show_bottomPriceName.prefix(8)) + String(constCareFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func scrub(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(dataViewText.prefix(6)) + "tRoom" + String(k_enterData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func output(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (showEmptyData.lowercased() + "ate/" + k_hiddenFormat.replacingOccurrences(of: "file", with: "t"))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: userViewUrl.reversed(), encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func memberCompletion(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(mainListStr) + show_tempDropMessage.replacingOccurrences(of: "path", with: "r") + String(user_accountData.prefix(7)))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: user_modelName.map{sharedOf(custom: $0)}, encoding: .utf8)!: toUids, String(bytes: user_viewLiveModelValue.map{outsideDisk(text: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func awakeCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(userGestureTitle.prefix(6)) + String(user_keyMsg))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func giveUp(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(app_regularUrl))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: k_valueId.map{sharedEqual(array: $0)}, encoding: .utf8)!: content, String(bytes: kRawStr.map{windowRefresh(view: $0)}, encoding: .utf8)!: isTop, String(bytes: showClearMsg.map{equalPairData(player: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
