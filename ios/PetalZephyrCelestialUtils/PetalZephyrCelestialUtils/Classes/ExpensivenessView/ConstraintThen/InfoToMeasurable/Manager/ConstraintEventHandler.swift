
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let mainPushUntilText:[UInt8] = [0xed,0xfc,0xef,0xe9,0xe4,0xe2,0xee,0xe9,0xfc,0xe9,0xe8,0xee,0xe2,0xfe,0xf5,0xfc,0xf3,0xfa,0xf8,0xe2,0xf3,0xf2,0xe9,0xf4,0xfb,0xf4,0xfe,0xfc,0xe9,0xf4,0xf2,0xf3]

/*: "p- :*/
fileprivate let userAtKey:[Character] = ["p","-"]

/*: - :*/
fileprivate let const_pleaseValue:String = "moment"

/*: "redirectUrl" :*/
fileprivate let userBagCancelMessage:[Character] = ["r","e","d","i","r"]
fileprivate let noti_textFormat:String = "lab without content pair dataectUrl"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let showSucceedPath:[Character] = ["V","O","I","C","E","R","O","O","M",":"]
fileprivate let k_atKey:[Character] = [" ","进","\u{623f}","模","型","解","析","失","败","\u{3002}"]

/*: "Party room exception" :*/
fileprivate let noti_ofSizeStr:String = "Party rohead equal equal"
fileprivate let constListText:[Character] = ["o","m"," ","e","x","c","e","p","t","i","o","n"]

/*: _ :*/
fileprivate let notiAllowValue:String = "_"

/*: "male" :*/
fileprivate let data_frameUrl:[UInt8] = [0x5,0x9,0x4,0xd]

/*: "female" :*/
fileprivate let noti_itemWhiteKey:String = "feafterale"

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let showKitMessage:[Character] = ["V","O","I","C","E"]
fileprivate let app_rawFormat:[Character] = ["R","O","O","M",":"," ","群","\u{804a}","进","房","\u{5931}","败","。","g","r","o"]
fileprivate let user_playerData:[Character] = ["u","p","I","d",":"," "]

/*: ." :*/
fileprivate let app_locationMessage:[Character] = ["."]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let data_touchLayerValue:String = "VOIaddER"
fileprivate let const_iconContent:String = " 切房模\u{578b}"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let showQueryStr:[Character] = ["V","O","I","C","E","R"]
fileprivate let dataMakeColorName:String = "message letOOM: 群聊切"
fileprivate let user_borderPath:String = "source temp time房失败。gr"

/*: "errCode" :*/
fileprivate let noti_chinKey:[UInt8] = [0x80,0x97,0x97,0xa6,0x8a,0x81,0x80]

/*: "custom_kickout_message" :*/
fileprivate let k_makePath:String = "videosto"
fileprivate let k_capSuccessStr:String = "ckout_minformation user"
fileprivate let app_tipUserValue:[Character] = ["e","s","s","a","g","e"]

/*: "closeRoom" :*/
fileprivate let show_playerId:String = "equal game data objectcloseRoom"

/*: "adminCloseRoom" :*/
fileprivate let mainViewEffectValue:[Character] = ["a","d","m","i","n","C","l","o","s","e","R","o","o","m"]

/*: "The room is closed, please go to another room" :*/
fileprivate let constGiftValue:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x72,0x65,0x68,0x74,0x6f,0x6e,0x61,0x20,0x6f,0x74,0x20,0x6f,0x67,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x73,0x6f,0x6c,0x63,0x20,0x73,0x69,0x20,0x6d,0x6f,0x6f,0x72,0x20,0x65,0x68,0x54]

/*: "OK" :*/
fileprivate let main_thanStr:String = "pathK"

/*: "You were kicked out of the room" :*/
fileprivate let constInsideMessage:[UInt8] = [0x8f,0xa5,0xab,0x56,0xad,0x9b,0xa8,0x9b,0x56,0xa1,0x9f,0x99,0xa1,0x9b,0x9a,0x56,0xa5,0xab,0xaa,0x56,0xa5,0x9c,0x56,0xaa,0x9e,0x9b,0x56,0xa8,0xa5,0xa5,0xa3]

fileprivate func maleLab(log num: UInt8) -> UInt8 {
    let value = Int(num) + 202
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let user_contentTitle:String = "VOImanagerE"
fileprivate let dataClickKey:String = "：进房\u{5931}\u{8d25}"
fileprivate let user_layerMsg:String = "var if rowID: "

/*: , error:  :*/
fileprivate let mainImageId:String = "window to, err"

/*: , reason:  :*/
fileprivate let kNameStr:String = "message use, reas"
fileprivate let main_valueShareText:String = "on: value let self equal data"

/*: , extendedData:  :*/
fileprivate let kCoverAllowValue:[Character] = [","," ","e","x","t","e","n","d","e","d","D"]
fileprivate let appGiftHeadAlwaysKey:[Character] = ["a","t","a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let constUpFormat:String = "pageIC"
fileprivate let kCoverFormat:String = "M: 当\u{524d}"
fileprivate let data_menuName:String = "，roomID: content let"

/*: , errorCode:  :*/
fileprivate let constLabelData:String = "view self model add, error"
fileprivate let k_userId:String = "center heightCode: "

/*: , streamID:  :*/
fileprivate let app_controlUrl:[Character] = [","," ","s","t","r","e","a","m","I"]
fileprivate let kTimeValue:[Character] = ["D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let data_explainUrl:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let kJumpName:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","拉","流","失","败","，","r","o","o","m","I","D",":"," "]

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let dataGiftValue:String = "VOcontent"
fileprivate let show_whiteValue:[Character] = [":"," ","\u{4fe1}","令","解","析","\u{5931}","败","\u{ff0c}","c","o","m","m"]
fileprivate let kEqualLabelId:String = "true front equipmentand: "

/*: , roomID:  :*/
fileprivate let user_statusStr:String = "view key in, room"
fileprivate let mainGreetUrl:String = "in s inID: "

/*: "data" :*/
fileprivate let const_tableTitle:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let constTingMsg:[Character] = ["p","o"]
fileprivate let dataAttachMessage:String = "sitiochannel"

/*: "xxxx--  :*/
fileprivate let data_managerStr:String = "xxxx-- register equal level"

/*: "opType" :*/
fileprivate let userWhiteHiddenUrl:String = "opTypview send class"
fileprivate let data_cookieMsg:String = "greet"

/*: "party:changeMike" :*/
fileprivate let noti_containerFormat:String = "party:chamake color self by filter"
fileprivate let appGiftBlockValue:String = "false user path in statusngeMike"

/*: "type" :*/
fileprivate let app_contentMakeName:String = "userpe"

/*: "toPosition" :*/
fileprivate let userChangeTitle:String = "break view true count selftoPos"

/*: "party:adminChangeMike" :*/
fileprivate let showSelectedValue:String = "parview"
fileprivate let main_memberData:[Character] = ["C","h","a","n","g","e","M","i","k","e"]

/*: "You were kicked off the seat" :*/
fileprivate let show_hiddenContent:String = "true array name edit contrastYou "
fileprivate let showLabData:String = "ked ofto self let pass"
fileprivate let constPriceData:String = "share titlee seat"

/*: "party:changeCoin" :*/
fileprivate let main_errorName:String = "list to with object styleparty:"
fileprivate let noti_packageData:String = "equal celleCoi"
fileprivate let show_lengthKey:String = "phone"

/*: "party:closeStream" :*/
fileprivate let dataAddFormat:[Character] = ["p","a","r","t","y",":","c","l","o","s","e","S"]
fileprivate let user_photoTitle:String = "trcomponentm"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ConstraintEventHandler.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let mainBarData = NSNotification.Name(rawValue: String(bytes: mainPushUntilText.map{$0^189}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let show_userId = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum VideoThreadPropertyProtocol: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class ConstraintEventHandler: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = HeritageHandyJSON()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: mainBarData, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: ConstraintEventHandler?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: MinimumContainerThen?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: InfoToMeasurable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = addPosition()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func bindTo() -> ConstraintEventHandler {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = ConstraintEventHandler()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func alongMedium() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if ConstraintEventHandler._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            ConstraintEventHandler._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: FillViewThen = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = FillViewThen.revetColorView()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.arrayVoiceAttribute()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension ConstraintEventHandler {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func halfStaffStatus(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        NameReqTool.mikeChangeTypeTranslationToVaporBathPosition(roomId: partyModel.roomId,
                                                                             //: uid: Int(NowInfoThen.share.loginUid) ?? 0,
                                                                             uid: Int(NowInfoThen.share.loginUid) ?? 0,
                                                                             //: type: type,
                                                                             type: type,
                                                                             //: position: position,
                                                                             position: position,
                                                                             //: toPos: toPosition)
                                                                             toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.charterMagnitudeeraction()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = InfoToMeasurable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.tegument(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.charterMagnitudeeraction()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func statusLocation(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        NameReqTool.expected(roomId: partyModel.roomId,
                                         //: uid: uid,
                                         uid: uid,
                                         //: type: type,
                                         type: type,
                                         //: position: position)
                                         position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func charterMagnitudeeraction() -> String {
        //: return "p-\(NowInfoThen.share.loginUid)-\(partyModel.roomId)"
        return "p-\(NowInfoThen.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension ConstraintEventHandler {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func mentalPictureId(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LengthPermissionTool.actionCheckStub(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.addMsg(showMsg: const_limitTitle)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            NameReqTool.rowEnable(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: TotalReactiveCompatible.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            TotalReactiveCompatible.share.constraintImage(urlStr: result[(String(userBagCancelMessage) + String(noti_textFormat.suffix(6)))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.addMsg(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTarget == false else {
                guard BankManagerSocketDelegate.shared.isTarget == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.addMsg(showMsg: dataMessageName)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = HeritageHandyJSON.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    WoodLogTool.knowHow(msg: (String(showSucceedPath) + String(k_atKey)))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.addMsg(showMsg: (String(noti_ofSizeStr.prefix(8)) + String(constListText)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = MinimumContainerThen()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.controllerEarnModel()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.faceView()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.searchedTo()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                dataMatchStr.isometric(eventID: "\(main_logMsg)_\(NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue ? String(bytes: data_frameUrl.map{$0^104}, encoding: .utf8)! : (noti_itemWhiteKey.replacingOccurrences(of: "after", with: "m")))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                BankManagerEnableReactiveCompatible.anSufficient(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        WoodLogTool.knowHow(msg: (String(showKitMessage) + String(app_rawFormat) + String(user_playerData)) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.addMsg(showMsg: (String(noti_ofSizeStr.prefix(8)) + String(constListText)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.userRender()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func voiceToCubbyId(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        NameReqTool.subIndex(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.bulgeOut()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == NowInfoThen.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == NowInfoThen.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = HeritageHandyJSON.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                WoodLogTool.knowHow(msg: (data_touchLayerValue.replacingOccurrences(of: "add", with: "C") + "OOM:" + const_iconContent + "解析失败。"))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.addMsg(showMsg: (String(noti_ofSizeStr.prefix(8)) + String(constListText)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = MinimumContainerThen()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.controllerEarnModel()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.faceView()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.cellObject(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            dataMatchStr.isometric(eventID: "\(main_logMsg)_\(NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue ? String(bytes: data_frameUrl.map{$0^104}, encoding: .utf8)! : (noti_itemWhiteKey.replacingOccurrences(of: "after", with: "m")))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            BankManagerEnableReactiveCompatible.anSufficient(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    WoodLogTool.knowHow(msg: (String(showQueryStr) + String(dataMakeColorName.suffix(8)) + String(user_borderPath.suffix(6)) + "oupId: ") + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.addMsg(showMsg: (String(noti_ofSizeStr.prefix(8)) + String(constListText)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.userRender()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension ConstraintEventHandler {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func vocalismMini() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.animated()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func arrayVoiceAttribute() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = buttonController() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: LengthViewController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func userRender() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.bulgeOut() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.halfStaffStatus(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            ConstraintEventHandler.alongMedium() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension ConstraintEventHandler {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func searchedTo() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = LineThen.getVoiceRoomAppId()
        profile.appID = LineThen.actuate()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: NowInfoThen.share.loginUid)
        let user = ZegoUser(userID: NowInfoThen.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func cellObject(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension ConstraintEventHandler: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            NameReqTool.tableStop(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.mike(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.quantityAccept()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == VideoThreadPropertyProtocol.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.halfStaffStatus(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                NameReqTool.tableStop(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.mike(key: key, model: value)
                        //: if String(value.uid) == NowInfoThen.share.loginUid {
                        if String(value.uid) == NowInfoThen.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.charterMagnitudeeraction()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.quantityAccept()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                dataMatchStr.dismissEnable(eventID: appMatchUrl, parameterStr: [String(bytes: noti_chinKey.map{$0^229}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(k_makePath.replacingOccurrences(of: "video", with: "cu") + "m_ki" + String(k_capSuccessStr.prefix(7)) + String(app_tipUserValue))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (String(show_playerId.suffix(9))) || value == (String(mainViewEffectValue)) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    userRender()
                    //: let config = ShowAlertConfig()
                    let config = MagnitudeAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    PileThen.refuseLay(message: String(bytes: constGiftValue.reversed(), encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: showTitleText, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    addMsg(showMsg: String(bytes: constInsideMessage.map{maleLab(log: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                WoodLogTool.knowHow(msg: (user_contentTitle.replacingOccurrences(of: "manager", with: "C") + "ROOM" + dataClickKey + "，room" + String(user_layerMsg.suffix(4))) + "\(roomID)" + (String(mainImageId.suffix(5)) + "or: ") + "\(errorCode)" + (String(kNameStr.suffix(6)) + String(main_valueShareText.prefix(4))) + "\(reason)" + (String(kCoverAllowValue) + String(appGiftHeadAlwaysKey)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                addMsg(showMsg: (String(noti_ofSizeStr.prefix(8)) + String(constListText)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                dataMatchStr.dismissEnable(eventID: appMatchUrl, parameterStr: [String(bytes: noti_chinKey.map{$0^229}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            userRender()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            WoodLogTool.knowHow(msg: (constUpFormat.replacingOccurrences(of: "page", with: "VO") + "EROO" + kCoverFormat + "用户推流失败" + String(data_menuName.prefix(9))) + "\(partyModel.roomId)" + (String(constLabelData.suffix(7)) + String(k_userId.suffix(6))) + "\(errorCode)" + (String(app_controlUrl) + String(kTimeValue)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            addMsg(showMsg: String(bytes: data_explainUrl.reversed(), encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = getWithPositionsData()
            //: for (position, model) in dict where String(model.uid) == NowInfoThen.share.loginUid {
            for (position, model) in dict where String(model.uid) == NowInfoThen.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                halfStaffStatus(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                cream(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                userRender()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            WoodLogTool.knowHow(msg: (String(kJumpName)) + "\(partyModel.roomId)" + (String(constLabelData.suffix(7)) + String(k_userId.suffix(6))) + "\(errorCode)" + (String(app_controlUrl) + String(kTimeValue)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                userRender()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.soundDegreeFrontAnimationLevelStrength(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = getWithPositionsData()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.soundDegreeFrontAnimationLevelStrength(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            WoodLogTool.knowHow(msg: (dataGiftValue.replacingOccurrences(of: "content", with: "IC") + "EROOM" + String(show_whiteValue) + String(kEqualLabelId.suffix(5))) + "\(command)" + (String(user_statusStr.suffix(6)) + String(mainGreetUrl.suffix(4))) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(String(const_tableTitle))][(String(constTingMsg) + dataAttachMessage.replacingOccurrences(of: "channel", with: "n"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < show_userId else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = InfoToMeasurable.deserialize(from: json[(String(const_tableTitle))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(data_managerStr.prefix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(userWhiteHiddenUrl.prefix(5)) + data_cookieMsg.replacingOccurrences(of: "greet", with: "e"))].stringValue == (String(noti_containerFormat.prefix(9)) + String(appGiftBlockValue.suffix(7))) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(String(const_tableTitle))][(app_contentMakeName.replacingOccurrences(of: "user", with: "ty"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                tegument(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = assemblageView(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    cream(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                cream(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(String(const_tableTitle))][(String(userChangeTitle.suffix(5)) + "ition")].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                tegument(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                tegument(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.firstEventMoment()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(userWhiteHiddenUrl.prefix(5)) + data_cookieMsg.replacingOccurrences(of: "greet", with: "e"))].stringValue == (showSelectedValue.replacingOccurrences(of: "view", with: "ty") + ":admin" + String(main_memberData)) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(String(const_tableTitle))][(app_contentMakeName.replacingOccurrences(of: "user", with: "ty"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                tegument(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = assemblageView(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    cream(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.firstEventMoment()
                    //: guard String(oldModel.uid) == NowInfoThen.share.loginUid else { return }
                    guard String(oldModel.uid) == NowInfoThen.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    addMsg(showMsg: (String(show_hiddenContent.suffix(4)) + "were kic" + String(showLabData.prefix(6)) + "f th" + String(constPriceData.suffix(6))).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                tegument(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == NowInfoThen.share.loginUid else { return }
                guard String(mikeModel.uid) == NowInfoThen.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                tegument(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == NowInfoThen.share.loginUid else { return }
                guard String(mikeModel.uid) == NowInfoThen.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(userWhiteHiddenUrl.prefix(5)) + data_cookieMsg.replacingOccurrences(of: "greet", with: "e"))].stringValue == (String(main_errorName.suffix(6)) + "chang" + String(noti_packageData.suffix(4)) + show_lengthKey.replacingOccurrences(of: "phone", with: "n")) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            ConstraintEventHandler.bindTo().getWithPositionsData().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    tegument(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(userWhiteHiddenUrl.prefix(5)) + data_cookieMsg.replacingOccurrences(of: "greet", with: "e"))].stringValue == (String(dataAddFormat) + user_photoTitle.replacingOccurrences(of: "component", with: "ea")) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension ConstraintEventHandler {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func tegument(position: Int, model: InfoToMeasurable) {
        //: modifyMikePosition(key: position, model: model)
        mike(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.statuteTextPosition(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        pull(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func cream(position: Int, model: InfoToMeasurable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        mike(key: position, model: InfoToMeasurable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.statuteTextPosition(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        pull(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func pull(uid: Int) {
        //: guard String(uid) == NowInfoThen.share.loginUid else { return }
        guard String(uid) == NowInfoThen.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = getWithPositionsData()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.equivalence()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension ConstraintEventHandler {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func addPosition() -> [Int: InfoToMeasurable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< show_userId {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            mike(key: index, model: InfoToMeasurable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func mike(key: Int, model: InfoToMeasurable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func assemblageView(key: Int) -> InfoToMeasurable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func getWithPositionsData() -> [Int: InfoToMeasurable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
