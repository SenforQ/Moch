
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userPlayerId:[UInt8] = [0xc2,0xc5,0xc2,0xdf,0x83,0xc8,0xc4,0xcf,0xce,0xd9,0x91,0x82,0x8b,0xc3,0xca,0xd8,0x8b,0xc5,0xc4,0xdf,0x8b,0xc9,0xce,0xce,0xc5,0x8b,0xc2,0xc6,0xdb,0xc7,0xce,0xc6,0xce,0xc5,0xdf,0xce,0xcf]

/*: "uid" :*/
fileprivate let appToPath:[UInt8] = [0xbd,0xb1,0xac]

fileprivate func arrayApp(card num: UInt8) -> UInt8 {
    let value = Int(num) - 72
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let userStyleMessage:String = "title toheadPic"

/*: "nickname" :*/
fileprivate let constModelOffValue:String = "nimomentkna"
fileprivate let kCurrentFormat:String = "ME"

/*: "birthday" :*/
fileprivate let user_formatUrl:String = "bequal"
fileprivate let k_propertyMsg:String = "rtcontextay"

/*: "videoUrl" :*/
fileprivate let show_bottomGreetContent:String = "videoUrlmake manager with add equal"

/*: "cover" :*/
fileprivate let mainInfoSexFormat:String = "modelover"

/*: "location" :*/
fileprivate let app_viewValue:String = "locaediti"
fileprivate let showBackgroundText:[Character] = ["o","n"]

/*: "cmd" :*/
fileprivate let userNameKey:[UInt8] = [0x19,0x17,0x1e]

private func indexView(from num: UInt8) -> UInt8 {
    return num ^ 122
}

/*: "refuseCall" :*/
fileprivate let mainLoadFormat:String = "rescales"
fileprivate let kDataName:[Character] = ["e","C","a","l","l"]

/*: "data" :*/
fileprivate let main_contentValue:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let data_nameFormat:String = "Call cell list"
fileprivate let k_makeAddValue:String = "rrstartion"

/*: "stopCall" :*/
fileprivate let noti_layerLocationMsg:[Character] = ["s","t"]
fileprivate let app_blockTitle:String = "up for bottomopCall"

/*: "logId" :*/
fileprivate let dataToCurName:[UInt8] = [0xc4,0xc7,0xbf,0xa1,0xbc]

fileprivate func equalAttach(start num: UInt8) -> UInt8 {
    let value = Int(num) + 168
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let notiEnableIconFormat:[Character] = ["c","a","n","c","e","l","C","a"]
fileprivate let appToTitle:[Character] = ["l","l"]

/*: "No answer" :*/
fileprivate let show_equalResultText:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let show_colorUrl:String = "durmodel"
fileprivate let app_collectionRefreshData:[Character] = ["i","o","n"]

/*: "She has hung up" :*/
fileprivate let data_cornerName:String = "She empty limit self frame data"
fileprivate let mainPlayerId:[Character] = [" ","u","p"]

/*: "cancelSex" :*/
fileprivate let noti_viewText:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let app_statusValue:String = "file let fieldHe ha"

/*: "endType" :*/
fileprivate let kModelStr:String = "else containerendType"

/*: "stopSex" :*/
fileprivate let appTrackFormat:String = "to frame title string resultstopSe"
fileprivate let appViewMediumData:[Character] = ["x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let appQuitStr:[UInt8] = [0x40,0x67,0x7a,0x7c,0x6f,0x6f,0x60,0x6a,0x60,0x6c,0x67,0x7d,0x29,0x6e,0x66,0x65,0x6d,0x29,0x6a,0x66,0x60,0x67,0x7a,0x25,0x29,0x6a,0x68,0x65,0x65,0x29,0x61,0x68,0x7a,0x29,0x6c,0x67,0x6d,0x6c,0x6d]

private func listSource(view num: UInt8) -> UInt8 {
    return num ^ 9
}

/*: "The other hung up the phone" :*/
fileprivate let appUserKey:[Character] = ["T","h","e"," ","o"]
fileprivate let notiManagerPath:String = "model forther "
fileprivate let show_modelDetailPath:String = "app social kindthe p"

/*: "type" :*/
fileprivate let dataRecordValue:String = "addpe"

/*: "jumpUrl" :*/
fileprivate let data_stylePath:String = "at key import videojumpUrl"

/*: "Cancel" :*/
fileprivate let showReplyVoiceUrl:String = "Cancelleft share"

/*: "Get gold coins" :*/
fileprivate let const_withUrl:String = "self icon make list liveGet go"
fileprivate let user_endCosUrl:String = "messageins"

/*: "addBean" :*/
fileprivate let dataArrayFormat:String = "some pop backgroundaddBean"

/*: "refuseSex" :*/
fileprivate let noti_bottomData:String = "rewith"
fileprivate let mainDeviceData:[Character] = ["x"]

/*: "onAcceptCall" :*/
fileprivate let show_labFormat:String = "color count true talk frontonAcce"
fileprivate let userViewKey:[Character] = ["l","l"]

/*: "acceptCall" :*/
fileprivate let show_windowModelMessage:String = "equipmentcce"
fileprivate let noti_keyGiftMessage:[Character] = ["p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let main_backgroundText:String = "leading model colorCall"
fileprivate let mainCountUrl:String = "nceshowshowed"

/*: "The call has ended" :*/
fileprivate let dataGiftMsg:[Character] = ["T","h","e"," ","c","a","l","l"," ","h"]
fileprivate let appLabName:[Character] = ["a","s"]
fileprivate let notiBeautyName:String = " endederror pop info fill"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BeautyOfViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let mainVideoTableText = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class BeautyOfViewController: TailRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: NameChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CellPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: ChangeReactiveCompatible? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: SaveThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        BankManagerSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        BankManagerSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        BankManagerSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userPlayerId.map{$0^171}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MisinformationReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.equalWithBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            direction()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            freshId(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.tillImpact()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        fortify()
        //: setupSubViewsConstraint()
        lowStatus()
        //: initNotificationObserver()
        enableDismiss()
        //: req_callGetUserInfo()
        titleChoice()
        //: timeoutRules()
        timeoutColor()
        //: setUpBGMPlayer()
        originSet()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.preview()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if NowInfoThen.share.appConfigMode.disableShootScreen {
            if NowInfoThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TrifleShieldView(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: user_deviceId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTarget = false
        BankManagerSocketDelegate.shared.isTarget = false
        //: self.stopVCAllActivties()
        self.roundThanCustom()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: LengthVideoView = {
        //: let v = TalkingBGVideoView()
        let v = LengthVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.skirtShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: ViewManagerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ViewManagerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: MusicView = {
        //: let v = TalkingVideoWindowView()
        let v = MusicView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = constExplainViewMsg - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if WithLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: appImageLimitPath + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.nationalService()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: TextThen = {
        //: let m = TalkingVideoChatManager()
        let m = TextThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func titleChoice() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        TextThen.gestureOf(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: appToPath.map{arrayApp(card: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(userStyleMessage.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(constModelOffValue.replacingOccurrences(of: "moment", with: "c") + kCurrentFormat.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(user_formatUrl.replacingOccurrences(of: "equal", with: "i") + k_propertyMsg.replacingOccurrences(of: "context", with: "hd"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(show_bottomGreetContent.prefix(8)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(mainInfoSexFormat.replacingOccurrences(of: "model", with: "c"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(app_viewValue.replacingOccurrences(of: "edit", with: "t") + String(showBackgroundText))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.cropInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.capacity()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func capacity() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = AddSubordinateCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = AddSubordinateCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.tableEffect(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.equalHide(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func direction() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        LengthPermissionTool.nearGift { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.addMsg(showMsg: const_limitTitle)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (mainLoadFormat.replacingOccurrences(of: "scale", with: "fu") + String(kDataName)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: appToPath.map{arrayApp(card: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.backgroundAnimated()
                //: return
                return
            }

            //: self.startPreview()
            self.preview()
            //: self.stopBGM()
            self.barTo()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CellPlayer.translation(type: .Response)
            //: self.startBGM()
            self.equalWithBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func preview() {
        //: self.videoManager.startPreview()
        self.videoManager.cause()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func roundThanCustom() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.barTo()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TargetThen.videoResource()
        //: TalkingSocketManager.shared.callerDelegate = nil
        BankManagerSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        BankManagerSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        BankManagerSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func backgroundAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.barTo()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func nationalService() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.awake()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.capacity()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func useObject() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.shouldByPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.capacity()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.bringHomeTurnGroup()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func bringHomeTurnGroup() {
        //: guard NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = SaveThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.invite()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func enableDismiss() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(throwPlay(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(constraintNotification(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: user_useId,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(streetSmartNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: userDetailName,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        mainVideoTableText.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func throwPlay(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            backgroundAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func streetSmartNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard BankManagerSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        BankManagerSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        BankManagerSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        BankManagerSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.frameworkEdit()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func constraintNotification(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.capacity()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension BeautyOfViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.addMsg(showMsg: (String(data_nameFormat.prefix(5)) + "inte" + k_makeAddValue.replacingOccurrences(of: "star", with: "up")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (String(noti_layerLocationMsg) + String(app_blockTitle.suffix(6))), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (String(notiEnableIconFormat) + String(appToTitle)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (mainLoadFormat.replacingOccurrences(of: "scale", with: "fu") + String(kDataName)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: appToPath.map{arrayApp(card: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.imageCustom()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                BankManagerSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            backgroundAnimated()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func originSet() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? TextPlayerType.Call : TextPlayerType.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CellPlayer.translation(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func equalWithBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.roundComponent()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func barTo() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.clickStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeoutColor() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? mainNoName : self.chatModel?.receiveTitle()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            holderFrame()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.holderFrame()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func holderFrame() {
        //: guard !TalkingSocketManager.shared.isTarget else { return }
        guard !BankManagerSocketDelegate.shared.isTarget else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.addMsg(showMsg: (String(show_equalResultText)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (String(notiEnableIconFormat) + String(appToTitle)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (mainLoadFormat.replacingOccurrences(of: "scale", with: "fu") + String(kDataName)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: appToPath.map{arrayApp(card: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        BankManagerSocketDelegate.shared.appFrom(info: socketDict)
        //: popCurrentViewController()
        backgroundAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func biology(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(show_colorUrl.replacingOccurrences(of: "model", with: "at") + String(app_collectionRefreshData))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.titleTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.via()
            //: createTalkRoom()
            useObject()
            //: self.stopBGM()
            self.barTo()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func freshId(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.awake()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.skirtShow(show: false)
        //: self.stopBGM()
        self.barTo()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.via()
        //: self.createTalkRoom()
        self.useObject()
    }
}

// MARK: - NameObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension BeautyOfViewController: NameObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func eigenvalueOfASquareMatrix(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(data_cornerName.prefix(4)) + "has hung" + String(mainPlayerId)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(noti_viewText))].stringValue == PointExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(app_statusValue.suffix(5)) + "s hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.addMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.imageCustom()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        backgroundAnimated()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func session(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        freshId(logId: json[String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func modelPhone(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kModelStr.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(data_cornerName.prefix(4)) + "has hung" + String(mainPlayerId)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(appTrackFormat.suffix(6)) + String(appViewMediumData))].stringValue == PointExpressionConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(app_statusValue.suffix(5)) + "s hung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: appQuitStr.map{listSource(view: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(appUserKey) + String(notiManagerPath.suffix(5)) + "hung up " + String(show_modelDetailPath.suffix(5)) + "hone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.addMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.imageCustom()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.forefront()
        //: popCurrentViewController()
        backgroundAnimated()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func filterOfRequest(msg: String, data: [String: Any]) {
        //: guard NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(dataRecordValue.replacingOccurrences(of: "add", with: "ty"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(data_stylePath.suffix(7)))] as? String else { return }
            //: let httpsUrl = NowInfoThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = NowInfoThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ViewDetailConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TotalReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            TotalReactiveCompatible.share.constraintImage(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            PileThen.downCrosshead(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(showReplyVoiceUrl.prefix(6))).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(const_withUrl.suffix(6)) + "ld c" + user_endCosUrl.replacingOccurrences(of: "message", with: "o")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                PileThen.obstruct()
                //: } rightBlock: {
            } rightBlock: {
                //: TotalReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                TotalReactiveCompatible.share.musculusSphincterPupillae(clickEvent: notiMarginId, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func acceptToGift(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.patronym(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func moveCellData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(dataArrayFormat.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.makeTheme(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.way(point: addBean)
        }
    }
}

// MARK: - ActionVideoPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension BeautyOfViewController: ActionVideoPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func doTarget(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(data_cornerName.prefix(4)) + "has hung" + String(mainPlayerId)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(noti_bottomData.replacingOccurrences(of: "with", with: "fu") + "seSe" + String(mainDeviceData))].stringValue == PointExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(app_statusValue.suffix(5)) + "s hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.addMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.imageCustom()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        backgroundAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func sprechgesang(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        freshId(logId: json[String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - BotchErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension BeautyOfViewController: BotchErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func startAccountingData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (mainLoadFormat.replacingOccurrences(of: "scale", with: "fu") + String(kDataName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.addMsg(showMsg: (String(show_equalResultText)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.imageCustom()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                BankManagerSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            backgroundAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(show_labFormat.suffix(6)) + "ptCa" + String(userViewKey)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue else { return }
            //: TotalReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            TotalReactiveCompatible.share.musculusSphincterPupillae(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(show_labFormat.suffix(6)) + "ptCa" + String(userViewKey)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.addMsg(showMsg: msg)
            //: popCurrentViewController()
            backgroundAnimated()
        }
    }
}

// MARK: - TwoThreadObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension BeautyOfViewController: TwoThreadObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func actionOrientation() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (show_windowModelMessage.replacingOccurrences(of: "equipment", with: "a") + String(noti_keyGiftMessage)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: appToPath.map{arrayApp(card: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func picBy() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.addMsg(showMsg: (String(main_backgroundText.suffix(4)) + "s are ca" + mainCountUrl.replacingOccurrences(of: "show", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (String(notiEnableIconFormat) + String(appToTitle)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.addMsg(showMsg: (String(main_backgroundText.suffix(4)) + "s are ca" + mainCountUrl.replacingOccurrences(of: "show", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (mainLoadFormat.replacingOccurrences(of: "scale", with: "fu") + String(kDataName)), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: appToPath.map{arrayApp(card: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.addMsg(showMsg: (String(dataGiftMsg) + String(appLabName) + String(notiBeautyName.prefix(6))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: userNameKey.map{indexView(from: $0)}, encoding: .utf8)!: (String(noti_layerLocationMsg) + String(app_blockTitle.suffix(6))), String(bytes: main_contentValue.reversed(), encoding: .utf8)!: [String(bytes: dataToCurName.map{equalAttach(start: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        backgroundAnimated()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func makeByFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.colorByLive(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func tabForbidden(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.skirtShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.adjustCamera(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.capacity()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func join() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        BankManagerSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = ChangeReactiveCompatible.wisecrack()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.modusVivendiVoiceFrom()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.giftScreen()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.buttonController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        backgroundAnimated()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func publicTransport() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension BeautyOfViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func fortify() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.exceptView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func lowStatus() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
