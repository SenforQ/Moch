
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showAgeRowData:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let constContainerId:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let dataContentPath:[Character] = ["h","e"]
fileprivate let kBlockData:String = "self collection if name titleadPic"

/*: "nickname" :*/
fileprivate let constInsidePath:String = "imageickimage"
fileprivate let notiContentSumFormat:String = "amarrange"

/*: "birthday" :*/
fileprivate let kTopUrl:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let userPerFormat:String = "pathde"

/*: "location" :*/
fileprivate let notiToMsg:[Character] = ["l","o"]
fileprivate let data_hiddenValue:String = "makeation"

/*: "cmd" :*/
fileprivate let data_valueName:[UInt8] = [0x33,0x3d,0x34]

private func rowModel(equal num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "refuseCall" :*/
fileprivate let kTillPath:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let k_contentVoiceKey:String = "environmentl"

/*: "data" :*/
fileprivate let notiHiddenKey:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let show_viewFormat:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u"]
fileprivate let app_leadingPath:String = "pmanageron"

/*: "stopCall" :*/
fileprivate let noti_instanceUrl:String = "stopCalmy hidden"
fileprivate let app_segmentKey:String = "frame"

/*: "logId" :*/
fileprivate let show_addFormat:[UInt8] = [0xf1,0xf4,0xec,0xce,0xe9]

fileprivate func rankVar(voice num: UInt8) -> UInt8 {
    let value = Int(num) + 123
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let constCountStr:String = "CANCEL"

/*: "No answer" :*/
fileprivate let const_targetAdjustText:[Character] = ["N","o"," ","a","n","s"]
fileprivate let k_progressFormat:[Character] = ["w","e","r"]

/*: "duration" :*/
fileprivate let constSessionPath:[Character] = ["d","u","r","a","t"]
fileprivate let main_tapName:[Character] = ["i","o","n"]

/*: "She has hung up" :*/
fileprivate let showLabData:String = "size"
fileprivate let noti_textId:[Character] = ["h","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let dataMakePath:[Character] = ["c","a","n","c"]
fileprivate let appIndexPath:String = "elSexto awake value gallery"

/*: "He has hung up" :*/
fileprivate let app_normalPath:String = "He hframe index equal"
fileprivate let app_demonstrateCellId:String = "view nor available pic viewg up"

/*: "endType" :*/
fileprivate let showFemaleStr:String = "endTypeapp tap"

/*: "stopSex" :*/
fileprivate let kViewId:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let k_infoData:[UInt8] = [0x10,0x37,0x2a,0x2c,0x3f,0x3f,0x30,0x3a,0x30,0x3c,0x37,0x2d,0x79,0x3e,0x36,0x35,0x3d,0x79,0x3a,0x36,0x30,0x37,0x2a,0x75,0x79,0x3a,0x38,0x35,0x35,0x79,0x31,0x38,0x2a,0x79,0x3c,0x37,0x3d,0x3c,0x3d]

private func appearLittle(my num: UInt8) -> UInt8 {
    return num ^ 89
}

/*: "The other hung up the phone" :*/
fileprivate let data_viewKey:String = "self with currentThe o"
fileprivate let app_iconData:String = " hunleft label"
fileprivate let k_progressKey:String = "position function lab for sign the ph"
fileprivate let show_managerStr:[Character] = ["o","n","e"]

/*: "type" :*/
fileprivate let main_infoStr:String = "tyshadowe"

/*: "jumpUrl" :*/
fileprivate let user_playerFormat:String = "jumpUrlsize string"

/*: "Cancel" :*/
fileprivate let appListKey:String = "register frame collection level genderCancel"

/*: "Get gold coins" :*/
fileprivate let showFrameValue:String = "Get gogreet make show let"
fileprivate let appBeautyId:String = "oiraw"

/*: "addBean" :*/
fileprivate let user_extraFirmAppKey:String = "height collection var moveaddBean"

/*: "refuseSex" :*/
fileprivate let data_cameraMessage:String = "have"
fileprivate let const_barValue:String = "cancel in should right textfuseSex"

/*: "onAcceptCall" :*/
fileprivate let notiIndexTitle:String = "onAself make let"
fileprivate let mainEqualTitle:String = "return modelcceptCall"

/*: "acceptCall" :*/
fileprivate let app_upFormat:String = "apushept"

/*: "Calls are cancelled" :*/
fileprivate let k_countKey:String = "bottom match search live elseCalls"
fileprivate let app_centerFormat:String = " canctime self perform user"
fileprivate let k_appFormat:String = "ELLED"

/*: "The call has ended" :*/
fileprivate let kModeUrl:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n","d","e"]
fileprivate let user_movementMsg:String = "D"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViewEqualObserverDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let show_noMsg = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class ViewEqualObserverDelegate: TailRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: NameChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CellPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: MinimumMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        BankManagerSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        BankManagerSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        BankManagerSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showAgeRowData.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MisinformationReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.clickWithinAdd()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            aboveSizeAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.cach()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        atEqual()
        //: setupSubViewsConstraint()
        autochthonal()
        //: initNotificationObserver()
        lie()
        //: req_callGetUserInfo()
        pathCell()
        //: timeoutRules()
        nameBeauty()
        //: resumeTalk()
        commensurateTalk()
        //: setUpBGMPlayer()
        afterSet()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if NowInfoThen.share.appConfigMode.disableShootScreen {
            if NowInfoThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TrifleShieldView(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: user_deviceId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTarget = false
        BankManagerSocketDelegate.shared.isTarget = false
        //: self.stopVCAllActivties()
        self.daily()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: HiddenMagnitudeeractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = HiddenMagnitudeeractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: DistrictManagerCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = DistrictManagerCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func pathCell() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        TextThen.gestureOf(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: constContainerId.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(dataContentPath) + String(kBlockData.suffix(5)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(constInsidePath.replacingOccurrences(of: "image", with: "n") + notiContentSumFormat.replacingOccurrences(of: "arrange", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(kTopUrl))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(userPerFormat.replacingOccurrences(of: "path", with: "vi") + "oUrl")].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(notiToMsg) + data_hiddenValue.replacingOccurrences(of: "make", with: "c"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.isSucceed()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func aboveSizeAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LengthPermissionTool.actionCheckStub(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.addMsg(showMsg: const_limitTitle)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (String(kTillPath) + k_contentVoiceKey.replacingOccurrences(of: "environment", with: "al")), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: constContainerId.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.searchedTarget()
                //: return
                return
            }

            //: self.stopBGM()
            self.pastDismiss()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CellPlayer.translation(type: .Response)
            //: self.startBGM()
            self.clickWithinAdd()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func daily() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.pastDismiss()
        //: TalkingSocketManager.shared.callerDelegate = nil
        BankManagerSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        BankManagerSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        BankManagerSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func searchedTarget(animated: Bool = true) {
        //: self.stopBGM()
        self.pastDismiss()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func anyData() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.followInput(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func commensurateTalk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            anyData()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func lie() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(walkIn(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(successUser(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: userDetailName,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        show_noMsg.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func walkIn(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            searchedTarget()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func successUser(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard BankManagerSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        BankManagerSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        BankManagerSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        BankManagerSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.levelMake(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension ViewEqualObserverDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.addMsg(showMsg: (String(show_viewFormat) + app_leadingPath.replacingOccurrences(of: "manager", with: "ti")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (String(noti_instanceUrl.prefix(7)) + app_segmentKey.replacingOccurrences(of: "frame", with: "l")), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (constCountStr.lowercased() + "Call"), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (String(kTillPath) + k_contentVoiceKey.replacingOccurrences(of: "environment", with: "al")), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: constContainerId.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.imageCustom()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                BankManagerSocketDelegate.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            searchedTarget()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.withEnable(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func afterSet() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? TextPlayerType.Call : TextPlayerType.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CellPlayer.translation(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func clickWithinAdd() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.roundComponent()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func pastDismiss() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.clickStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func nameBeauty() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? mainNoName : self.chatModel?.receiveTitle()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            birdSEyeViewMake()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.birdSEyeViewMake()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func birdSEyeViewMake() {
        //: guard !TalkingSocketManager.shared.isTarget else { return }
        guard !BankManagerSocketDelegate.shared.isTarget else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.addMsg(showMsg: (String(const_targetAdjustText) + String(k_progressFormat)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (constCountStr.lowercased() + "Call"), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (String(kTillPath) + k_contentVoiceKey.replacingOccurrences(of: "environment", with: "al")), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: constContainerId.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        BankManagerSocketDelegate.shared.appFrom(info: socketDict)
        //: popCurrentViewController()
        searchedTarget()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func equalData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(constSessionPath) + String(main_tapName))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.dialTelephone(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.exquisiteness()
            //: createTalkRoom()
            anyData()
            //: self.stopBGM()
            self.pastDismiss()
        }
    }
}

// MARK: - NameObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension ViewEqualObserverDelegate: NameObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func eigenvalueOfASquareMatrix(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (showLabData.replacingOccurrences(of: "size", with: "S") + String(noti_textId)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(dataMakePath) + String(appIndexPath.prefix(5)))].stringValue == PointExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(app_normalPath.prefix(4)) + "as hun" + String(app_demonstrateCellId.suffix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.addMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.imageCustom()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        searchedTarget()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func session(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.pastDismiss()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.exquisiteness()
        //: self.createTalkRoom()
        self.anyData()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func modelPhone(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(showFemaleStr.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (showLabData.replacingOccurrences(of: "size", with: "S") + String(noti_textId)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kViewId))].stringValue == PointExpressionConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(app_normalPath.prefix(4)) + "as hun" + String(app_demonstrateCellId.suffix(4))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: k_infoData.map{appearLittle(my: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(data_viewKey.suffix(5)) + "ther" + String(app_iconData.prefix(4)) + "g up" + String(k_progressKey.suffix(7)) + String(show_managerStr)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.addMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.imageCustom()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.subDismiss()
        //: self.popCurrentViewController()
        self.searchedTarget()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: app_intimateKey, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func filterOfRequest(msg: String, data: [String: Any]) {
        //: guard NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(main_infoStr.replacingOccurrences(of: "shadow", with: "p"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(user_playerFormat.prefix(7)))] as? String else { return }
            //: let httpsUrl = NowInfoThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = NowInfoThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ViewDetailConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TotalReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            TotalReactiveCompatible.share.constraintImage(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            PileThen.downCrosshead(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(appListKey.suffix(6))).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(showFrameValue.prefix(6)) + "ld c" + appBeautyId.replacingOccurrences(of: "raw", with: "ns")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                PileThen.obstruct()
                //: } rightBlock: {
            } rightBlock: {
                //: TotalReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                TotalReactiveCompatible.share.musculusSphincterPupillae(clickEvent: notiMarginId, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func moveCellData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(user_extraFirmAppKey.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.tableSum(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.comparable(point: addBean)
        }
    }
}

// MARK: - ActionVideoPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ViewEqualObserverDelegate: ActionVideoPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func doTarget(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (showLabData.replacingOccurrences(of: "size", with: "S") + String(noti_textId)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(data_cameraMessage.replacingOccurrences(of: "have", with: "re") + String(const_barValue.suffix(7)))].stringValue == PointExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(app_normalPath.prefix(4)) + "as hun" + String(app_demonstrateCellId.suffix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.addMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.imageCustom()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            BankManagerSocketDelegate.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        searchedTarget()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func sprechgesang(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.pastDismiss()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.exquisiteness()
        //: self.createTalkRoom()
        self.anyData()
    }
}

// MARK: - BotchErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension ViewEqualObserverDelegate: BotchErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func startAccountingData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(kTillPath) + k_contentVoiceKey.replacingOccurrences(of: "environment", with: "al")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.addMsg(showMsg: (String(const_targetAdjustText) + String(k_progressFormat)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.imageCustom()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                BankManagerSocketDelegate.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.searchedTarget()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(notiIndexTitle.prefix(3)) + String(mainEqualTitle.suffix(9))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue else { return }
            //: TotalReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            TotalReactiveCompatible.share.musculusSphincterPupillae(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(notiIndexTitle.prefix(3)) + String(mainEqualTitle.suffix(9))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.addMsg(showMsg: msg)
            //: self.popCurrentViewController()
            self.searchedTarget()
        }
    }
}

// MARK: - CollectionObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension ViewEqualObserverDelegate: CollectionObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func betweenAccept() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (app_upFormat.replacingOccurrences(of: "push", with: "cc") + "Call"), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: constContainerId.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func onLoad() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.addMsg(showMsg: (String(k_countKey.suffix(5)) + " are" + String(app_centerFormat.prefix(5)) + k_appFormat.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (constCountStr.lowercased() + "Call"), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.addMsg(showMsg: (String(k_countKey.suffix(5)) + " are" + String(app_centerFormat.prefix(5)) + k_appFormat.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (String(kTillPath) + k_contentVoiceKey.replacingOccurrences(of: "environment", with: "al")), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: constContainerId.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.addMsg(showMsg: (String(kModeUrl) + user_movementMsg.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: data_valueName.map{rowModel(equal: $0)}, encoding: .utf8)!: (String(noti_instanceUrl.prefix(7)) + app_segmentKey.replacingOccurrences(of: "frame", with: "l")), String(bytes: notiHiddenKey.reversed(), encoding: .utf8)!: [String(bytes: show_addFormat.map{rankVar(voice: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.subDismiss()
        //: self.popCurrentViewController()
        self.searchedTarget()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func pageCell(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.taskAcross(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func speaker(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.placeHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func deliverDrunkMove() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        BankManagerSocketDelegate.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = MinimumMiniView.byAdd()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.buttonController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.outExample()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.maxTime(talkingTime)
        //: popCurrentViewController()
        searchedTarget()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func viewFor(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.maxTime(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func spaceDonation(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.coverFile(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension ViewEqualObserverDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func atEqual() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func autochthonal() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
