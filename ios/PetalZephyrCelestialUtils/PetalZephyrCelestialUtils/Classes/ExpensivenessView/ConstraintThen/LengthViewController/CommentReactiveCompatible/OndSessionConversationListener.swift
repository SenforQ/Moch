
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let const_fatalData:String = "IM\u{9000}出"
fileprivate let data_belowValue:String = "ode:manager gift with"

/*: , desc: :*/
fileprivate let k_viewSuccessFormat:[Character] = [","," "]
fileprivate let show_limitFormat:[Character] = ["d","e","s","c",":"]

/*: ." :*/
fileprivate let user_resultMessage:String = "session"

/*: "IM登录失败，code: :*/
fileprivate let data_managerMessage:String = "let groupIM登录失败"
fileprivate let showFaceMessage:[Character] = ["\u{ff0c}","c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let user_modelMessage:String = "IM\u{83b7}取"
fileprivate let showCircleData:String = "数\u{5931}败\u{ff0c}"

/*: "crushBadge" :*/
fileprivate let showStateMsg:[Character] = ["c","r","u","s","h","B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let app_selectionId:[UInt8] = [0x6a,0x69,0x6c,0x56,0x7d,0x75,0x6a,0x6d,0x7a]

fileprivate func monthAll(text num: UInt8) -> UInt8 {
    let value = Int(num) - 8
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let userSegmentData:String = "colorlaza"

/*: "roomNum" :*/
fileprivate let constManagerKey:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let main_spendValue:String = "iconm"

/*: "onlyOneRoomId" :*/
fileprivate let data_tillText:[Character] = ["o","n","l","y","O","n","e","R","o","o","m","I"]
fileprivate let main_videoUrl:String = "tab"

/*: "LIVE-CHAT" :*/
fileprivate let show_toKey:String = "LIVE-string succeed color let data"
fileprivate let appInstanceShareId:[Character] = ["C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let showMakeId:String = "pa"
fileprivate let userBindText:[Character] = ["R","T","Y","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let user_giftKey:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let data_voiceMsg:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let kMakeName:String = "app moment elsenewPk"
fileprivate let noti_pairId:String = "pop rategGift"

/*: "MF:ChatMentionMsg" :*/
fileprivate let appNameValueStr:[Character] = ["M","F",":","C","h","a"]
fileprivate let appTableValue:[Character] = ["t","M","e","n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let kSharedPath:[UInt8] = [0x5c,0x40,0x4d]

private func viewGift(block num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "onlineStatus" :*/
fileprivate let dataMainValue:[UInt8] = [0x6e,0x6f,0x6d,0x68,0x6f,0x64,0x52,0x75,0x60,0x75,0x74,0x72]

private func quantityryTop(to num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "isNewUser" :*/
fileprivate let noti_giftUrl:[UInt8] = [0xa9,0xb3,0x8e,0xa5,0xb7,0x95,0xb3,0xa5,0xb2]

/*: "c2c_ :*/
fileprivate let k_makeMessage:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let main_upModelMessage:[Character] = ["I","M","\u{83b7}","取","会"]
fileprivate let dataVoiceBeautyKey:String = "content file else lab cell话失败，c"

/*: , code: :*/
fileprivate let data_titleHowId:[Character] = [","," ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let main_clickFormat:[Character] = ["I","M","插","入","消","息","失","败","，","t","a","r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let show_burnFormat:String = "chmessagege"
fileprivate let kEffectStr:String = "tiplayern"

/*: "rYMsgType" :*/
fileprivate let data_blockPath:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let k_meErrorContent:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o"]
fileprivate let kBackgroundStr:[Character] = ["m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let app_userData:String = "IM获取会self enter fatal make"
fileprivate let data_contentFormat:String = "ode:to return"

/*: "msgInfo" :*/
fileprivate let showPlaceMsg:[Character] = ["m","s","g","I","n"]
fileprivate let data_postLabId:[Character] = ["f","o"]

/*: "needFold" :*/
fileprivate let data_valueName:String = "needFoldgender var at action to"

/*: "group_ :*/
fileprivate let appMakeValue:String = "choiceroup"
fileprivate let user_resultName:String = "by"

/*: "IM删除会话失败，convID: :*/
fileprivate let appModelUrl:[Character] = ["I","M","删","除","会","话","失"]
fileprivate let data_unknownMessage:[Character] = ["败","，","c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let data_userPath:[Character] = ["I","M","置","顶","会","话","失","败","，","c","o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let user_viewMsg:[Character] = ["S","D","K"," ","正","\u{5728}","连","接","\u{5230}","服","\u{52a1}"]
fileprivate let user_textReceivePath:[Character] = ["器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let const_dateContent:String = "SDK 已\u{7ecf}成"
fileprivate let data_valueId:String = "功连接到服label"

/*: "IM连接服务器失败，code: :*/
fileprivate let kSearchErrorData:[Character] = ["I","M","连","接","\u{670d}","务","器","失","败","，","c"]
fileprivate let noti_showToStr:[Character] = ["o","d","e",":"]

/*: , error: :*/
fileprivate let dataPlayFormat:[Character] = [","," ","e","r","r","o"]
fileprivate let main_nameAddFormat:[Character] = ["r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let appSendShowTitle:[Character] = ["I","M","账"]
fileprivate let appCutLabelTagPath:String = "\u{53f7}被挤下线。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let showMakeModelTitle:[UInt8] = [0x6,]

/*: "Offline Notification" :*/
fileprivate let k_tablePathFormat:String = "Offliremove view size"
fileprivate let constLabelUrl:String = "OTIFIC"

/*: "Cancel" :*/
fileprivate let app_tapUrl:String = "Cancelfor to file view"

/*: "Re Login" :*/
fileprivate let appCellMessage:String = "back labRe Lo"
fileprivate let data_toBusyIconPath:String = "playern"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OndSessionConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol SocialManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func toRemove(data: [CommentReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func contribution(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func quote(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum DigitizerDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class OndSessionConversationListener: NSObject {
@objcMembers public class OndSessionConversationListener: NSObject {
    //: public static let shared = OndSessionConversationListener()
    public static let shared = OndSessionConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [CommentReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [CommentReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [CommentReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [CommentReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [CommentReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: CommentReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.backInitialization()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func pathSub() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(constIndexData, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func backInitialization() {
        //: func__removeAllDelegate()
        parameter()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.viewList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func viewList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = CommentReactiveCompatible.tag(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = CommentReactiveCompatible.tag(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func fast() -> [CommentReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if NowInfoThen.share.appStatus == AppSkinStatus.special.rawValue {
        if NowInfoThen.share.appStatus == PointRelatableTarget.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == LineThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == LineThen.imageConnection() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension OndSessionConversationListener {
extension OndSessionConversationListener {
    //: func func__removeAllDelegate() {
    func parameter() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func indexDelegate(_ delegate: SocialManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func constraintDelegate(_ delegate: SocialManagerDelegate) {
        //: self.func__invokeDelegate {
        self.nurseLog {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func nurseLog(_ invocation: (SocialManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! SocialManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension OndSessionConversationListener {
extension OndSessionConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func lapse() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            WoodLogTool.knowHow(msg: (const_fatalData + "\u{767b}录失败，c" + String(data_belowValue.prefix(4))) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.backInitialization()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func loging(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = NowInfoThen.share.appUserConfigMode.uSig
        let uSigStr = NowInfoThen.share.appUserConfigMode.uSig
        //: let uidStr = NowInfoThen.share.loginUserMode.userID
        let uidStr = NowInfoThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.toImage()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            WoodLogTool.knowHow(msg: (String(data_managerMessage.suffix(6)) + String(showFaceMessage)) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func actionTarget() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || NowInfoThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || NowInfoThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.addMsg(showMsg: mainTurnMsg)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension OndSessionConversationListener {
extension OndSessionConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func toImage() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            WoodLogTool.knowHow(msg: (user_modelMessage + "未读消息总" + showCircleData + "code:") + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func remote() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: MonetaryValueThen.req_sysMessageList { _, result in
        MonetaryValueThen.headPushDownStack { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(showStateMsg))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: mainReadFormat, object: self, userInfo: [String(bytes: app_selectionId.map{monthAll(text: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func pastMapSearch(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: MonetaryValueThen.req_getChatRoomInfo { succeed, result, _ in
        MonetaryValueThen.plat { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(userSegmentData.replacingOccurrences(of: "color", with: "p"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(constManagerKey))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = CommentReactiveCompatible.tag(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(main_spendValue.replacingOccurrences(of: "icon", with: "nu"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(data_tillText) + main_videoUrl.replacingOccurrences(of: "tab", with: "d"))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension OndSessionConversationListener: V2TIMAdvancedMsgListener {
extension OndSessionConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(show_toKey.prefix(5)) + String(appInstanceShareId))) || msg.groupID.contains((showMakeId.uppercased() + String(userBindText)))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            BankManagerEnableReactiveCompatible.upward().subStatus(msg: msg)
            //: return
            return
        }
        //: if msg.sender == NowInfoThen.share.loginUserMode.userID { return }
        if msg.sender == NowInfoThen.share.loginUserMode.userID { return }

        //: let dic = TitleChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TitleChatManager.indexUse(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(user_giftKey))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ChatModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: const_tableBarValue, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(data_voiceMsg))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(kMakeName.suffix(5)) + String(noti_pairId.suffix(5))) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: const_viewMessage, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TitleChatManager.checkVoiceMessage(message: msg) {
            if TitleChatManager.enter(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = SendReactiveCompatible.contemporary(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                BeautyMsgTable.cost(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.clickDic(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            FaceButtonReactiveCompatible.shared.anyPhoto(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            FaceButtonReactiveCompatible.shared.pickUpDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(appNameValueStr) + String(appTableValue)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            LabReactiveCompatible.share.roundReply(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: WithChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: WithChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != LineThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != LineThen.requestId() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.nurseLog { $0.quote?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            SessionThen.shared.tipOrigin(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: kSharedPath.map{viewGift(block: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: dataMainValue.map{quantityryTop(to: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: noti_giftUrl.map{$0^192}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: notiKeyViewContent,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            AtReactiveCompatible.colorGift(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            WithChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: const_recordData, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: EqualCellData, isPrivete: Bool) {
    @objc public func cast(targetId: String, msgCellData: EqualCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.equalPrivete(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(k_makeMessage)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.showKey(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.casing(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                WoodLogTool.knowHow(msg: (String(main_upModelMessage) + String(dataVoiceBeautyKey.suffix(5)) + "onvId:") + "\(convId)" + (String(data_titleHowId)) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func equalPrivete(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = NowInfoThen.share.loginUserMode.userID
        let senderStr = NowInfoThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                WoodLogTool.knowHow(msg: (String(main_clickFormat)) + "\(targetId)" + (String(data_titleHowId)) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func clickDic(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(show_burnFormat.replacingOccurrences(of: "message", with: "an") + "Direc" + kEffectStr.replacingOccurrences(of: "player", with: "o"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && TotalReactiveCompatible.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(data_blockPath))].stringValue == (String(k_meErrorContent) + String(kBackgroundStr)) && isSelf && TotalReactiveCompatible.share.addressTo()!.isKind(of: LengthViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: noti_indexTitle) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: noti_indexTitle)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension OndSessionConversationListener: V2TIMConversationListener {
extension OndSessionConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.showKey(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.casing(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.showKey(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.casing(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: NowInfoThen.share.unreadMessageNum = Int(totalUnreadCount)
        NowInfoThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.nurseLog {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.contribution?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func casing(conVersationList: [CommentReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if AtReactiveCompatible.dropShared().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                AtReactiveCompatible.dropShared().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.nurseLog {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.toRemove?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func completion(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [CommentReactiveCompatible] = []

        // V2TIMConversation->CommentReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = CommentReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.toConv(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [CommentReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ToInfoTable.voiceCount(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.sight(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension OndSessionConversationListener {
extension OndSessionConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func nothings(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.completion(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.casing(conVersationList: arr as? [CommentReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            WoodLogTool.knowHow(msg: (String(app_userData.prefix(5)) + "\u{8bdd}列表失败\u{ff0c}c" + String(data_contentFormat.prefix(4))) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func videoExamineed(targetId: String) -> CommentReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func showKey(convList: [V2TIMConversation]) -> [CommentReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [CommentReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = CommentReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.toConv(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.sight(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func sight(convModel: CommentReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.viewList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: DigitizerDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - NowInfoThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - NowInfoThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = AtReactiveCompatible.viewText(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TitleChatManager.parseTXMessageData(data: elemData)
            let dict = TitleChatManager.indexUse(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(user_giftKey))][(String(showPlaceMsg) + String(data_postLabId))][(String(data_valueName.prefix(8)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.straitAndNarrowKeyKindSize(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.straitAndNarrowKeyKindSize(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func straitAndNarrowKeyKindSize(convList: [CommentReactiveCompatible], dataType: DigitizerDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func imaginationImageType(targetId: String, dataType: DigitizerDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(k_makeMessage)) + "\(targetId)" : (appMakeValue.replacingOccurrences(of: "choice", with: "g") + user_resultName.replacingOccurrences(of: "by", with: "_")) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            AtReactiveCompatible.withWrap(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(NowInfoThen.share.loginUserMode.userID, toUid: targetId)
            BeautyMsgTable.downAppear(NowInfoThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.toImage()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            WoodLogTool.knowHow(msg: (String(appModelUrl) + String(data_unknownMessage)) + "\(convID)" + (String(data_titleHowId)) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func television(convModel: CommentReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            WoodLogTool.knowHow(msg: (String(data_userPath)) + "\(convModel.conversationID)" + (String(data_titleHowId)) + "\(code)" + (String(k_viewSuccessFormat) + String(show_limitFormat)) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.straitAndNarrowKeyKindSize(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.straitAndNarrowKeyKindSize(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension OndSessionConversationListener: V2TIMSDKListener {
extension OndSessionConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(user_viewMsg) + String(user_textReceivePath)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (const_dateContent + data_valueId.replacingOccurrences(of: "label", with: "务器")))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        WoodLogTool.knowHow(msg: (String(kSearchErrorData) + String(noti_showToStr)) + "\(code)" + (String(dataPlayFormat) + String(main_nameAddFormat)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        WoodLogTool.knowHow(msg: (String(appSendShowTitle) + appCutLabelTagPath))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        BankManagerSocketDelegate.shared.modelSuccessfully(userLoginOut: true)
        //: OndSessionConversationListener.shared.isConnection = false
        OndSessionConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (SocialKingfisherCompatible.shared as! SocialKingfisherCompatible).startQuote()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: showMakeModelTitle.map{$0^95}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = MagnitudeAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if OfDetailAddrTool.share.interfaceLang == MusicScalarLiteral.ar.rawValue || OfDetailAddrTool.share.interfaceLang == MusicScalarLiteral.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            OfDetailAddrTool.share.interfaceLang == MusicScalarLiteral.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.outView(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        PileThen.downCrosshead()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        PileThen.downCrosshead(title: (String(k_tablePathFormat.prefix(5)) + "ne N" + constLabelUrl.lowercased() + "ation").localized, message: msg, leftBtnTitle: (String(app_tapUrl.prefix(6))).localized, rightBtnTitle: (String(appCellMessage.suffix(5)) + data_toBusyIconPath.replacingOccurrences(of: "player", with: "gi")).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ProduceViewPushListener.decide().blockFemale()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            ConstraintEventHandler.bindTo().userRender()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            BoundaryReactiveCompatible.button { t in
                //: ProgressHUD.dismiss()
                DeleteReactiveCompatible.deflector()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: mainAddTitle, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            BoundaryReactiveCompatible.vendee { t in
                //: if t {
                if t {
                    //: OndSessionConversationListener.shared.func__LogingIn { _ in
                    OndSessionConversationListener.shared.loging { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if ProduceViewPushListener.decide().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(ProduceViewPushListener.decide().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    BankManagerSocketDelegate.shared.blackLabel()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
