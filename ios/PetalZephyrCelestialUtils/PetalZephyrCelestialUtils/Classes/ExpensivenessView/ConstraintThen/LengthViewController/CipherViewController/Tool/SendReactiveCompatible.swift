
//: Declare String Begin

/*: "msgType" :*/
fileprivate let userServiceMsg:String = "actual"
fileprivate let kToMessage:[Character] = ["g","T","y","p","e"]

/*: "tips" :*/
fileprivate let main_sharedNameData:String = "tmodels"

/*: "extra" :*/
fileprivate let app_makeValue:String = "extrwill"

/*: "msgInfo" :*/
fileprivate let showChangeId:String = "msource"
fileprivate let notiManagerId:[Character] = ["g","I","n","f","o"]

/*: "user" :*/
fileprivate let showTargetMsg:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let appLabelTitle:String = "UID"

/*: "nickname" :*/
fileprivate let appDetailId:[UInt8] = [0xd4,0xd3,0xd9,0xd1,0xd4,0xdb,0xd7,0xdf]

private func targetCorner(make num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "headPic" :*/
fileprivate let data_toEqualName:[UInt8] = [0x4f,0x42,0x46,0x43,0x77,0x4e,0x44]

private func equipmentAdd(on num: UInt8) -> UInt8 {
    return num ^ 39
}

/*: "age" :*/
fileprivate let mainProgressModelUrl:[UInt8] = [0x6c,0x6a,0x68]

private func colorTouch(text num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "gift" :*/
fileprivate let notiUniversalDetailKey:String = "gorigint"

/*: "isGroup" :*/
fileprivate let mainNorMsg:String = "isGroaccount cell in model"
fileprivate let notiTimeFormat:String = "UP"

/*: "WantGift" :*/
fileprivate let dataHourValue:[Character] = ["W","a"]
fileprivate let k_modelFormat:[Character] = ["n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let show_toMessage:[Character] = ["t","x","t"]

/*: "img" :*/
fileprivate let mainArrayUrl:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let appModelMessage:String = "viimageeo"

/*: "audio" :*/
fileprivate let kSignatureMsg:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let noti_giftValue:String = "MESS"

/*: "contentType" :*/
fileprivate let app_viewMessage:[Character] = ["c","o","n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let const_topText:String = "observer"
fileprivate let data_filterMsg:String = "status app touchmgMsg"

/*: "imageUri" :*/
fileprivate let noti_byMsg:String = "background let height name videoimageUri"

/*: "type" :*/
fileprivate let notiCenterMessage:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let app_soundLabelFramePath:String = "tableic"

/*: "isBurn" :*/
fileprivate let kSnapFormat:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let appMonthResultGlobalText:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let noti_photoPath:String = "equivalent"
fileprivate let dataFramePath:String = "aramevent"

/*: "audioLength" :*/
fileprivate let notiViewName:String = "aumobile"
fileprivate let constLayerTitle:String = "price"

/*: "len" :*/
fileprivate let data_timeErrorName:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let data_modeViewValue:String = "audiback"

/*: "senderId" :*/
fileprivate let const_textTitle:String = "sconvert"
fileprivate let user_selectionData:String = "nderIdcell path new self"

/*: "AudioMsg" :*/
fileprivate let const_reportName:String = "skin"
fileprivate let showLabData:[Character] = ["u","d","i","o","M","s","g"]

/*: "content" :*/
fileprivate let showAllValue:String = "cupntent"

/*: "TxtMsg" :*/
fileprivate let userAllowKey:String = "TxtMsgshould in"

/*: "message" :*/
fileprivate let constImageNameData:[Character] = ["m","e"]
fileprivate let noti_hiddenTitle:String = "ssagapp"

/*: "quoteMsgId" :*/
fileprivate let show_dateModelIfMsg:String = "qulive"
fileprivate let dataHowFramePath:[Character] = ["I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let notiMethodTitle:[Character] = ["q"]
fileprivate let k_sizeInputKey:String = "return color in nor ifuoteM"

/*: "rYMsgType" :*/
fileprivate let notiPathStr:String = "rYMsgTshow text gift input"
fileprivate let app_dataMsg:String = "yuser"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let k_labelCardKey:String = "GJ:Sadd copy m view"
fileprivate let app_fileTitle:String = "case let show viewtoGree"
fileprivate let data_tagMessage:[Character] = ["s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let kTimeUrl:[Character] = ["c","h","a","n","g","e","D","i"]
fileprivate let const_modelToKey:[Character] = ["r","e","c","t","i","o","n"]

/*: "system" :*/
fileprivate let k_targetResultKey:String = "SYSTEM"

/*: "title" :*/
fileprivate let app_clickNameTitle:String = "titpathe"

/*: "id" :*/
fileprivate let userLocationMessage:String = "title"

/*: "icon" :*/
fileprivate let k_toBagText:String = "ICON"

/*: "all" :*/
fileprivate let appInfoKey:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let kDirectionTitle:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let dataToFormat:String = "voicetatu"
fileprivate let showViewMsg:String = "S"

/*: "msgIncome" :*/
fileprivate let main_assetId:[Character] = ["m","s"]
fileprivate let app_dateViewKey:String = "for add type vargIncome"

/*: "msgId" :*/
fileprivate let show_lineTitle:String = "msgIdname self tag"

/*: "SessionTextConvertible" :*/
fileprivate let mainGiftSharedName:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","S","t","a","t"]
fileprivate let kEqualFormat:String = "umodel"

/*: "TMsgTranslatedContent" :*/
fileprivate let noti_timeKey:String = "TMsgTlock color of"
fileprivate let constSizeStr:String = "atedContdata input"
fileprivate let mainTagName:String = "endata"

/*: "formatType" :*/
fileprivate let user_tapMsg:String = "formamenu"
fileprivate let kLeadingValue:String = "identity image size error untilType"

/*: "plain" :*/
fileprivate let user_tableStr:String = "iconlain"

/*: "imageData" :*/
fileprivate let notiImageSumValue:[Character] = ["i","m","a","g","e","D","a","t"]
fileprivate let appFilterUrl:String = "A"

/*: "VideoMsg" :*/
fileprivate let mainWillUrl:String = "VideoMsgvar model count"

/*: "match" :*/
fileprivate let notiEraseEqualMessage:[Character] = ["m","a","t","c","h"]

/*: "mf" :*/
fileprivate let show_identityStr:String = "MF"

/*: "userIntro" :*/
fileprivate let notiUserValuePath:String = "userIntroat extension icon size"

/*: "targetId" :*/
fileprivate let dataEqualMessage:String = "databaseargedatabase"
fileprivate let show_mName:[Character] = ["I","d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let const_popularUrl:String = "YYYY"
fileprivate let const_minHiddenData:String = " HH:view corner to make of"

/*: "sendTime" :*/
fileprivate let dataPathStr:[Character] = ["s","e","n","d","T","i","m"]
fileprivate let k_arrayData:String = "finish"

/*: "isRead" :*/
fileprivate let const_dataMessage:[Character] = ["i"]
fileprivate let show_maxMsg:String = "render ifsRead"

/*: "audioData" :*/
fileprivate let constRangeName:String = "AUDI"

/*: "audioUri" :*/
fileprivate let k_finishMsg:String = "sizeudio"
fileprivate let k_conversationMessage:[Character] = ["U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SendReactiveCompatible.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum FactoidQuantityLiteral: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - SendReactiveCompatible

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class SendReactiveCompatible: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func noCase() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EqualCellData? {
    public class func exceptImage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EqualCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = actionBackground(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: OndSessionConversationListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            OndSessionConversationListener.shared.cast(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EqualCellData? {
    public class func actionBackground(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EqualCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] = (main_sharedNameData.replacingOccurrences(of: "model", with: "ip"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(app_makeValue.replacingOccurrences(of: "will", with: "a")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = pressIn(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ContentThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ChatModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? TimeDailyModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(showTargetMsg))] = [
            //: "uid": NowInfoThen.share.loginUserMode.userID,
            (appLabelTitle.lowercased()): NowInfoThen.share.loginUserMode.userID,
            //: "nickname": NowInfoThen.share.loginUserMode.nickname,
            String(bytes: appDetailId.map{targetCorner(make: $0)}, encoding: .utf8)!: NowInfoThen.share.loginUserMode.nickname,
            //: "headPic": NowInfoThen.share.loginUserMode.headPic,
            String(bytes: data_toEqualName.map{equipmentAdd(on: $0)}, encoding: .utf8)!: NowInfoThen.share.loginUserMode.headPic,
            //: "age": NowInfoThen.share.loginUserMode.age,
            String(bytes: mainProgressModelUrl.map{colorTouch(text: $0)}, encoding: .utf8)!: NowInfoThen.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(app_makeValue.replacingOccurrences(of: "will", with: "a")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = pressIn(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (notiUniversalDetailKey.replacingOccurrences(of: "origin", with: "if")) {
            //: var data: EqualCellData
            var data: EqualCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(mainNorMsg.prefix(5)) + notiTimeFormat.lowercased())] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ModelReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = SessionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = SessionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (String(dataHourValue) + String(k_modelFormat)) {
            //: let data = CompartmentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CompartmentCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (String(show_toMessage)) {
            //: var data: EqualCellData
            var data: EqualCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(mainNorMsg.prefix(5)) + notiTimeFormat.lowercased())] as? String == "1" { // 群聊
                //: data = FillDeleteThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = FillDeleteThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ExpensivenessCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ExpensivenessCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (String(mainArrayUrl)) {
            //: let data = TailCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TailCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (appModelMessage.replacingOccurrences(of: "image", with: "d")) {
            //: let data = AuditoryImageCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AuditoryImageCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (String(kSignatureMsg)) {
            //: let data = BackgroundReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BackgroundReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(notiUniversalDetailKey.replacingOccurrences(of: "origin", with: "if"))] != nil && (msgInfo[(noti_giftValue.lowercased() + "ageType")] as? Int) == 4 {
            //: let data = ExpensivenessCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ExpensivenessCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ChatModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: EqualCellData) -> EqualCellData? {
    public class func lightColored(withData cellData: EqualCellData) -> EqualCellData? {
        // 图片
        //: if let reCellData = cellData as? TailCellData {
        if let reCellData = cellData as? TailCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(LineThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(LineThen.statusAdd())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(app_viewMessage))] = (const_topText.replacingOccurrences(of: "observer", with: "I") + String(data_filterMsg.suffix(5)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] = (String(mainArrayUrl))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(noti_byMsg.suffix(8)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(notiCenterMessage))] = (const_topText.replacingOccurrences(of: "observer", with: "I") + String(data_filterMsg.suffix(5)))
            //: params["pic"] = filePath
            params[(app_soundLabelFramePath.replacingOccurrences(of: "table", with: "p"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(kSnapFormat))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(kSnapFormat))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(appMonthResultGlobalText))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(appMonthResultGlobalText))] = "0"
            }

            //: dic["params"] = params
            dic[(noti_photoPath.replacingOccurrences(of: "equivalent", with: "p") + dataFramePath.replacingOccurrences(of: "event", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = actionBackground(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? AuditoryImageCellData {
        if let reCellData = cellData as? AuditoryImageCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? BackgroundReactiveCompatible {
        if let reCellData = cellData as? BackgroundReactiveCompatible {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = AtReactiveCompatible.magnitudeimate(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(notiViewName.replacingOccurrences(of: "mobile", with: "di") + "oLengt" + constLayerTitle.replacingOccurrences(of: "price", with: "h"))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(data_timeErrorName))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(data_modeViewValue.replacingOccurrences(of: "back", with: "o") + "Sandbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(data_modeViewValue.replacingOccurrences(of: "back", with: "o") + "Sandbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] = (String(kSignatureMsg))
            //: msgInfo["senderId"] = NowInfoThen.share.loginUserMode.userID
            msgInfo[(const_textTitle.replacingOccurrences(of: "convert", with: "e") + String(user_selectionData.prefix(6)))] = NowInfoThen.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(notiCenterMessage))] = (const_reportName.replacingOccurrences(of: "skin", with: "A") + String(showLabData))
            //: dic["params"] = params
            dic[(noti_photoPath.replacingOccurrences(of: "equivalent", with: "p") + dataFramePath.replacingOccurrences(of: "event", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = actionBackground(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ExpensivenessCellData {
        if let reCellData = cellData as? ExpensivenessCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(showAllValue.replacingOccurrences(of: "up", with: "o"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] = (String(show_toMessage))
            //: dic["msgInfo"] = msgInfo
            dic[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(notiCenterMessage))] = (String(userAllowKey.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(constImageNameData) + noti_hiddenTitle.replacingOccurrences(of: "app", with: "e"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.plus() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(show_dateModelIfMsg.replacingOccurrences(of: "live", with: "ot") + "eMsg" + String(dataHowFramePath))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(notiMethodTitle) + String(k_sizeInputKey.suffix(5)) + "sgInfo")] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(noti_photoPath.replacingOccurrences(of: "equivalent", with: "p") + dataFramePath.replacingOccurrences(of: "event", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = actionBackground(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EqualCellData? {
    public class func setAbout(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EqualCellData? {
        //: var cellData: EqualCellData?
        var cellData: EqualCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(app_makeValue.replacingOccurrences(of: "will", with: "a"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(showTargetMsg))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ChatModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == NowInfoThen.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == NowInfoThen.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == NowInfoThen.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == NowInfoThen.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(notiPathStr.prefix(6)) + app_dataMsg.replacingOccurrences(of: "user", with: "pe"))] as? String == (String(k_labelCardKey.prefix(4)) + "ess:Au" + String(app_fileTitle.suffix(6)) + "t:TxtCu" + String(data_tagMessage)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(kTimeUrl) + String(const_modelToKey))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = iconCamera(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (k_targetResultKey.lowercased()) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ChatModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? TimeDailyModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ContentThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(showAllValue.replacingOccurrences(of: "up", with: "o"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = TailCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TailCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = AuditoryImageCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AuditoryImageCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ModelReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = SessionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SessionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = aggregation(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(app_makeValue.replacingOccurrences(of: "will", with: "a"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? DailyModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = CompartmentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CompartmentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = BackgroundReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BackgroundReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? BackgroundReactiveCompatible {
            if let audioCellData = cellData as? BackgroundReactiveCompatible {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = property(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = BeautyMsgTable.namePush(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = BeautyMsgTable.cost(contemporary(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CookieCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(app_clickNameTitle.replacingOccurrences(of: "path", with: "l"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(showAllValue.replacingOccurrences(of: "up", with: "o"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(mainArrayUrl))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = NameCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = FillDeleteThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FillDeleteThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ExpensivenessCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ExpensivenessCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? PushModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(k_toBagText.lowercased())] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: NowInfoThen.share.loginUserMode.headPic ?? ""
                        NowInfoThen.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = DailyModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(showAllValue.replacingOccurrences(of: "up", with: "o"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(app_makeValue.replacingOccurrences(of: "will", with: "a"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = CenterModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(appInfoKey))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(kDirectionTitle))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.squareOn(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(dataToFormat.replacingOccurrences(of: "voice", with: "s") + showViewMsg.lowercased())] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(main_assetId) + String(app_dateViewKey.suffix(7)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(noti_giftValue.lowercased() + "ageType")] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(show_lineTitle.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(mainGiftSharedName) + kEqualFormat.replacingOccurrences(of: "model", with: "s"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = SessionTextConvertible(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(noti_timeKey.prefix(5)) + "ransl" + String(constSizeStr.prefix(8)) + mainTagName.replacingOccurrences(of: "data", with: "t"))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = OpenModel.target()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = OpenModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = AlongModel.financialAid()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = AlongModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = AllMakeModel.close()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func iconCamera(_ extraDic: [String: Any]) -> FactoidQuantityLiteral {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(app_makeValue.replacingOccurrences(of: "will", with: "a"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ChatModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(user_tapMsg.replacingOccurrences(of: "menu", with: "t") + String(kLeadingValue.suffix(4)))] as? String == (user_tableStr.replacingOccurrences(of: "icon", with: "p")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(noti_byMsg.suffix(8)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(notiImageSumValue) + appFilterUrl.lowercased())] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(mainWillUrl.prefix(8))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = searchTable(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(notiEraseEqualMessage))] as? Int) == 1 || (msgInfo?[(String(notiEraseEqualMessage))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (main_sharedNameData.replacingOccurrences(of: "model", with: "ip")) || (dic?[(noti_giftValue.lowercased() + "ageType")] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(user_tapMsg.replacingOccurrences(of: "menu", with: "t") + String(kLeadingValue.suffix(4)))] as? String == "mf") && ((dic?[(noti_giftValue.lowercased() + "ageType")] as? Int) == 15 || (dic?[(noti_giftValue.lowercased() + "ageType")] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (String(notiUserValuePath.prefix(9))) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String == (String(dataHourValue) + String(k_modelFormat)) || (msgInfo?[(noti_giftValue.lowercased() + "ageType")] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func contemporary(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.squareOn(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(app_makeValue.replacingOccurrences(of: "will", with: "a"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(showChangeId.replacingOccurrences(of: "source", with: "s") + String(notiManagerId))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(showTargetMsg))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(const_textTitle.replacingOccurrences(of: "convert", with: "e") + String(user_selectionData.prefix(6)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == NowInfoThen.share.loginUserMode.userID {
        if senderId == NowInfoThen.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(const_textTitle.replacingOccurrences(of: "convert", with: "e") + String(user_selectionData.prefix(6)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(dataEqualMessage.replacingOccurrences(of: "database", with: "t") + String(show_mName))] = message.userID ?? (user?[(dataEqualMessage.replacingOccurrences(of: "database", with: "t") + String(show_mName))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(const_textTitle.replacingOccurrences(of: "convert", with: "e") + String(user_selectionData.prefix(6)))] = "\(msgInfo?[(appLabelTitle.lowercased())] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(dataEqualMessage.replacingOccurrences(of: "database", with: "t") + String(show_mName))] = "\(msgInfo?[(appLabelTitle.lowercased())] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(show_lineTitle.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(show_lineTitle.prefix(5)))] = "\(msgInfo?[(String(show_lineTitle.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.squareOn(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(show_lineTitle.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(show_lineTitle.prefix(5)))] = "\(customDic[(String(show_lineTitle.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(show_lineTitle.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((data_modeViewValue.replacingOccurrences(of: "back", with: "o") + "Sandbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(data_modeViewValue.replacingOccurrences(of: "back", with: "o") + "Sandbox")] = "\(msgInfo?[(data_modeViewValue.replacingOccurrences(of: "back", with: "o") + "Sandbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((notiViewName.replacingOccurrences(of: "mobile", with: "di") + "oLengt" + constLayerTitle.replacingOccurrences(of: "price", with: "h"))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(notiViewName.replacingOccurrences(of: "mobile", with: "di") + "oLengt" + constLayerTitle.replacingOccurrences(of: "price", with: "h"))] = "\(msgInfo?[(notiViewName.replacingOccurrences(of: "mobile", with: "di") + "oLengt" + constLayerTitle.replacingOccurrences(of: "price", with: "h"))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (const_popularUrl.lowercased() + "-MM-dd" + String(const_minHiddenData.prefix(4)) + "mm:ss")
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(dataPathStr) + k_arrayData.replacingOccurrences(of: "finish", with: "e"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == NowInfoThen.share.loginUserMode.userID ? "1" : "0"
        info?[(String(const_dataMessage) + String(show_maxMsg.suffix(5)))] = senderId == NowInfoThen.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func property(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = contemporary(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(show_lineTitle.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func aggregation(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func pressIn(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func searchTable(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(userServiceMsg.replacingOccurrences(of: "actual", with: "ms") + String(kToMessage))] as? String, msgType == (String(kSignatureMsg)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(app_viewMessage))] as? String,
           //: contentType == "AudioMsg"
           contentType == (const_reportName.replacingOccurrences(of: "skin", with: "A") + String(showLabData))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(constRangeName.lowercased() + "oData")] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(k_finishMsg.replacingOccurrences(of: "size", with: "a") + String(k_conversationMessage))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
