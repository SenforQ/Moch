
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kSparkKey:[UInt8] = [0x49,0x4e,0x49,0x54,0x8,0x43,0x4f,0x44,0x45,0x52,0x1a,0x9,0x0,0x48,0x41,0x53,0x0,0x4e,0x4f,0x54,0x0,0x42,0x45,0x45,0x4e,0x0,0x49,0x4d,0x50,0x4c,0x45,0x4d,0x45,0x4e,0x54,0x45,0x44]

fileprivate func blockMode(content num: UInt8) -> UInt8 {
    let value = Int(num) + 32
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "F5F5F5" :*/
fileprivate let dataLabStr:[Character] = ["F","5","F","5","F","5"]

/*: "#128CFF" :*/
fileprivate let kSocialId:[Character] = ["#"]
fileprivate let main_willHourTitle:String = "reason app remove128CFF"

/*: "Feedback" :*/
fileprivate let notiDirectionTouchTitle:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let showObserverId:[Character] = ["#","8"]
fileprivate let showWillId:[Character] = ["3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let dataPathMsg:String = "Quick sex to add make model"
fileprivate let userUpWithId:String = "message right viewGr"
fileprivate let mainKitKey:[Character] = ["e","e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let user_cellName:[Character] = ["B","e","a","u","t","y"," ","P","h","o"]
fileprivate let noti_dataTitle:String = "tons"

/*: "url" :*/
fileprivate let showValueInfoName:String = "urmake"

/*: "unlockGift" :*/
fileprivate let app_contentLabData:String = "ulive"
fileprivate let kErrorPath:String = "path string pointockGift"

/*: "%d" :*/
fileprivate let show_targetStr:String = "lab request request%d"

/*: "isBurn" :*/
fileprivate let mainBlockMessage:[UInt8] = [0xc6,0xdc,0xed,0xda,0xdd,0xc1]

private func modelStatus(layer num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "giftId" :*/
fileprivate let appFirstValue:String = "giftItip self let"
fileprivate let showResultPath:String = "view"

/*: "rYMsgType" :*/
fileprivate let mainCollectionPath:[Character] = ["r","Y","M","s","g","T","y"]
fileprivate let dataSinceName:String = "match"

/*: "GJ:CallCustom" :*/
fileprivate let show_frameId:String = "GJ:Cacountry user"
fileprivate let noti_showGestureValue:String = "stcolumn"

/*: "changeDirection" :*/
fileprivate let notiListMessage:String = "CHANG"
fileprivate let app_normalMsg:String = "ecaddo"
fileprivate let dataImagePath:String = "time"

/*: "extra" :*/
fileprivate let dataMyFormat:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let noti_userId:String = "msgInfofile filter location self true"

/*: "toUid" :*/
fileprivate let kDataToValue:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let appAspectKey:[Character] = ["w","a","n","t","G"]
fileprivate let data_resultName:String = "iraw"

/*: "jumpKey" :*/
fileprivate let notiSucceedAppName:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let data_requestName:String = "trlabe"
fileprivate let main_colorName:String = "nAuthkey string"

/*: "logType" :*/
fileprivate let show_thenStr:[Character] = ["l","o"]
fileprivate let noti_listTitle:String = "gTypeplain to"

/*: "msgType" :*/
fileprivate let show_valueArrayMsg:[UInt8] = [0xb1,0xaf,0xbb,0x88,0xa5,0xac,0xb9]

private func colorButton(info num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "txt" :*/
fileprivate let mainLiveContent:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let user_toolName:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let kDataTargetMessage:String = "q"
fileprivate let showNameStr:[Character] = ["u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let show_releaseErrorText:[UInt8] = [0x1e,0xf,0x1c,0xf,0x3,0x1d]

private func eventFromBack(source num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "content" :*/
fileprivate let k_labelKey:[UInt8] = [0xae,0xa2,0xa3,0xb9,0xa8,0xa3,0xb9]

private func imageEqual(match num: UInt8) -> UInt8 {
    return num ^ 205
}

/*: "id" :*/
fileprivate let showModelTitle:String = "videod"

/*: "txtId" :*/
fileprivate let constItemValue:String = "txtIdto self"

/*: "message" :*/
fileprivate let kMakeId:[UInt8] = [0xa3,0xab,0xbd,0xbd,0xaf,0xa9,0xab]

private func detailInput(text num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "TxtMsg" :*/
fileprivate let kWillText:String = "let import let modelTxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let const_curMessage:[UInt8] = [0x9d,0xa4,0xa2,0xa8,0xae,0xeb,0xa3,0xae,0xa7,0xa7,0xa4,0xeb,0xaf,0xa4,0xbc,0xa5,0xa7,0xa4,0xaa,0xaf,0xeb,0xad,0xaa,0xa2,0xa7,0xae,0xaf,0xe5,0xeb,0x9b,0xb9,0xa4,0xa8,0xae,0xaf,0xbe,0xb9,0xae]

/*: "length" :*/
fileprivate let noti_toValue:String = "item"
fileprivate let notiTalkPathMsg:String = "enshowth"

/*: "audio" :*/
fileprivate let mainLabelId:String = "intimatedio"

/*: "audioId" :*/
fileprivate let k_toFillKey:[Character] = ["a","u","d","i","o"]
fileprivate let dataSucceedUserPlayerTitle:[Character] = ["I","d"]

/*: "audioLength" :*/
fileprivate let data_keyPath:[UInt8] = [0x9a,0x8e,0x9f,0x92,0x94,0xb7,0x9e,0x95,0x9c,0x8f,0x93]

/*: "audioSandbox" :*/
fileprivate let userExplainText:[Character] = ["a","u","d","i","o","S"]
fileprivate let app_targetFormat:String = "andbtimex"

/*: "senderId" :*/
fileprivate let app_effectTitle:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let k_tempStr:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let dataColorId:String = "Can\'t "
fileprivate let data_styleKey:String = "view case explain shape availablelank m"

/*: "quoteMsgId" :*/
fileprivate let const_iconMessage:String = "qstopt"
fileprivate let data_colorStr:[Character] = ["I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let kVideoFormat:[Character] = ["q","u","o","t","e","M","s","g","I"]
fileprivate let data_whiteValue:String = "nameo"

/*: "contentType" :*/
fileprivate let main_fillValue:[UInt8] = [0x1c,0x28,0x27,0x2d,0x1e,0x27,0x2d,0xd,0x32,0x29,0x1e]

fileprivate func fatalHidden(model num: UInt8) -> UInt8 {
    let value = Int(num) - 185
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let k_makeContent:String = "define lock equal var labelImgMsg"

/*: "img" :*/
fileprivate let k_qualityUrl:String = "ilabg"

/*: "imageUri" :*/
fileprivate let kCounteractionValue:[UInt8] = [0x60,0x64,0x68,0x6e,0x6c,0x5c,0x7b,0x60]

private func itemRequest(all num: UInt8) -> UInt8 {
    return num ^ 9
}

/*: "customSend" :*/
fileprivate let kAddLayerContent:[UInt8] = [0x32,0x24,0x22,0x25,0x3e,0x3c,0x2,0x34,0x3f,0x35]

private func nameMoreColor(page num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "pic" :*/
fileprivate let k_allData:String = "listc"

/*: "VideoMsg" :*/
fileprivate let data_viewExecuteShowStr:String = "path attentionVideo"
fileprivate let app_titleIndexStr:String = "move acceptMsg"

/*: "video" :*/
fileprivate let user_selectedText:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let notiUserTitle:[UInt8] = [0x67,0x6d,0x49,0x72,0x65,0x76,0x6f,0x63]

/*: "videoUrl" :*/
fileprivate let user_finishMessage:[UInt8] = [0xe5,0xfa,0xf7,0xf6,0xfc,0xc6,0xe1,0xff]

private func norValue(contact num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "duration" :*/
fileprivate let show_trackPath:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let dataNameFormat:[UInt8] = [0x58,0x51,0x5a]

private func modelMode(application num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "Documents/User/voice/" :*/
fileprivate let mainTitleName:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","v","o","i"]
fileprivate let k_insideName:[Character] = ["c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let mainMakeStr:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","r"]
fileprivate let user_sizeText:String = "view tableecord/"

/*: "msgIncome" :*/
fileprivate let k_activityData:String = "msgIncomtheory label"
fileprivate let show_addText:[Character] = ["e"]

/*: "mfBean" :*/
fileprivate let kInfoText:[UInt8] = [0x5f,0x54,0x70,0x57,0x53,0x5c]

/*: "messageType" :*/
fileprivate let userNameAtPath:String = "mcolorssag"

/*: "WantGift" :*/
fileprivate let app_dataUserName:[Character] = ["W"]
fileprivate let constLabFormat:[Character] = ["a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let main_succeedCurrentText:[Character] = ["_","P","r","i","v","a","t"]
fileprivate let user_waitHalfKey:String = "block to if block selfeChatTi"
fileprivate let mainInputId:[Character] = ["p","s","T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let data_onValue:String = "data equal var backgroundmsgId"

/*: "totalMfCoin" :*/
fileprivate let user_viewMessage:String = "totalMfCoimage filter element"
fileprivate let noti_requestValue:[Character] = ["i","n"]

/*: "attachSysMsg" :*/
fileprivate let main_targetStr:String = "ATTACH"
fileprivate let userScreenPath:[Character] = ["S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let main_bottomUrl:String = "INTI"
fileprivate let showUserMessage:String = "hcelltcell"

/*: _ :*/
fileprivate let mainVisualId:String = "_"

/*: "totalIntimate" :*/
fileprivate let dataNameKey:[Character] = ["t","o","t","a","l","I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let constCropText:[UInt8] = [0x64,0x69,0x75]

/*: "intimate" :*/
fileprivate let showKnowNameAfterData:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let user_sectionName:[UInt8] = [0xeb,0x9,0x16,0xcf,0x1c,0xc8,0x1d,0x1b,0xd,0xc8,0x1c,0x10,0xd,0xc8,0xe,0x1d,0x16,0xb,0x1c,0x11,0x17,0x16,0xc8,0xc,0x1d,0xd,0xc8,0x1c,0x17,0xc8,0x21,0x17,0x1d,0x1a,0xc8,0x14,0x9,0xb,0x13,0xc8,0x17,0xe,0xc8,0x11,0x16,0x1c,0x11,0x15,0x9,0xb,0x21,0xc8,0x14,0xd,0x1e,0xd,0x14,0xd6]

fileprivate func availableImage(video num: UInt8) -> UInt8 {
    let value = Int(num) + 88
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let k_retSizeData:String = "giftNuview self"
fileprivate let constEqualName:[Character] = ["m"]

/*: "pkgItemsetId" :*/
fileprivate let mainMaleResignMsg:String = "button makepkgIte"
fileprivate let main_toMsg:[Character] = ["m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let notiWithName:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let userLabStr:[Character] = ["w","a","n","t","G"]
fileprivate let appSectionUrl:[Character] = ["i","f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let notiImageText:[Character] = ["W","a","n","t","G","i"]
fileprivate let userFatalId:[Character] = ["f","t","M","s","g"]

/*: "gift" :*/
fileprivate let mainCreateKey:String = "gifcomplete"

/*: "View info" :*/
fileprivate let const_viewAppName:String = "model"
fileprivate let showMentionValue:String = "view height sizeiew "

/*: "Notes" :*/
fileprivate let main_dateKey:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let k_viewShareData:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let app_smallKey:[Character] = ["V","i","e","w"," ","r","e","a","d"]
fileprivate let appDataPlayerTableText:String = "user session video replacement rece"
fileprivate let kMakeViewValue:String = "ipts?model constraint else never cell"

/*: "He" :*/
fileprivate let notiInsideAppText:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let show_refuseContent:String = "state source try gift typeShe"

/*: "%@ is using too low a version" :*/
fileprivate let dataControlMsg:String = "%@ is on else center"
fileprivate let constTextUrl:String = "too lgift to"
fileprivate let kAddStr:String = "pice"
fileprivate let data_feedbackModelEffectPath:String = "RSION"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let k_faceTitlePath:String = "app appShoot"
fileprivate let main_makeKey:String = " at letype var type equal selected"
fileprivate let mainArrayUrl:[Character] = ["a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let appBlockMsg:[Character] = ["U","p","l","o","a","d"," ","f","a","i"]
fileprivate let main_positionBucketEndValue:[Character] = ["l","e","d"]

/*: "播放状态: :*/
fileprivate let notiViewTitle:[Character] = ["\u{64ad}","放","状","\u{6001}",":"]

/*: "c2c_" :*/
fileprivate let const_lastKey:String = "image topc2c_"

/*: "msg" :*/
fileprivate let notiCellKey:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let k_worldTableData:String = "styleps"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let user_sizeUrl:[UInt8] = [0xe1,0xd7,0xcd,0x98,0xdb,0xd9,0xd6,0x98,0xcd,0xcb,0xdd,0x98,0xcc,0xd0,0xdd,0x98,0xce,0xd1,0xdc,0xdd,0xd7,0x98,0xdb,0xd0,0xd9,0xcc,0x98,0xde,0xdd,0xd9,0xcc,0xcd,0xca,0xdd,0x98,0xcc,0xd7,0x98,0xdf,0xd1,0xce,0xdd,0x98,0x9d,0xf8,0x98,0xd9,0x98,0xdb,0xd9,0xd4,0xd4]

/*: "cellData" :*/
fileprivate let showPlayerFormat:[Character] = ["c","e"]
fileprivate let app_viewId:String = "llDataself area fire"

/*: "System notification" :*/
fileprivate let show_playTitle:[Character] = ["S","y","s","t","e"]
fileprivate let main_groupStr:String = "m notifshare make table on name"
fileprivate let userListValue:String = "kitation"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LengthViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum PriceViewFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class LengthViewController: TailRecognizerDelegate {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = LengthThen()
    private var userWrap = LengthThen() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = PriceViewFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: LengthThen = LengthThen()) {
    init(chatID: String, isFrom: PriceViewFromEnum = .Normal, userWrap: LengthThen = LengthThen()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        LengthViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        WithChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == LineThen.getXiaoMiID() {
        if targetId == LineThen.requestId() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == LineThen.getCustomerServiceID() {
        } else if targetId == LineThen.imageConnection() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kSparkKey.map{blockMode(content: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if NowInfoThen.share.appConfigMode.disableShootScreen {
            if NowInfoThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = TrifleShieldView(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: user_deviceId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        frame()
        //: createUI()
        capacityEmpty()
        //: createUIConstraints()
        proposalSire()
        //: bindInteraction()
        orientation()
        //: addTapGestureRecognizer()
        tapText()
        //: func__requestChatInfo()
        center()
        //: func__getFillDraftText()
        carryingQuantityoAction()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        SmartReactiveCompatible.shared.upwardly()
        //: func__addVoiceNotification()
        tagToColumn()
        //: addReadPeceiptNotification()
        setUp()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            executeData()
            //: func__TipsNotification()
            magnitudeNotification()
            //: if NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue {
            if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue {
                //: func__setReplyTipsView()
                funcValueBirdSEyeView()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        messageTo(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        messageTo(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            bringFor()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: CipherViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = CipherViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(dataLabStr)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(GainToViewCell.self, forCellReuseIdentifier: GainToViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(TelevisionReactiveCompatible.self, forCellReuseIdentifier: TelevisionReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(FillLengthMsgCell.self, forCellReuseIdentifier: FillLengthMsgCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(EnableViewCell.self, forCellReuseIdentifier: EnableViewCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(GainView.self, forCellReuseIdentifier: GainView.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ContainerMsgCell.self, forCellReuseIdentifier: ContainerMsgCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(FaceReactiveCompatible.self, forCellReuseIdentifier: FaceReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(EquipmentMsgCell.self, forCellReuseIdentifier: EquipmentMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(AtSaveMessageCell.self, forCellReuseIdentifier: AtSaveMessageCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: GainMessageCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ImageViewCell.self, forCellReuseIdentifier: GainMessageCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: FaceObjectProtocol = {
        //: let m = TalkingCallMenuManager()
        let m = FaceObjectProtocol()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: ScreenRemoveInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: TodayViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = ScreenRemoveInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(kSocialId) + String(main_willHourTitle.suffix(6))))!, .font: UIFont.photographicEquipment(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(notiDirectionTouchTitle)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mvp), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: OfThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = OfThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: VisualImageView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = VisualImageView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.photographicEquipment(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(showObserverId) + String(showWillId))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(dataPathMsg.prefix(6)) + String(userUpWithId.suffix(2)) + String(mainKitKey)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.anPathColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(black), for: .touchUpInside)
        //: btn.isHidden = !NowInfoThen.share.appUserConfigMode.showDirectGreet || NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue || NowInfoThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !NowInfoThen.share.appUserConfigMode.showDirectGreet || NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue || NowInfoThen.share.appStatus == PointRelatableTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: TailTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = TailTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: StockInTradeView = {
        //: let v = TalkingSelectTranslationView()
        let v = StockInTradeView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.stage(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: PracticeView = {
        //: let v = TalkingTranslationResultView()
        let v = PracticeView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.clockWatchingModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.photographicEquipment(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(showObserverId) + String(showWillId))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(user_cellName) + noti_dataTitle.replacingOccurrences(of: "on", with: "o")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.anPathColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(verbalize), for: .touchUpInside)
        //: btn.isHidden = NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue || NowInfoThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue || NowInfoThen.share.appStatus == PointRelatableTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ContainerSessionThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ContainerSessionThen()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: ValueBannerDelegate = {
        //: let view = TalkingPrivateBannerView.init()
        let view = ValueBannerDelegate()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: FundamentalQuantityScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = FundamentalQuantityScreenView()
        //: v.isHidden = (NowInfoThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (NowInfoThen.share.appStatus == PointRelatableTarget.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: TopNameReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = TopNameReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.viewAm()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension LengthViewController: ThreadBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func smallShow(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue {
                if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue {
                if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.draftCopyImage(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension LengthViewController: ModelDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func black() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        WithChatManager.subHandler(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.addMsg(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.audio(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func verbalize() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        WithChatManager.gen(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = ValueCookieThen()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().luculentUpwardly(urlStr: dict?[(showValueInfoName.replacingOccurrences(of: "make", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(app_contentLabData.replacingOccurrences(of: "live", with: "nl") + String(kErrorPath.suffix(7)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = QuerylistModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: mainBlockMessage.map{modelStatus(layer: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(appFirstValue.prefix(5)) + showResultPath.replacingOccurrences(of: "view", with: "d"))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.ofAction(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EqualCellData! {
    func release(_: CipherViewController!, handle data: V2TIMMessage!) -> EqualCellData! {
        //: let dic = TitleChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TitleChatManager.indexUse(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = SendReactiveCompatible.setAbout(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: CookieCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(mainCollectionPath) + dataSinceName.replacingOccurrences(of: "match", with: "pe"))].stringValue == (String(show_frameId.prefix(5)) + "llCu" + noti_showGestureValue.replacingOccurrences(of: "column", with: "om")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(notiListMessage.lowercased() + "eDir" + app_normalMsg.replacingOccurrences(of: "add", with: "ti") + dataImagePath.replacingOccurrences(of: "time", with: "n"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(dataMyFormat))][(String(noti_userId.prefix(7)))][(String(kDataToValue))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ExpensivenessCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ExpensivenessCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: BackgroundReactiveCompatible.self) == true ||
        if cellData?.isKind(of: BackgroundReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: TailCellData.self) == true ||
            cellData?.isKind(of: TailCellData.self) == true ||
            //: cellData?.isKind(of: ExpensivenessCellData.self) == true ||
            cellData?.isKind(of: ExpensivenessCellData.self) == true ||
            //: cellData?.isKind(of: AuditoryImageCellData.self) == true {
            cellData?.isKind(of: AuditoryImageCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            WithChatManager.dataAll(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            WithChatManager.enableIndex(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EqualCellData!) -> GainMessageCell! {
    func with(_: CipherViewController!, info data: EqualCellData!) -> GainMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = appWith(data: data)

        //: if data.isKind(of: ExpensivenessCellData.self) {
        if data.isKind(of: ExpensivenessCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = GainToViewCell(style: .default, reuseIdentifier: GainToViewCell.className())
            //: let cellData = data as! ExpensivenessCellData
            let cellData = data as! ExpensivenessCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.plus() {
                //: if cellData.msgModel.quoteMsgInfo.uid == NowInfoThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == NowInfoThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = NowInfoThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = NowInfoThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: TailCellData.self) {
        if data.isKind(of: TailCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = TelevisionReactiveCompatible(style: .default, reuseIdentifier: TelevisionReactiveCompatible.className())
            //: let cellData = data as! TailCellData
            let cellData = data as! TailCellData
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AuditoryImageCellData.self) {
        if data.isKind(of: AuditoryImageCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = FillLengthMsgCell(style: .default, reuseIdentifier: FillLengthMsgCell.className())
            //: let cellData = data as! AuditoryImageCellData
            let cellData = data as! AuditoryImageCellData
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SessionCellData.self) {
        if data.isKind(of: SessionCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = EnableViewCell(style: .default, reuseIdentifier: EnableViewCell.className())
            //: let cellData = data as! SessionCellData
            let cellData = data as! SessionCellData
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: CompartmentCellData.self) {
        if data.isKind(of: CompartmentCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = GainView(style: .default, reuseIdentifier: GainView.className())
            //: let cellData = data as! CompartmentCellData
            let cellData = data as! CompartmentCellData
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = TitleGainHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.liveStr(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(appAspectKey) + data_resultName.replacingOccurrences(of: "raw", with: "ft")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: BackgroundReactiveCompatible.self) {
        if data.isKind(of: BackgroundReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = EquipmentMsgCell(style: .default, reuseIdentifier: EquipmentMsgCell.className())
            //: let cellData = data as! BackgroundReactiveCompatible
            let cellData = data as! BackgroundReactiveCompatible
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: CookieCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = FaceReactiveCompatible(style: .default, reuseIdentifier: FaceReactiveCompatible.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! CookieCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: ContentThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ContainerMsgCell(style: .default, reuseIdentifier: ContainerMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! ContentThen
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: NameCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = AtSaveMessageCell(style: .default, reuseIdentifier: AtSaveMessageCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! NameCellData
            //: cell.fill(with: cellData)
            cell.compose(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: GainMessageCell!) {
    func smother(_: CipherViewController!, intervalerior cell: GainMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != LineThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != LineThen.imageConnection() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: TotalReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            TotalReactiveCompatible.share.arrange(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: GainMessageCell!) {
    func head(_: CipherViewController!, read cell: GainMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TelevisionReactiveCompatible.self) { // 图片
            //: let imageCellData = cell.messageData as! TailCellData
            let imageCellData = cell.messageData as! TailCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = FrontierViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.layer(imageCellData, managerModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.sizeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showMarginKey)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.visualisation(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.pullDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                controllerEarnModel()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(data_formatName)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = InfoMeasurable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = EqualAddMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [InfoMeasurable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = MomentScreenVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                controllerEarnModel()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: FillLengthMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! AuditoryImageCellData
            let videoCellData = cell.messageData as! AuditoryImageCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = CurrentVideoVc(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                controllerEarnModel()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = ModelViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.pathModel(videoCellData, videoGiftName: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.sizeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showMarginKey)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.visualisation(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.pullDict(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                controllerEarnModel()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: EquipmentMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! EquipmentMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! BackgroundReactiveCompatible, cellView: audioCell)
            colorProduct(cellData: audioCell.cellData as! BackgroundReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: GainToViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! GainToViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TitleChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TitleChatManager.indexUse(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(dataMyFormat))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(notiSucceedAppName))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(notiSucceedAppName))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (data_requestName.replacingOccurrences(of: "lab", with: "u") + "Perso" + String(main_colorName.prefix(5))) {
                    //: if NowInfoThen.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if NowInfoThen.share.loginUserMode.isTPAuth == ToTableConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if NowInfoThen.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if NowInfoThen.share.loginUserMode.isTPAuth == ToTableConvertible.unknown.rawValue {
                        //: TotalReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                        TotalReactiveCompatible.share.giftSocial(toast: nil)
                        //: } else if NowInfoThen.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if NowInfoThen.share.loginUserMode.isTPAuth == ToTableConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = InfoRecognizerDelegate()
                        //: TotalReactiveCompatible.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        TotalReactiveCompatible.share.addressTo()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: TotalReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                        TotalReactiveCompatible.share.giftSocial(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<GainHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TotalReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TotalReactiveCompatible.share.removeToLog(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(mainCollectionPath) + dataSinceName.replacingOccurrences(of: "match", with: "pe"))] as? String == (String(show_frameId.prefix(5)) + "llCu" + noti_showGestureValue.replacingOccurrences(of: "column", with: "om")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(noti_userId.prefix(7)))][(String(show_thenStr) + String(noti_listTitle.prefix(5)))].intValue == 1 {
                    //: checkAndCallVoice()
                    dataDownVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    textOfVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ContainerMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ContainerMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TitleChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TitleChatManager.indexUse(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(dataMyFormat))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(notiSucceedAppName))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<GainHandyJSON>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TotalReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TotalReactiveCompatible.share.removeToLog(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: FaceReactiveCompatible.self) {
            //: TotalReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            TotalReactiveCompatible.share.arrange(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: GainMessageCell!) {
    func heliogram(to _: CipherViewController!, accumulation cell: GainMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: PriceCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TelevisionReactiveCompatible.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! TelevisionReactiveCompatible
            //: cellData = imageCell.cellData as! TailCellData
            cellData = imageCell.cellData as! TailCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: FillLengthMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! FillLengthMsgCell
            //: cellData = videoCell.cellData as! AuditoryImageCellData
            cellData = videoCell.cellData as! AuditoryImageCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: EquipmentMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! EquipmentMsgCell
            //: cellData = audioCell.cellData as! BackgroundReactiveCompatible
            cellData = audioCell.cellData as! BackgroundReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: GainToViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! GainToViewCell
            //: cellData = textCell.cellData as! ExpensivenessCellData
            cellData = textCell.cellData as! ExpensivenessCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: EqualCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: EqualCellData? = SendReactiveCompatible.lightColored(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: AuditoryImageCellData.self) {
                if reCellData!.isKind(of: AuditoryImageCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    commit(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    equalReply(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: EqualCellData!) {
    func fundamentalMeasure(_: CipherViewController!, custom cellData: EqualCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = WithChatManager.digitiserId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: EqualCellData!) {
    func broadcast(_: CipherViewController!, texture cellData: EqualCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        WithChatManager.loadId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: EqualCellData) -> EqualCellData {
    private func appWith(data: EqualCellData) -> EqualCellData {
        //: let isSelf = data.msgModel.user.uid == Int(NowInfoThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(NowInfoThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if NowInfoThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if NowInfoThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = NowInfoThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = NowInfoThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = NowInfoThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = NowInfoThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func airt() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (String(mainLiveContent))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(user_toolName)): (kDataTargetMessage.uppercased() + String(showNameStr))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.equalReply(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func audio(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: k_labelKey.map{imageEqual(match: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: k_labelKey.map{imageEqual(match: $0)}, encoding: .utf8)!: content, (String(constItemValue.prefix(5))): greetId, String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (String(mainLiveContent))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: kMakeId.map{detailInput(text: $0)}, encoding: .utf8)!: content, (String(constItemValue.prefix(5))): greetId, (String(user_toolName)): (String(kWillText.suffix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.equalReply(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            MinimumInfoTaskManager.shared.plantHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.addMsg(showMsg: String(bytes: const_curMessage.map{$0^203}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        WithChatManager.changeEqual(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(noti_toValue.replacingOccurrences(of: "item", with: "l") + notiTalkPathMsg.replacingOccurrences(of: "show", with: "g"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (mainLabelId.replacingOccurrences(of: "intimate", with: "au")),
                               //: "audioId": greetId,
                               (String(k_toFillKey) + String(dataSucceedUserPlayerTitle)): greetId,
                               //: "audioLength": length,
                               String(bytes: data_keyPath.map{$0^251}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o")): model.unZipFilePath,
                               //: "senderId": NowInfoThen.share.loginUserMode.userID]
                               String(bytes: app_effectTitle.reversed(), encoding: .utf8)!: NowInfoThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(k_toFillKey) + String(dataSucceedUserPlayerTitle)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: data_keyPath.map{$0^251}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(user_toolName)): (String(k_tempStr))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.equalReply(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func clockWatchingModel(msgStr: String, quoteModel: GiftModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            addMsg(showMsg: (dataColorId + "send b" + String(data_styleKey.suffix(6)) + "essage").localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: k_labelKey.map{imageEqual(match: $0)}, encoding: .utf8)!: temStr, String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (String(mainLiveContent))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: kMakeId.map{detailInput(text: $0)}, encoding: .utf8)!: temStr, (String(user_toolName)): (String(kWillText.suffix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(noti_userId.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(const_iconMessage.replacingOccurrences(of: "stop", with: "uo") + "eMsg" + String(data_colorStr))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(kVideoFormat) + data_whiteValue.replacingOccurrences(of: "name", with: "nf"))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? GiftModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            equalReply(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func ofAction(img: UIImage, sendModel: QuerylistModel) {
        //: let imagePath = TailCellData.getChatImagePath(image: img)
        let imagePath = TailCellData.addImage(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: main_fillValue.map{fatalHidden(model: $0)}, encoding: .utf8)!: (String(k_makeContent.suffix(6))), String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (k_qualityUrl.replacingOccurrences(of: "lab", with: "m")), String(bytes: kCounteractionValue.map{itemRequest(all: $0)}, encoding: .utf8)!: imagePath, String(bytes: kAddLayerContent.map{nameMoreColor(page: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(k_allData.replacingOccurrences(of: "list", with: "pi")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(user_toolName)): (String(k_makeContent.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: mainBlockMessage.map{modelStatus(layer: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(appFirstValue.prefix(5)) + showResultPath.replacingOccurrences(of: "view", with: "d")): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            equalReply(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: EqualCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func commit(msgData: EqualCellData?, sendModel: QuerylistModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: main_fillValue.map{fatalHidden(model: $0)}, encoding: .utf8)!: (String(data_viewExecuteShowStr.suffix(5)) + String(app_titleIndexStr.suffix(3))), String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (String(user_selectedText)), String(bytes: kAddLayerContent.map{nameMoreColor(page: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(user_toolName)): (String(data_viewExecuteShowStr.suffix(5)) + String(app_titleIndexStr.suffix(3))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: notiUserTitle.reversed(), encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: user_finishMessage.map{norValue(contact: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: show_trackPath.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: mainBlockMessage.map{modelStatus(layer: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(appFirstValue.prefix(5)) + showResultPath.replacingOccurrences(of: "view", with: "d")): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = SendReactiveCompatible.actionBackground(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.filter(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.showWith(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            self.msgView.simulation(cellData, usage: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.showWith(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.cell(dict, message: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        equalReply(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func astatine(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (mainLabelId.replacingOccurrences(of: "intimate", with: "au")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: data_keyPath.map{$0^251}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o")): voicePath,
                       //: "senderId": NowInfoThen.share.loginUserMode.userID]
                       String(bytes: app_effectTitle.reversed(), encoding: .utf8)!: NowInfoThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o")): voicePath,
                         //: "type": "AudioMsg",
                         (String(user_toolName)): (String(k_tempStr)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: dataNameFormat.map{modelMode(application: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            equalReply(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EqualCellData, reSend: Bool) {
    private func equalReply(msgData: EqualCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            addMsg(showMsg: mainTurnMsg)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(kDataToValue))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: AuditoryImageCellData.self) == false && msgData.isKind(of: CompartmentCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: AuditoryImageCellData.self) == false, msgData.isKind(of: CompartmentCellData.self) == false, params[(String(user_toolName))] as? String != (kDataTargetMessage.uppercased() + String(showNameStr)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.filter(msgData)
        }
        //: let recordTime = TitleChatManager.getMessageInsertTime()
        let recordTime = TitleChatManager.levelDismiss()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((k_allData.replacingOccurrences(of: "list", with: "pi"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(k_allData.replacingOccurrences(of: "list", with: "pi"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(k_allData.replacingOccurrences(of: "list", with: "pi"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(user_toolName))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(k_tempStr)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(userExplainText) + app_targetFormat.replacingOccurrences(of: "time", with: "o")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(mainTitleName) + String(k_insideName))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = const_domainTitle + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(mainMakeStr) + String(user_sizeText.suffix(6)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = noti_objectScreenFormat + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(k_toFillKey) + String(dataSucceedUserPlayerTitle))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(mainLabelId.replacingOccurrences(of: "intimate", with: "au"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: MonetaryValueThen.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        MonetaryValueThen.throughGift(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (kDataTargetMessage.uppercased() + String(showNameStr)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.name(msgData, survival: nil, surcharge: NSNumber(value: 0), session: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.sizeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: show_dataItemMsg)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(constItemValue.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    WithChatManager.changeEqual(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.orderedSeriesGift(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(noti_userId.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(k_activityData.prefix(8)) + String(show_addText))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(k_activityData.prefix(8)) + String(show_addText))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if ProduceViewPushListener.decide().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: constBarNetPath, object: nil, userInfo: [String(bytes: kInfoText.map{$0^50}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.chase(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(userNameAtPath.replacingOccurrences(of: "color", with: "e") + "eType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (kDataTargetMessage.uppercased() + String(showNameStr)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!] = (String(app_dataUserName) + String(constLabFormat))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    dataMatchStr.isometric(eventID: main_keyPath)
                    //: } else if type == "QuickMsg" {
                } else if type == (kDataTargetMessage.uppercased() + String(showNameStr)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!] = (String(mainLiveContent))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(noti_userId.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.filter(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(NowInfoThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(NowInfoThen.share.loginUserMode.userID)" + (String(main_succeedCurrentText) + String(user_waitHalfKey.suffix(7)) + String(mainInputId)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if NowInfoThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.male.rawValue, type != (kDataTargetMessage.uppercased() + String(showNameStr)) {
                //: self.removeReplyTipsView()
                self.premium()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.name(updateMsgData, survival: msgInfo[(String(data_onValue.suffix(5)))] as? String, surcharge: msgIncome, session: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            WithChatManager.enableIndex(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(user_viewMessage.prefix(9)) + String(noti_requestValue))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(user_viewMessage.prefix(9)) + String(noti_requestValue))] as! NSNumber
                //: NowInfoThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                NowInfoThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((main_targetStr.lowercased() + String(userScreenPath))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(main_targetStr.lowercased() + String(userScreenPath))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.cloisteredLine(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((main_bottomUrl.lowercased() + "mateP" + showUserMessage.replacingOccurrences(of: "cell", with: "o"))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(main_bottomUrl.lowercased() + "mateP" + showUserMessage.replacingOccurrences(of: "cell", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? OpenModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.layer(updateMsgData, managerModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(user_selectedText))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(user_selectedText))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? AlongModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.pathModel(updateMsgData, videoGiftName: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        chatPrivateGiftVideoBuildTip()

        //: updateSession()
        enablee()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func enablee() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        AtReactiveCompatible.ofKey(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func sizeEvent(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard NowInfoThen.share.loginUserMode.status != 1 else {
            guard NowInfoThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    addMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TotalReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            TotalReactiveCompatible.share.musculusSphincterPupillae(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.panoramicView()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = InsertPopView(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: user_deviceId))
            //: view.show()
            view.popShow()
            //: } else if errorCode == 60003, !NowInfoThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !NowInfoThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            PopupReactiveCompatible.shared.changeShape()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                addMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func pullDict(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(noti_userId.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.chase(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func likeness() {
        //: if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.startUser(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(NowInfoThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(NowInfoThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (main_clickSenseKey.array(forKey: notiFailMsg)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.overdo()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                main_clickSenseKey.set(intimateCacheData, forKey: notiFailMsg)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func facialNerveNotification(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TitleChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TitleChatManager.indexUse(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(dataMyFormat))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(noti_userId.prefix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(dataNameKey))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.leafSmall(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func chase(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(dataNameKey))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(dataNameKey))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.leafSmall(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func leafSmall(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = AtReactiveCompatible.viewText(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.startUser(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.stopCountimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.upOf(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.upOf(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        AtReactiveCompatible.lade(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: constCropText.reversed(), encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: showKnowNameAfterData.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: user_showFormat,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension LengthViewController: DelegateFaceThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func belt() {
        //: guard NowInfoThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.feature(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func mention(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func anoint(actionType: BalletScalar) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    bindPath()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    towardView()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                bindPath()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if NowInfoThen.share.loginUserMode.sex == Gender.female.rawValue {
            if NowInfoThen.share.loginUserMode.sex == PointExpressionConvertible.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if NowInfoThen.share.loginUserMode.loungePlus {
                if NowInfoThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.weltanschauungTotalerval(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                towardView()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    progressDoing()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    towardView()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                progressDoing()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.vanguardApp(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func towardView() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.addMsg(showMsg: String(bytes: user_sectionName.map{availableImage(video: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func be(giftModel _: TitleGainHandyJSON, giftNum _: String, model _: EqualMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func velleityPlay(giftModel: TitleGainHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        liveStr(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: CompartmentCellData?, fromStr: String?) {
    func liveStr(giftModel: TitleGainHandyJSON, giftNum: String, cellData: CompartmentCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            addMsg(showMsg: mainTurnMsg)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(kDataToValue))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(appFirstValue.prefix(5)) + showResultPath.replacingOccurrences(of: "view", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(k_retSizeData.prefix(6)) + String(constEqualName))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(mainMaleResignMsg.suffix(6)) + String(main_toMsg))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(notiWithName))] = fromStr
        }
        // 送礼接口
        //: MonetaryValueThen.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        MonetaryValueThen.centerCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.sizeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: show_frameKey)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.bestowment(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.visualisation(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.enablee()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(noti_userId.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(dataNameKey))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(dataNameKey))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.leafSmall(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.message(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                dataMatchStr.isometric(eventID: app_textFormat)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func instanceNum(giftModel: TitleGainHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        dataMatchStr.isometric(eventID: user_profileStr)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            addMsg(showMsg: mainTurnMsg)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= NowInfoThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            towardView()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(userLabStr) + String(appSectionUrl))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(user_toolName))] = (String(notiImageText) + String(userFatalId))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (String(app_dataUserName) + String(constLabFormat))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(noti_userId.prefix(7))): msgInfo, String(bytes: show_releaseErrorText.map{eventFromBack(source: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SendReactiveCompatible.actionBackground(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            equalReply(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func lagniappe(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(app_formalMsg - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.activeSumBase(true)
            self.msgView.activeSumBase(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func nameHeight(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.activeSumBase(true)
            self.msgView.activeSumBase(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func byRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.byRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                slightCollection(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func bottomOn() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if SmartReactiveCompatible.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SmartReactiveCompatible.shared.popWithPlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func visualisation(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(noti_userId.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!] = (mainCreateKey.replacingOccurrences(of: "complete", with: "t"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(noti_userId.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = SendReactiveCompatible.actionBackground(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.filter(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(k_activityData.prefix(8)) + String(show_addText))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(k_activityData.prefix(8)) + String(show_addText))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.name(cellData, survival: msgInfoDic[(String(data_onValue.suffix(5)))] as? String, surcharge: msgIncome, session: .Msg_Status_Succ)
                
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        crown(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(user_viewMessage.prefix(9)) + String(noti_requestValue))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(user_viewMessage.prefix(9)) + String(noti_requestValue))] as! NSNumber
            //: NowInfoThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            NowInfoThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: NowInfoThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.undersurface(needReload: false, mf_coin: NowInfoThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func executeData() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.timeInfo(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func crown(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.rowDown(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func beyondUp(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.labObject(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func picImage() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func dataDownVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.barComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.enablee()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func textOfVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.outDelete { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.enablee()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension LengthViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func mvp() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = TargetViewFeedbackVc()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        controllerEarnModel()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func whereaboutsBelow() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MunuDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.doingList(cellTitleList: [(const_viewAppName.replacingOccurrences(of: "model", with: "V") + String(showMentionValue.suffix(4)) + "info").localized, (String(main_dateKey)).localized, (String(k_viewShareData)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .towardOf()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: TotalReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                TotalReactiveCompatible.share.arrange(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                viewAm()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = WithFrameAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.equalAttribute(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func viewAm() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = RelieveViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.actionAdd(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.suite(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func makeDetail() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            slightCollection(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            controllerEarnModel()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func pouch(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = AppHandyJSON.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.nameUp(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func setUp() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(nameNeed(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: const_recordData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(anyItem),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: userPlayMsg,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func nameNeed(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard NowInfoThen.share.loginUserMode.loungePlus == true else { return }
        guard NowInfoThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func anyItem() {
        //: NowInfoThen.share.loginUserMode.loungePlus = true
        NowInfoThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func pause() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? EqualCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? EqualCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if WithChatManager.behindAdd(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.press(tipStr: (String(app_smallKey) + String(appDataPlayerTableText.suffix(5)) + String(kMakeViewValue.prefix(5))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension LengthViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func bindPath() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        LengthPermissionTool.lever(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = NumberReactiveCompatible.acceptVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.bringHomeWithoutPhoto(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func paintOrPhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        LengthPermissionTool.paneOfGlass(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.bringHomeWithoutPhoto(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func bringHomeWithoutPhoto(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = QuerylistModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.ofAction(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = MomentScreenVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = QuerylistModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.ofAction(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        controllerEarnModel()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func progressDoing() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard WithChatManager.likeVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == PointExpressionConvertible.male.rawValue ? "He".localized : (String(show_refuseContent.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.addMsg(showMsg: (String(dataControlMsg.prefix(6)) + "using " + String(constTextUrl.prefix(5)) + "ow a " + kAddStr.replacingOccurrences(of: "pic", with: "v") + data_feedbackModelEffectPath.lowercased()).toAppear(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.playerWith()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func playerWith() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = NumberReactiveCompatible.acceptVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            NumberReactiveCompatible.overMessage(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.videoColor(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.addMsg(showMsg: (String(k_faceTitlePath.suffix(5)) + " video" + String(main_makeKey.prefix(6)) + String(mainArrayUrl)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func videoColor(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: GainEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = VisualCommunicationViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = QuerylistModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.commit(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        DeleteReactiveCompatible.deflector()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        controllerEarnModel()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: EqualCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func showWith(msgData: EqualCellData, sendModel: QuerylistModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        TailSignatureProvider.shared.uploadFromCompletion(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.addMsg(showMsg: (String(appBlockMsg) + String(main_positionBucketEndValue)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.name(msgData, survival: nil, surcharge: NSNumber(value: 0), session: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.commit(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension LengthViewController {
    //: func func__addVoiceNotification() {
    func tagToColumn() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(conference),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(isBar),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(asset(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(untilClick(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: showListData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(facialNerveNotification(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: const_tableBarValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func messageTo(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            SmartReactiveCompatible.shared.upwardly()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            SmartReactiveCompatible.shared.cradle()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func conference() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        messageTo(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func isBar() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        messageTo(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func asset(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            messageTo(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            messageTo(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: BackgroundReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func colorProduct(cellData: BackgroundReactiveCompatible, cellView: EquipmentMsgCell) {
        //: guard TalkingSocketManager.shared.isTarget == false else {
        guard BankManagerSocketDelegate.shared.isTarget == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.addMsg(showMsg: dataMessageName)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? EqualCellData)!.isKind(of: BackgroundReactiveCompatible.self) == false {
            if (indexModel as? EqualCellData)!.isKind(of: BackgroundReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! BackgroundReactiveCompatible
            let audioModel = indexModel as! BackgroundReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = AtReactiveCompatible.magnitudeimate(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        SmartReactiveCompatible.shared.popWithPlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if SmartReactiveCompatible.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(notiViewTitle)) + "\(SmartReactiveCompatible.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if SmartReactiveCompatible.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                SmartReactiveCompatible.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        SmartReactiveCompatible.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        SmartReactiveCompatible.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: EqualCellData = self.msgView.uiMsgs.object(at: index) as! EqualCellData
                let subModel: EqualCellData = self.msgView.uiMsgs.object(at: index) as! EqualCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: BackgroundReactiveCompatible.self) {
                if subModel.isKind(of: BackgroundReactiveCompatible.self) {
                    //: let audioModel = subModel as! BackgroundReactiveCompatible
                    let audioModel = subModel as! BackgroundReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = AtReactiveCompatible.magnitudeimate(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    BeautyMsgTable.companyText(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        SmartReactiveCompatible.shared.putt(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func center() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.gender()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.audio(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? EqualCellData
                let indexModel = model as? EqualCellData
                //: if indexModel?.isKind(of: BackgroundReactiveCompatible.self) == true || indexModel?.isKind(of: TailCellData.self) == true || indexModel?.isKind(of: ExpensivenessCellData.self) == true || indexModel?.isKind(of: AuditoryImageCellData.self) == true {
                if indexModel?.isKind(of: BackgroundReactiveCompatible.self) == true || indexModel?.isKind(of: TailCellData.self) == true || indexModel?.isKind(of: ExpensivenessCellData.self) == true || indexModel?.isKind(of: AuditoryImageCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.chatPrivateGiftVideoBuildTip()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.plugIn()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.pause()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func gender() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.viewFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.withData(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.likeness()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == NowInfoThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == NowInfoThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == NowInfoThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == NowInfoThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.suite(userWrap)
    }

    //: func func__getFillDraftText() {
    func carryingQuantityoAction() {
        //: let userID = "c2c_"+targetId
        let userID = (String(const_lastKey.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.belt()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func bringFor() {
        //: let userID = "c2c_"+targetId
        let userID = (String(const_lastKey.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension LengthViewController {
    //: func func__TipsNotification() {
    func magnitudeNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(actionTurnDestroy(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: data_userKeyId,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func actionTurnDestroy(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: ContentThen = userinfo[(String(notiCellKey))] as! ContentThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.activeSumBase(true)
        self.msgView.activeSumBase(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func press(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: k_labelKey.map{imageEqual(match: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: show_valueArrayMsg.map{colorButton(info: $0)}, encoding: .utf8)!: (k_worldTableData.replacingOccurrences(of: "style", with: "ti"))]
        //: var message: EqualCellData?
        var message: EqualCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = SendReactiveCompatible.exceptImage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = SendReactiveCompatible.actionBackground(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.activeSumBase(true)
        self.msgView.activeSumBase(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func plugIn() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        WithChatManager.sectionWrite { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.press(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func cloisteredLine(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.press(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func chatPrivateGiftVideoBuildTip() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        WithChatManager.isochronous(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.press(tipStr: String(bytes: user_sizeUrl.map{$0^184}, encoding: .utf8)!.toAppear(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func untilClick(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: EqualCellData = dict?["cellData"] as! EqualCellData
        let cellData: EqualCellData = dict?[(String(showPlayerFormat) + String(app_viewId.prefix(6)))] as! EqualCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.activeSumBase(true)
        self.msgView.activeSumBase(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension LengthViewController: HeritageReactiveCompatible {
    //: func func__setReplyTipsView() {
    func funcValueBirdSEyeView() {
        //: let key =  "\(NowInfoThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(NowInfoThen.share.loginUserMode.userID)" + (String(main_succeedCurrentText) + String(user_waitHalfKey.suffix(7)) + String(mainInputId)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.executeView()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            smallShow(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            premium()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func exaggerateStr(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        clockWatchingModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        premium()
    }

    //: func removeReplyTipsView() {
    func premium() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            smallShow(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func frame() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: mainMessageId, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: user_deviceId - app_formalMsg, width: constExplainViewMsg, height: user_deviceId)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: constExplainViewMsg,
                                     //: height: ScreenHeight)
                                     height: user_deviceId)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func slightCollection(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.buttonController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if ConstraintEventHandler.bindTo().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: app_keyErrUrl, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: InfoViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: user_deviceId - app_formalMsg,
                                     //: width: ScreenWidth,
                                     width: constExplainViewMsg,
                                     //: height: ScreenHeight)
                                     height: user_deviceId)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if ConstraintEventHandler.bindTo().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: app_keyErrUrl, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension LengthViewController {
    /// 创建UI
    //: private func createUI() {
    private func capacityEmpty() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(show_playTitle) + String(main_groupStr.prefix(7)) + userListValue.replacingOccurrences(of: "kit", with: "ic")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func proposalSire() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? app_formalMsg : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.hauntWithoutRadii(width: contentView.width,
                                          //: height: contentView.height,
                                          height: contentView.height,
                                          //: corners: [.topLeft, .topRight],
                                          corners: [.topLeft, .topRight],
                                          //: cornerRadii: .init(width: 8, height: 8))
                                          cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_lineMsg : showMessageId)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + noti_limitPartyInfoUrl)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_lineMsg : showMessageId)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + noti_limitPartyInfoUrl)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + noti_limitPartyInfoUrl)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func orientation() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.makeDetail()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.whereaboutsBelow()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.anoint(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                dataMatchStr.isometric(eventID: main_userMessageKey, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
