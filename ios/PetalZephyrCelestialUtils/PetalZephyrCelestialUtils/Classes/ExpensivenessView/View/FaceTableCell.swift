
//: Declare String Begin

/*: "formatType" :*/
fileprivate let app_blockScaleUrl:[Character] = ["f","o","r","m","a","t","T"]
fileprivate let data_frameId:String = "ypmake"

/*: "plain" :*/
fileprivate let dataMessageStr:String = "pstartain"

/*: "init(coder:) has not been implemented" :*/
fileprivate let kPressContent:[UInt8] = [0xcf,0xc8,0xcf,0xd2,0x8e,0xc5,0xc9,0xc2,0xc3,0xd4,0x9c,0x8f,0x86,0xce,0xc7,0xd5,0x86,0xc8,0xc9,0xd2,0x86,0xc4,0xc3,0xc3,0xc8,0x86,0xcf,0xcb,0xd6,0xca,0xc3,0xcb,0xc3,0xc8,0xd2,0xc3,0xc2]

private func letMirror(exception num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "#FF2348" :*/
fileprivate let appViewValue:String = "#FF2348text view image pan kit"

/*: "icon_lounge" :*/
fileprivate let appResultId:[Character] = ["i","c","o","n","_","l","o","u","n","g","e"]

/*: "icon_home_v" :*/
fileprivate let mainTextData:String = "mode send let return leadingicon_"
fileprivate let appGroupData:String = "normal"

/*: "icon_chat_list_new" :*/
fileprivate let showSeatMsg:[Character] = ["i","c","o","n","_"]
fileprivate let noti_voiceStr:String = "chvideo"
fileprivate let noti_makeData:String = "t_limake equal model for block"

/*: "Official" :*/
fileprivate let userTextFormat:[Character] = ["O","f","f","i","c","i","a","l"]

/*: "#30D00B" :*/
fileprivate let showEquipmentKey:[Character] = ["#","3","0","D","0","0","B"]

/*: "iv_message_honey" :*/
fileprivate let kToText:String = "extension leading moment viewiv_me"
fileprivate let appCustomPath:String = "share transform below modele_honey"

/*: "#FF60B9" :*/
fileprivate let mainPathUrl:[Character] = ["#","F","F","6","0","B","9"]

/*: "#EEEEEE" :*/
fileprivate let notiSearchName:String = "base self file else#EEEEEE"

/*: "opacity" :*/
fileprivate let const_makeRetStreamPath:String = "howp"
fileprivate let appViewUrl:[Character] = ["a","c","i","t","y"]

/*: "path" :*/
fileprivate let kTopBottomUrl:String = "patstatus"

/*: "extra" :*/
fileprivate let constTitleData:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let notiModelKey:String = "msgInfomenu if"

/*: "content" :*/
fileprivate let dataCellLabelPath:[Character] = ["c"]
fileprivate let kAddMessage:[Character] = ["o","n","t","e","n","t"]

/*: "messageType" :*/
fileprivate let dataHiddenStr:[Character] = ["m","e"]
fileprivate let mainModeStr:String = "ssageTypevar manager name make"

/*: "[Reply messages to get points]" :*/
fileprivate let data_tableIconTitle:[Character] = ["[","R","e","p","l","y"," ","m","e","s","s","a","g","e","s"," ","t","o"," ","g","e","t"," ","p","o","i","n","t","s"]
fileprivate let const_viewUrl:String = "]"

/*: "title" :*/
fileprivate let show_makeErrorPath:[Character] = ["t","i","t","l","e"]

/*: "img" :*/
fileprivate let kRequestValue:String = "finishg"

/*: "[Photo]" :*/
fileprivate let const_modelTitle:String = "icon smart let[Photo]"

/*: "[Audio]" :*/
fileprivate let data_viewFormat:String = "make info status if[Audio]"

/*: "rYMsgType" :*/
fileprivate let notiTopMessage:String = "rYMsgTypmessage make data"
fileprivate let const_matchBarPath:[Character] = ["e"]

/*: "GJ:CallCustom" :*/
fileprivate let dataViewId:String = "GJ:Calself file"
fileprivate let appVideoMsg:String = "time label aslCustom"

/*: "callCmd" :*/
fileprivate let mainScaleData:String = "fee"
fileprivate let showDataText:String = "data nonellCmd"

/*: "missCall" :*/
fileprivate let const_pinPath:String = "info head mediummissCa"
fileprivate let mainTouchUserFormat:String = "LL"

/*: "[Missed Call]" :*/
fileprivate let constStyleFormat:String = "label mode center[Missed "
fileprivate let const_appStr:[Character] = ["C","a","l","l","]"]

/*: "logType" :*/
fileprivate let const_tableId:String = "L"
fileprivate let constAddMessage:String = "ogTypemodel refuse white height"

/*: "[Voice Call]" :*/
fileprivate let noti_viewId:String = "video player progress[Voice C"
fileprivate let constQuoteFormat:String = "all]var style"

/*: "[Video Call]" :*/
fileprivate let show_momentText:String = "[Videoinput as line interaction name"
fileprivate let data_backMessage:String = "let in for to near Call]"

/*: "video" :*/
fileprivate let data_labContentKey:[Character] = ["v","i","d","e","o"]

/*: "contentType" :*/
fileprivate let noti_countAppVarStr:[Character] = ["c","o","n","t","e","n"]
fileprivate let user_centerViewStr:[Character] = ["t","T","y","p","e"]

/*: "VideoMsg" :*/
fileprivate let noti_giftRefuseContent:[Character] = ["V","i","d","e","o"]
fileprivate let mainAtModelKey:String = "Msgmake make first image"

/*: "[Short Video]" :*/
fileprivate let app_quotePath:String = "[Shorsize action formal data hidden"

/*: "match" :*/
fileprivate let showRefreshText:String = "mcountch"

/*: "[Cupid messaging]" :*/
fileprivate let kTextBurnPath:[Character] = ["[","C","u","p","i","d"," "]
fileprivate let noti_connectionData:[Character] = ["m","e","s","s","a"]
fileprivate let const_labName:[Character] = ["g","i","n","g","]"]

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let k_insideKey:[Character] = ["G","J",":","S","e","s","s",":","T","x","t","C","u","s","t"]
fileprivate let notiPageRecordMsg:[Character] = ["o","m"]

/*: "isSend" :*/
fileprivate let appBroadcastMsg:String = "left text user name upisSend"

/*: "Sent Gift" :*/
fileprivate let kHiddenStr:[Character] = ["S","e","n"]
fileprivate let app_videoStr:[Character] = ["t"," ","G","i","f","t"]

/*: "Receive Gift" :*/
fileprivate let constSexViewName:[Character] = ["R","e","c","e","i","v","e"," ","G"]
fileprivate let dataLiveTitle:[Character] = ["i","f","t"]

/*: "uid" :*/
fileprivate let appFrameId:String = "celld"

/*: "gift" :*/
fileprivate let user_pageData:String = "gititlet"

/*: "name" :*/
fileprivate let const_viewStr:[Character] = ["n","a","m","e"]

/*: "num" :*/
fileprivate let appWhitePathFormat:String = "NUM"

/*:   :*/
fileprivate let main_addName:[Character] = [" "]

/*:  x :*/
fileprivate let data_colorMessage:[Character] = [" ","x"]

/*: "imageUri" :*/
fileprivate let data_centerMsg:String = "immomente"
fileprivate let showPlayKey:String = "Uriif model image as"

/*: "imageData" :*/
fileprivate let userReadBagMsg:String = "ivaluea"
fileprivate let notiNextContent:String = "cell letgeData"

/*: "<img>" :*/
fileprivate let mainEndData:String = "self make quote<img>"

/*: "</img>" :*/
fileprivate let app_normalMessage:String = "</img>clear view cell"

/*: "wantGift" :*/
fileprivate let userColorTitle:String = "wantGiftlet key from view"

/*: "intimatePhoto" :*/
fileprivate let notiModeUrl:String = "idataim"
fileprivate let constSucceedKey:[Character] = ["o","t","o"]

/*: "audioData" :*/
fileprivate let showEqualName:String = "audioDatstatus leading self"
fileprivate let app_momentBoxFormat:[Character] = ["a"]

/*: "audioUri" :*/
fileprivate let show_originFormat:[Character] = ["a","u","d","i","o","U","r","i"]

/*: "draftContent" :*/
fileprivate let data_messageMsg:String = "draftConat post name popular"
fileprivate let user_startPath:String = "teerrort"

/*: "[draft]%@" :*/
fileprivate let kIndexName:String = "[drtype name"
fileprivate let mainLineStr:String = "signature equalaft]%@"

/*: "99999+" :*/
fileprivate let mainRemoveMsg:[UInt8] = [0xdf,0xdf,0xdf,0xdf,0xdf,0xd1]

fileprivate func iconCell(live num: UInt8) -> UInt8 {
    let value = Int(num) + 90
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "New" :*/
fileprivate let dataPathMessage:String = "Newdata time view"

/*: "99+" :*/
fileprivate let notiMainId:String = "toptop+"

/*: "Account is restricted！" :*/
fileprivate let appModelMsg:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i","c","t","e"]
fileprivate let noti_whiteStr:String = "d！path gift"

/*: "%@ Online" :*/
fileprivate let app_messageId:String = "%@ Onframe leading text in"
fileprivate let const_screenPath:String = "linsize"

/*: "Messages not been replied here" :*/
fileprivate let constTableId:String = "Messaclose margin"
fileprivate let show_videoId:String = "not bdecide mouth button"
fileprivate let main_tapPath:String = "duringepl"
fileprivate let k_equalFormat:String = "heinput"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FaceTableCell.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let main_senseUserName = (String(app_blockScaleUrl) + data_frameId.replacingOccurrences(of: "make", with: "e"))
//: let RCSystemFormatTypePlain            = "plain"
let app_viewMsg = (dataMessageStr.replacingOccurrences(of: "start", with: "l"))

//: class TalkingChatListTableCell: UITableViewCell {
class FaceTableCell: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kPressContent.map{letMirror(exception: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        toUi()
        //: createCellUIConstraints()
        adjust()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 13)
        label.font = .photographicEquipment(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(appViewValue.prefix(7))))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 18)
        label.font = .photographicEquipment(type: .Medium, fontSize: 18)
        //: label.textColor = .appTitleColor()
        label.textColor = .towardOf()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .photographicEquipment(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .someRegard()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 12)
        label.font = .photographicEquipment(type: .Medium, fontSize: 12)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .someRegard()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.atomicCount85(name: (String(appResultId)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.BundleImageNamed(name: "icon_home_v")
        imgV.image = UIImage.atomicCount85(name: (String(mainTextData.suffix(5)) + "home_" + appGroupData.replacingOccurrences(of: "normal", with: "v")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: BlockButton = {
        //: let imgV = TalkingButton()
        let imgV = BlockButton()
        //: imgV.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.atomicCount85(name: (String(showSeatMsg) + noti_voiceStr.replacingOccurrences(of: "video", with: "a") + String(noti_makeData.prefix(4)) + "st_new")), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.forClick(color: UIColor.tips(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(userTextFormat)).localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 11)
        img.titleLabel?.font = UIFont.labelWith(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (String(showEquipmentKey)))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: BlockButton = {
        //: let btn = TalkingButton.init()
        let btn = BlockButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.atomicCount85(name: (String(kToText.suffix(5)) + "ssag" + String(appCustomPath.suffix(7)))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .photographicEquipment(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(mainPathUrl))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: if LanguageManager.shared.direction == .rightToLeft {
        if WithLanguageManager.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension FaceTableCell {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(notiSearchName.suffix(7))))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (const_makeRetStreamPath.replacingOccurrences(of: "how", with: "o") + String(appViewUrl)))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (const_makeRetStreamPath.replacingOccurrences(of: "how", with: "o") + String(appViewUrl)))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (kTopBottomUrl.replacingOccurrences(of: "status", with: "h")))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (kTopBottomUrl.replacingOccurrences(of: "status", with: "h")))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension FaceTableCell {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func appShow(model: CommentReactiveCompatible?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        individualOther(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        pathBy(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        cameraLog(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.loadDate(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(notiSearchName.suffix(7))))
            //: } else {
        } else {
            //: contentView.backgroundColor = (NowInfoThen.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (NowInfoThen.share.appStatus == PointRelatableTarget.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func noSub(model: CommentReactiveCompatible) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: CanisterMagnitudeLiteral = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.equalMessage(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.photographicEquipment(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(String(constTitleData))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(notiModelKey.prefix(7)))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(String(dataCellLabelPath) + String(kAddMessage))].stringValue.openTogether()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(String(dataHiddenStr) + String(mainModeStr.prefix(9)))].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(String(app_blockScaleUrl) + data_frameId.replacingOccurrences(of: "make", with: "e"))].stringValue

        //: if TalkingPrivateChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if WithChatManager.toOff(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(data_tableIconTitle) + const_viewUrl.capitalized).localized
            //: let color  = UIColor.appValueColor()
            let color = UIColor.heatherMixture()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(String(show_makeErrorPath))].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(String(dataCellLabelPath) + String(kAddMessage))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(kRequestValue.replacingOccurrences(of: "finish", with: "im"))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(const_modelTitle.suffix(7))).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(String(dataCellLabelPath) + String(kAddMessage))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(String(dataCellLabelPath) + String(kAddMessage))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.openTogether()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if TitleChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if TitleChatManager.numberMsginfo(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(data_viewFormat.suffix(7))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(notiTopMessage.prefix(8)) + String(const_matchBarPath))].stringValue == (String(dataViewId.prefix(6)) + String(appVideoMsg.suffix(7))) { // 音视频通话
            //: if msgInfo["callCmd"].stringValue == "missCall" { // 未接来电
            if msgInfo[(mainScaleData.replacingOccurrences(of: "fee", with: "ca") + String(showDataText.suffix(5)))].stringValue == (String(const_pinPath.suffix(6)) + mainTouchUserFormat.lowercased()) { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(constStyleFormat.suffix(8)) + String(const_appStr)).localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(const_tableId.lowercased() + String(constAddMessage.prefix(6)))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (String(noti_viewId.suffix(8)) + String(constQuoteFormat.prefix(4))).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(show_momentText.prefix(6)) + String(data_backMessage.suffix(6))).localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if msgInfo["video"].exists() || msgInfo["contentType"].stringValue == "VideoMsg" { // 短视频
        } else if msgInfo[(String(data_labContentKey))].exists() || msgInfo[(String(noti_countAppVarStr) + String(user_centerViewStr))].stringValue == (String(noti_giftRefuseContent) + String(mainAtModelKey.prefix(3))) { // 短视频
            //: content = "[Short Video]".localized
            content = (String(app_quotePath.prefix(5)) + "t Video]").localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(showRefreshText.replacingOccurrences(of: "count", with: "at"))].stringValue == "1" || msgInfo[(showRefreshText.replacingOccurrences(of: "count", with: "at"))].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(kTextBurnPath) + String(noti_connectionData) + String(const_labName)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(notiTopMessage.prefix(8)) + String(const_matchBarPath))].stringValue == (String(k_insideKey) + String(notiPageRecordMsg)) { // 提示消息
            //: content = json["content"].stringValue
            content = json[(String(dataCellLabelPath) + String(kAddMessage))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(appBroadcastMsg.suffix(6)))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (String(kHiddenStr) + String(app_videoStr)).localized : (String(constSexViewName) + String(dataLiveTitle)).localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(appFrameId.replacingOccurrences(of: "cell", with: "ui"))].stringValue
                //: string1 = (uid == NowInfoThen.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == NowInfoThen.share.loginUserMode.userID) ? (String(kHiddenStr) + String(app_videoStr)).localized : (String(constSexViewName) + String(dataLiveTitle)).localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(user_pageData.replacingOccurrences(of: "title", with: "f"))][(String(const_viewStr))].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(user_pageData.replacingOccurrences(of: "title", with: "f"))][(appWhitePathFormat.lowercased())].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(data_centerMsg.replacingOccurrences(of: "moment", with: "ag") + String(showPlayKey.prefix(3)))].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(userReadBagMsg.replacingOccurrences(of: "value", with: "m") + String(notiNextContent.suffix(6)))].stringValue

            //: if TitleChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TitleChatManager.numberMsginfo(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(data_viewFormat.suffix(7))).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(const_modelTitle.suffix(7))).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == app_viewMsg {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(String(dataCellLabelPath) + String(kAddMessage))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(data_centerMsg.replacingOccurrences(of: "moment", with: "ag") + String(showPlayKey.prefix(3)))].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(userReadBagMsg.replacingOccurrences(of: "value", with: "m") + String(notiNextContent.suffix(6)))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(mainEndData.suffix(5)))), content.hasSuffix((String(app_normalMessage.prefix(6)))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(mainEndData.suffix(5))), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(app_normalMessage.prefix(6))), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(const_modelTitle.suffix(7))).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.openTogether()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(String(userColorTitle.prefix(8)))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(String(dataCellLabelPath) + String(kAddMessage))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else {
        } else {
            // 图片

            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(data_centerMsg.replacingOccurrences(of: "moment", with: "ag") + String(showPlayKey.prefix(3)))].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(userReadBagMsg.replacingOccurrences(of: "value", with: "m") + String(notiNextContent.suffix(6)))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(notiModeUrl.replacingOccurrences(of: "data", with: "nt") + "atePh" + String(constSucceedKey))].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(const_modelTitle.suffix(7))).localized
            }
            // 语音消息
            //: if TitleChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TitleChatManager.numberMsginfo(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(data_viewFormat.suffix(7))).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(data_viewFormat.suffix(7))).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = NowInfoThen.share.loginUserMode.userID
                let mid = NowInfoThen.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(String(showEqualName.prefix(8)) + String(app_momentBoxFormat))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(String(showEqualName.prefix(8)) + String(app_momentBoxFormat))].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(String(show_originFormat))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(String(show_originFormat))].stringValue
                    }
                }
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.someRegard()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.appValueDetailColor()
                let unreadColor = UIColor.someRegard()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.photographicEquipment(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func equalMessage(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((String(data_messageMsg.prefix(8)) + user_startPath.replacingOccurrences(of: "error", with: "n"))) {
            //: let dict = NSDictionary.dictionary(withJsonString: draft)
            let dict = NSDictionary.squareOn(withJsonString: draft)
            //: draft = "\(String(describing: dict?["draftContent"]))"
            draft = "\(String(describing: dict?[(String(data_messageMsg.prefix(8)) + user_startPath.replacingOccurrences(of: "error", with: "n"))]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(kIndexName.prefix(3)) + String(mainLineStr.suffix(6))).toAppear(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.tips(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension FaceTableCell {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: LengthThen?, model: TalkingConversationModel, isOther: Bool) {
    private func individualOther(userInfo: LengthThen?, model: CommentReactiveCompatible, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == LineThen.getXiaoMiID() || userInfo?.uid == LineThen.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == LineThen.requestId() || userInfo?.uid == LineThen.imageConnection() {
            //: nickNameLabel.textColor = .appTitleColor()
            nickNameLabel.textColor = .towardOf()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.BundleImageNamed(name: model.faceUrl)
                headImgView.image = UIImage.atomicCount85(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.luculentUpwardly(urlStr: model.faceUrl, placeImg: UIImage.towardLineFrom())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .userVipColor():.appTitleColor()
        nickNameLabel.textColor = userInfo.loungePlus ? .equalColor() : .towardOf()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.luculentUpwardly(urlStr: userInfo.headPic, placeImg: UIImage.putReplacement(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.tingWith(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
        //: imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
        imtimateBtn.setTitle(model.totalIntimate > 99999 ? String(bytes: mainRemoveMsg.map{iconCell(live: $0)}, encoding: .utf8)! : "\(model.totalIntimate)", for: .normal)
        //: imtimateBtn.isHidden = model.totalIntimate <= 0
        imtimateBtn.isHidden = model.totalIntimate <= 0
        //: if model.totalIntimate > 0 {
        if model.totalIntimate > 0 {
            //: let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .pingfangFont(type: .Medium, fontSize: 12)], context: nil)
            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .photographicEquipment(type: .Medium, fontSize: 12)], context: nil)
            //: imtimateBtn.snp.updateConstraints { make in
            imtimateBtn.snp.updateConstraints { make in
                //: make.width.equalTo((size?.width ?? 50) + 20)
                make.width.equalTo((size?.width ?? 50) + 20)
            }
        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(dataPathMessage.prefix(3))).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangMediumFont(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.outView(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func pathBy(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func cameraLog(model: CommentReactiveCompatible) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(appModelMsg) + String(noti_whiteStr.prefix(2))).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.photographicEquipment(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.someRegard(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = FaceTableCell.noSub(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(app_messageId.prefix(5)) + const_screenPath.replacingOccurrences(of: "size", with: "e")).toAppear(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.photographicEquipment(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.someRegard(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(constTableId.prefix(5)) + "ges " + String(show_videoId.prefix(5)) + "een " + main_tapPath.replacingOccurrences(of: "during", with: "r") + "ied " + k_equalFormat.replacingOccurrences(of: "input", with: "re")).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.photographicEquipment(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.appValueDetailColor()
                NSAttributedString.Key.foregroundColor: UIColor.someRegard(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = FaceTableCell.equalMessage(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.pingfangFont(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.photographicEquipment(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension FaceTableCell {
    //: private func createCellUI() {
    private func toUi() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func adjust() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LanguageManager.shared.direction == .rightToLeft {
            if WithLanguageManager.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
