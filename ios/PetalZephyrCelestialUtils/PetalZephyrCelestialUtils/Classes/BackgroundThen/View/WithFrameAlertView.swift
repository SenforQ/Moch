
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showFeatureTitle:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let notiYearId:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let noti_toStr:String = "Submitstring in"

/*: "Please tell us your problems                   " :*/
fileprivate let main_labUrl:[UInt8] = [0x9f,0xa3,0xaa,0xae,0xbc,0xaa,0xef,0xbb,0xaa,0xa3,0xa3,0xef,0xba,0xbc,0xef,0xb6,0xa0,0xba,0xbd,0xef,0xbf,0xbd,0xa0,0xad,0xa3,0xaa,0xa2,0xbc,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef,0xef]

/*: "You need to choose at least one." :*/
fileprivate let showTextSizeName:[UInt8] = [0x45,0x73,0x69,0x3c,0x72,0x79,0x79,0x78,0x3c,0x68,0x73,0x3c,0x7f,0x74,0x73,0x73,0x6f,0x79,0x3c,0x7d,0x68,0x3c,0x70,0x79,0x7d,0x6f,0x68,0x3c,0x73,0x72,0x79,0x32]

private func endUser(player num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "Please tell us your problems." :*/
fileprivate let userSectionName:String = "Please true if left height"
fileprivate let user_entryLogKey:String = "tpathl"
fileprivate let show_dateMessage:String = "tableur"
fileprivate let main_equalMsg:String = "type list image invite pro"

/*: "app/userReport" :*/
fileprivate let user_messageModelFormat:String = "app/ubutton var make equal let"
fileprivate let main_normalId:String = "arrange"

/*: "id" :*/
fileprivate let user_videoName:[UInt8] = [0x1a,0x17]

private func blockEqual(tag num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "reason" :*/
fileprivate let dataDelayKey:String = "reasoimage"

/*: "desc" :*/
fileprivate let mainAtName:[UInt8] = [0x1a,0x1b,0xd,0x1d]

/*: "mf/moment/report" :*/
fileprivate let user_frameworkKey:String = "mf/momein user menu status"
fileprivate let showGiftTitle:String = "particle show self label inputnt/report"

/*: "momentId" :*/
fileprivate let k_makeData:[UInt8] = [0x8e,0x8c,0x8e,0x86,0x8d,0x97,0xaa,0x87]

private func itemCounterval(to num: UInt8) -> UInt8 {
    return num ^ 227
}

/*: "mf/momentReply/report" :*/
fileprivate let user_attachPath:String = "mf/momsize add"
fileprivate let show_labelMsg:String = "ply/requivalent true"
fileprivate let constToolGiftName:String = "finish"

/*: "app/videoReport" :*/
fileprivate let show_logTitle:[Character] = ["a","p","p","/","v","i"]
fileprivate let dataLabPath:String = "deoRetitle make"

/*: "logId" :*/
fileprivate let user_itemMessage:[UInt8] = [0x7e,0x7d,0x75,0x5b,0x76]

private func textEqual(frame num: UInt8) -> UInt8 {
    return num ^ 18
}

/*: "Submitted successfully" :*/
fileprivate let k_succeedFormat:String = "Submiup with back text else"
fileprivate let appListFormat:String = " succlass self touch lab"
fileprivate let noti_atKey:String = "count"

/*: "Nudity" :*/
fileprivate let dataLogFormat:String = "return responseNudity"

/*: "Violence" :*/
fileprivate let user_femaleText:String = "Violeapp for make in reason"
fileprivate let show_targetUrl:String = "NCE"

/*: "Harassment" :*/
fileprivate let show_errorValue:String = "if modelHarass"

/*: "Suicide or Self-injury" :*/
fileprivate let user_areaStr:String = "Suicidmake dismiss self"
fileprivate let showImageModeId:String = "sex cell modelSelf"

/*: "False Information" :*/
fileprivate let data_withPath:[Character] = ["F","a","l","s","e"," ","I","n"]
fileprivate let show_mainFormat:[Character] = ["f","o","r","m","a","t","i","o","n"]

/*: "Spam" :*/
fileprivate let user_titleValue:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let kIconValue:String = "true social self user targetHate"

/*: "Terrorism" :*/
fileprivate let showPlainContent:[Character] = ["T","e","r","r","o","r","i"]
fileprivate let userMessageText:String = "svoice"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let mainTargetNameEquipmentFormat:[UInt8] = [0xf4,0x2,0xd,0x6,0xc1,0x10,0x7,0xc1,0xea,0xd,0xd,0x6,0x8,0x2,0xd,0xc1,0x10,0x13,0xc1,0xf3,0x6,0x8,0x16,0xd,0x2,0x15,0x6,0x5,0xc1,0xe8,0x10,0x10,0x5,0x14]

fileprivate func errorAccept(make num: UInt8) -> UInt8 {
    let value = Int(num) - 161
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let kSelectAssetId:[Character] = ["S","e","n","d","i","n","g"," ","d","i"]
fileprivate let noti_addStr:String = "sgstatusst"
fileprivate let data_sectionMsg:String = "messafatales"

/*: "Using photos of nudity or drugs" :*/
fileprivate let appPushPicLetPath:[UInt8] = [0x1c,0x3a,0x30,0x35,0x2e,0xe7,0x37,0x2f,0x36,0x3b,0x36,0x3a,0xe7,0x36,0x2d,0xe7,0x35,0x3c,0x2b,0x30,0x3b,0x40,0xe7,0x36,0x39,0xe7,0x2b,0x39,0x3c,0x2e,0x3a]

fileprivate func messageTool(current num: UInt8) -> UInt8 {
    let value = Int(num) - 199
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Harassing or sending ads" :*/
fileprivate let constVideoNextTitle:String = "photo"
fileprivate let constCoverFormat:String = "ARAS"
fileprivate let main_menuMessage:String = "data make or "
fileprivate let const_countTitle:String = "title viewg ads"

/*: "Defraud or using fake info" :*/
fileprivate let const_resultStr:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," ","u","s","i","n","g"," ","f","a","k","e"," ","i","n","f"]
fileprivate let k_refuseUrl:[Character] = ["o"]

/*: "Others" :*/
fileprivate let user_hiddenKey:String = "Othersvar equal count cell"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WithFrameAlertView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum ViewAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class WithFrameAlertView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: ViewAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [VideoSocialThen] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = VideoSocialThen()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: ViewAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        spreadDown()
        //: setupSubViewsConstraint()
        colorTitle()
        //: setupReasons()
        total()
        //: bindInteraction()
        exhibit()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showFeatureTitle.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(notiYearId)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.anPathColor(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.photographicEquipment(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.anPathColor().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(writeDown), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(noti_toStr.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.photographicEquipment(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.gradientMore(colors: UIColor.showCorner(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(corner), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: constExplainViewMsg - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.showViewCreate(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = AtViewDelegate(placeholder: String(bytes: main_labUrl.map{$0^207}, encoding: .utf8)!.localized, placeholderColor: UIColor.someRegard(), frame: CGRect(x: 10, y: 10, width: constExplainViewMsg - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .photographicEquipment(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.towardOf()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .photographicEquipment(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.showViewCreate(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(StatuteNameTailCellView.self, forCellReuseIdentifier: StatuteNameTailCellView.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension WithFrameAlertView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func three() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func equalAttribute(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = LineThen.getWindow()
            let window = LineThen.getName()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func less(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func writeDown() {
        //: self.dismissReportView(succeed: false)
        self.less(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func corner() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.addMsg(showMsg: String(bytes: showTextSizeName.map{endUser(player: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.addMsg(showMsg: (String(userSectionName.prefix(7)) + user_entryLogKey.replacingOccurrences(of: "path", with: "el") + " us " + show_dateMessage.replacingOccurrences(of: "table", with: "yo") + String(main_equalMsg.suffix(4)) + "blems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(user_messageModelFormat.prefix(5)) + "serRepor" + main_normalId.replacingOccurrences(of: "arrange", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (dataDelayKey.replacingOccurrences(of: "image", with: "n")): selectedModel.type, String(bytes: mainAtName.map{$0^126}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(user_frameworkKey.prefix(7)) + String(showGiftTitle.suffix(9)))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: k_makeData.map{itemCounterval(to: $0)}, encoding: .utf8)!: tempId, (dataDelayKey.replacingOccurrences(of: "image", with: "n")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(user_attachPath.prefix(6)) + "entRe" + String(show_labelMsg.prefix(5)) + "epor" + constToolGiftName.replacingOccurrences(of: "finish", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (dataDelayKey.replacingOccurrences(of: "image", with: "n")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(show_logTitle) + String(dataLabPath.prefix(5)) + "port")
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: user_itemMessage.map{textEqual(frame: $0)}, encoding: .utf8)!: tempId, (dataDelayKey.replacingOccurrences(of: "image", with: "n")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        ThreadCookieThen.enable(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.beforeDemonstrate(showMsg: (String(k_succeedFormat.prefix(5)) + "tted" + String(appListFormat.prefix(4)) + "cessfull" + noti_atKey.replacingOccurrences(of: "count", with: "y")).localized)
                //: self.dismissReportView(succeed: true)
                self.less(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension WithFrameAlertView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: StatuteNameTailCellView.className()) as! StatuteNameTailCellView
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.modify(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension WithFrameAlertView {
    /// 添加视图
    //: private func setupSubviews() {
    private func spreadDown() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func colorTitle() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func total() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(dataLogFormat.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(user_femaleText.prefix(5)) + show_targetUrl.lowercased()).localized,
                           //: "Harassment".localized,
                           (String(show_errorValue.suffix(6)) + "ment").localized,
                           //: "Suicide or Self-injury".localized,
                           (String(user_areaStr.prefix(6)) + "e or " + String(showImageModeId.suffix(4)) + "-injury").localized,
                           //: "False Information".localized,
                           (String(data_withPath) + String(show_mainFormat)).localized,
                           //: "Spam".localized,
                           (String(user_titleValue)).localized,
                           //: "Hate Speech".localized,
                           (String(kIconValue.suffix(4)) + " Speech").localized,
                           //: "Terrorism".localized,
                           (String(showPlainContent) + userMessageText.replacingOccurrences(of: "voice", with: "m")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: mainTargetNameEquipmentFormat.map{errorAccept(make: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(kSelectAssetId) + noti_addStr.replacingOccurrences(of: "status", with: "u") + "ing " + data_sectionMsg.replacingOccurrences(of: "fatal", with: "g")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: appPushPicLetPath.map{messageTool(current: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (constVideoNextTitle.replacingOccurrences(of: "photo", with: "H") + constCoverFormat.lowercased() + "sing" + String(main_menuMessage.suffix(4)) + "sendin" + String(const_countTitle.suffix(5))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(const_resultStr) + String(k_refuseUrl)).localized,
                           //: "Others".localized]
                           (String(user_hiddenKey.prefix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = NowInfoThen.share.appConfigMode.videoReport["reason"] {
            if let reason = NowInfoThen.share.appConfigMode.videoReport[(dataDelayKey.replacingOccurrences(of: "image", with: "n"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = VideoSocialThen()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func exhibit() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
