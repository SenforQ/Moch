
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userModelPath:[UInt8] = [0x7e,0x79,0x7e,0x63,0x3f,0x74,0x78,0x73,0x72,0x65,0x2d,0x3e,0x37,0x7f,0x76,0x64,0x37,0x79,0x78,0x63,0x37,0x75,0x72,0x72,0x79,0x37,0x7e,0x7a,0x67,0x7b,0x72,0x7a,0x72,0x79,0x63,0x72,0x73]

/*: "Load..." :*/
fileprivate let userCenterMId:[Character] = ["L"]
fileprivate let k_layerFormat:String = "path to valueoad..."

/*: "Skip" :*/
fileprivate let kSizeData:String = "right selected return let addSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let showButtonFormat:[UInt8] = [0x3a,0x6,0x6,0x2,0x26,0x1d,0x1d,0x1e,0x5c,0x3c,0x13,0x6,0x1b,0x4,0x17,0x26,0x1d,0x38,0x1,0x5a,0x55,0x0,0x17,0x11,0x1a,0x13,0x0,0x15,0x17,0x55,0x5b]

private func maleCustom(push num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let mainBlockStyleData:[UInt8] = [0x1d,0x3,0x4,0xe,0x5,0x1d,0x44,0x5,0x4,0x3a,0xb,0xd,0xf,0x39,0x2,0x5,0x1d,0x4c,0x4c,0x5,0x4,0x3a,0xb,0xd,0xf,0x39,0x2,0x5,0x1d,0x42,0x43,0x51]

private func noseMake(app num: UInt8) -> UInt8 {
    return num ^ 106
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let appCenterTitle:String = "jsEvenself super nor"
fileprivate let noti_colorMomentKeyPath:String = "geShow)cookie view class var data"
fileprivate let data_sessionMessagePath:String = ":conversation"

/*: --- :*/
fileprivate let notiItemContent:String = "---"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let user_mLabelData:[UInt8] = [0xa1,0xbf,0xb8,0xb2,0xb9,0xa1,0xf8,0xb9,0xb8,0x86,0xb7,0xb1,0xb3,0x9e,0xbf,0xb2,0xb3,0xf0,0xf0,0xb9,0xb8,0x86,0xb7,0xb1,0xb3,0x9e,0xbf,0xb2,0xb3,0xfe,0xff,0xed]

private func contentEqual(domain num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let showLineAppReturnPath:String = "jsEvenself name"
fileprivate let mainProContent:[Character] = ["a","g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let const_requestName:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let noti_viewKey:String = "canGoBsection field text"
fileprivate let dataBagCommentPrintTitle:String = "playerck"

/*: "PHPSESSID= :*/
fileprivate let userBackgroundTitle:[Character] = ["P","H","P","S","E","S","S"]
fileprivate let appMapKey:[Character] = ["I","D","="]

/*: ";uid= :*/
fileprivate let k_toContent:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let show_mediumContent:[Character] = ["C"]
fileprivate let show_canName:String = "ooparaie"

/*: "nav_back_black_nor" :*/
fileprivate let userUpText:[Character] = ["n","a","v","_","b","a"]
fileprivate let data_listIndexInMsg:[Character] = ["c","k","_","b","l","a"]
fileprivate let noti_infoNameFormat:[Character] = ["c","k","_","n","o","r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let k_countData:String = "image false buttonico_i"
fileprivate let noti_giftMsg:[Character] = ["n","t","i","m","a","t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let data_cornerValue:[UInt8] = [0x15,0x1e,0x12,0x4,0x1c,0x14,0x1f,0x5,0x5f,0x16,0x14,0x5,0x34,0x1d,0x14,0x1c,0x14,0x1f,0x5,0x2,0x33,0x8,0x25,0x10,0x16,0x3f,0x10,0x1c,0x14,0x59,0x56,0x13,0x1e,0x15,0x8,0x56,0x58,0x2a,0x41,0x2c,0x5f,0x2,0x5,0x8,0x1d,0x14,0x5f,0x13,0x10,0x12,0x1a,0x16,0x3,0x1e,0x4,0x1f,0x15,0x4c,0x56,0x3,0x16,0x13,0x10,0x59,0x41,0x5d,0x41,0x5d,0x41,0x5d,0x41,0x58,0x56]

private func mainValue(collection num: UInt8) -> UInt8 {
    return num ^ 113
}

/*: "Alert" :*/
fileprivate let notiListValue:String = "Alertname add"

/*: "OK" :*/
fileprivate let kRequestAppKey:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let const_labelEqualIconValue:String = "frame"
fileprivate let showPositionId:String = "anmodele"

/*: "Done" :*/
fileprivate let dataEdgeValue:String = "if stringDone"

/*: "js call method name =  :*/
fileprivate let app_textDropFormat:String = "category path tempjs ca"
fileprivate let data_actionPath:String = "thod npage touch value fatal view"
fileprivate let app_imageValue:[Character] = ["="," "]

/*: , message =  :*/
fileprivate let constEquipmentData:String = "message object text path, message"
fileprivate let main_managerTitle:[Character] = [" ","="," "]

/*: "http://" :*/
fileprivate let appCollectionArrayText:String = "http://talk data type self let"

/*: "https://" :*/
fileprivate let k_playValue:String = "color"
fileprivate let show_labelPath:[Character] = ["t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let showTempMessage:[UInt8] = [0xa,0x3b,0x29,0x20,0x7d,0x20,0x5d,0x30,0x5b,0x29,0x27,0x3d,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x6e,0x72,0x75,0x74,0x65,0x72,0x20,0x7b,0x20,0x29,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x28,0x70,0x61,0x6d,0x2e,0x29,0x27,0x20,0x3b,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x3d,0x20,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x72,0x61,0x76]

/*: "uid= :*/
fileprivate let constMakeInsertPath:String = "to make if add insertuid="

/*: ;domain= :*/
fileprivate let const_becomeKey:String = ";dorequest no"
fileprivate let show_meValue:String = "main=constraint cost pic"

/*: ;path=/" :*/
fileprivate let mainModeKey:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let showPopName:String = "docummake"
fileprivate let k_sayData:[Character] = ["n","t",".","c","o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let constAppValue:String = "\';\n"

/*: ;domin= :*/
fileprivate let kTopMessage:[Character] = [";","d","o","m","i"]
fileprivate let noti_textData:String = "n=status add true hidden"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let show_hiddenTitle:[UInt8] = [0xda,0xd7,0x91,0x99,0xd4,0xe0,0xe0,0xdc,0xda,0xd6,0xbf,0xd2,0xde,0xd6,0xe4,0x9f,0xda,0xdf,0xd5,0xd6,0xe9,0xc0,0xd7,0x99,0x98,0xc1,0xb9,0xc1,0xc4,0xb6,0xc4,0xc4,0xba,0xb5,0x98,0x9a,0x91,0xae,0xae,0x91,0x9e,0xa2,0x9a,0x91,0xec,0x91,0xd5,0xe0,0xd4,0xe6,0xde,0xd6,0xdf,0xe5,0x9f,0xd4,0xe0,0xe0,0xdc,0xda,0xd6,0xae,0x98]

fileprivate func phoneCount(app num: UInt8) -> UInt8 {
    let value = Int(num) + 143
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: '; };\n" :*/
fileprivate let showToText:String = "\'; };\n"

/*: '" :*/
fileprivate let main_onMsg:String = "\'"

/*: "h5Domain =  :*/
fileprivate let dataCenterRawText:[Character] = ["h","5"]
fileprivate let constStartValue:[Character] = ["D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let k_cancelLabText:[Character] = ["g","e","t","A","l","l","C","o","o","k","i","e","s"]
fileprivate let noti_keyUrl:[Character] = [" ","="," "]

/*: "PHPSESSID" :*/
fileprivate let data_contentName:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let k_tableId:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let k_stopMsg:[Character] = ["a","p","p","C","o","o"]
fileprivate let user_tapRemindMsg:[Character] = ["k","i","e","s"," ","="," "]

/*: "UID" :*/
fileprivate let constAppearZoneStr:String = "needID"

/*: "navigator.userAgent" :*/
fileprivate let constScreenWithViewData:[Character] = ["n","a","v","i","g","a","t","o","r",".","u","s","e"]
fileprivate let userLengthValue:String = "rAgentto photo view back share"

/*: "xunwu_iphone" :*/
fileprivate let data_disappearContent:[Character] = ["x","u"]
fileprivate let k_toSizeActionKey:[Character] = ["n","w","u","_","i","p","h","o","n","e"]

/*: "  :*/
fileprivate let mainShareTitle:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let const_equalUrl:String = " xunwu_layer message progress"
fileprivate let noti_faceText:String = "DEVI"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let notiLayerStr:String = "xunwu_self point sex case to"
fileprivate let mainWillMakeId:String = "backhon"
fileprivate let dataViewButtonStr:[Character] = ["e"," ","x","u","n","w","u","_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let user_convertPath:String = "enter image scale xun"
fileprivate let user_sizeData:String = "ackage/model return var"

/*:  xunwu_bundleId/ :*/
fileprivate let noti_someoneMessage:[Character] = [" ","x","u","n","w","u","_","b","u"]
fileprivate let show_managerPath:[Character] = ["n","d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let constCollectionName:String = "  websend screen video location"
fileprivate let main_addData:String = "kit/1equal weight"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VanguardDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class VanguardDelegate: ModifyThen {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: ViewDetailConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: AddBeautyMirrorPath?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: AddBeautyMirrorPath?, webConfig: ViewDetailConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userModelPath.map{$0^23}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        bottomEqual()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        effectClick()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(userCenterMId) + String(k_layerFormat.suffix(6)))
        //: addNotification()
        infoMap()
        //: func__checkConfig()
        canvassConcluded()
        //: func__setUpWebView()
        drawView()
        //: func__registerObserver()
        goldObserver()
        //: func__beginStartRequest()
        rooms()
        //: func_checkAppleIAP()
        passageAction()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: notiBarStr, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            willImage(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.anPathColor(), .font: UIFont.photographicEquipment(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kSizeData.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension VanguardDelegate {
    /// 添加通知
    //: private func addNotification() {
    private func infoMap() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(clickWith),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: data_videoUseNoStr,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(bottomEqual),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func clickWith() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showButtonFormat.map{maleCustom(push: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func bottomEqual() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: mainBlockStyleData.map{noseMake(app: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(appCenterTitle.prefix(6)) + "t(onPa" + String(noti_colorMomentKeyPath.prefix(7)) + data_sessionMessagePath.replacingOccurrences(of: "conversation", with: " ")) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func effectClick() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: user_mLabelData.map{contentEqual(domain: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(showLineAppReturnPath.prefix(6)) + "t(onP" + String(mainProContent)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension VanguardDelegate {
    //: func func__registerObserver() {
    func goldObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(const_requestName)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(noti_viewKey.prefix(6)) + dataBagCommentPrintTitle.replacingOccurrences(of: "player", with: "a")))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(sizeDistance), name: app_intimateKey, object: nil)
    }

    //: func func__setUpWebView() {
    func drawView() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        totalimateFinish()
        //: func__addBridgeMethod()
        imageStatusFrom()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.objectVideoBurn()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            DeleteReactiveCompatible.withMethod(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(appImageLimitPath)
            }
        }
    }

    //: func func__beginStartRequest() {
    func rooms() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.postSubRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            postSubRequest()
        }
    }

    //: func func__startRequest() {
    func postSubRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = NowInfoThen.share.loginUid
        let uid = NowInfoThen.share.loginUid
        //: let ssid = NowInfoThen.share.loginSessionId
        let ssid = NowInfoThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(userBackgroundTitle) + String(appMapKey)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(k_toContent)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(show_mediumContent) + show_canName.replacingOccurrences(of: "para", with: "k")))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        lucidity()
        //: func__initRightButton()
        access()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func passageAction() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            SendVideoRequestDelegate.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.single(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            SendVideoRequestDelegate.shared.unioniseCreate()
        }
    }

    //: func func__initLeftButton() {
    func arc() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.atomicCount85(name: (String(userUpText) + String(data_listIndexInMsg) + String(noti_infoNameFormat))), style: .plain, target: self, action: #selector(afterAdd(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func access() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(AddBeautyMirrorPath.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.atomicCount85(name: (String(k_countData.suffix(5)) + String(noti_giftMsg))), style: .plain, target: self, action: #selector(totalimatePath))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func afterAdd(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        momentMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.buttonController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: VanguardDelegate.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! VanguardDelegate).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! VanguardDelegate).bottomEqual()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func canvassConcluded() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func hazeOverSucess(sucess: Bool) {
        //: ProgressHUD.dismiss()
        DeleteReactiveCompatible.deflector()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func lucidity() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: data_cornerValue.map{mainValue(collection: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func tillSum(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: TotalReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            TotalReactiveCompatible.share.constraintImage(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func downEqual(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func sizeDistance() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: app_intimateKey, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func totalimatePath() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = ValueDataSource(frame: CGRect(x: 0, y: 0, width: constExplainViewMsg, height: user_deviceId))
        //: view.show()
        view.behindButton()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension VanguardDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            DeleteReactiveCompatible.withMethod(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            hazeOverSucess(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        arc()
        //: func__clearJSBgColor()
        lucidity()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            hazeOverSucess(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            hazeOverSucess(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(notiListValue.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (const_labelEqualIconValue.replacingOccurrences(of: "frame", with: "C") + showPositionId.replacingOccurrences(of: "model", with: "cl")), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(dataEdgeValue.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension VanguardDelegate: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(app_textDropFormat.suffix(5)) + "ll me" + String(data_actionPath.prefix(6)) + "ame " + String(app_imageValue)) + "\(message.name)" + (String(constEquipmentData.suffix(9)) + String(main_managerTitle)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.shirtButton(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func imageStatusFrom() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ConstraintScalarLiteral.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func momentMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ConstraintScalarLiteral.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension VanguardDelegate {
    //: func func__addUserScript() {
    func resumeText() {
        //: var htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(appCollectionArrayText.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(appCollectionArrayText.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((k_playValue.replacingOccurrences(of: "color", with: "h") + String(show_labelPath))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (k_playValue.replacingOccurrences(of: "color", with: "h") + String(show_labelPath)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = NowInfoThen.share.loginUserMode.userID
        let tempUid: String = NowInfoThen.share.loginUserMode.userID
        //: let tempSessionID: String = NowInfoThen.share.loginSessionId
        let tempSessionID: String = NowInfoThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: showTempMessage.reversed(), encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(constMakeInsertPath.suffix(4))) + "\(tempUid)" + (String(const_becomeKey.prefix(3)) + String(show_meValue.prefix(5))) + "\(htmlDomain)" + (String(mainModeKey))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((showPopName.replacingOccurrences(of: "make", with: "e") + String(k_sayData)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(userBackgroundTitle) + String(appMapKey)) + "\(tempSessionID)" + (String(kTopMessage) + String(noti_textData.prefix(2))) + "\(htmlDomain)" + (String(mainModeKey))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((showPopName.replacingOccurrences(of: "make", with: "e") + String(k_sayData)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: show_hiddenTitle.map{phoneCount(app: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(constMakeInsertPath.suffix(4))) + "\(tempUid)" + (String(const_becomeKey.prefix(3)) + String(show_meValue.prefix(5))) + "\(htmlDomain)" + (String(mainModeKey))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((showPopName.replacingOccurrences(of: "make", with: "e") + String(k_sayData)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(constMakeInsertPath.suffix(4))) + "\(tempSessionID)" + (String(const_becomeKey.prefix(3)) + String(show_meValue.prefix(5))) + "\(htmlDomain)" + (String(mainModeKey))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((showPopName.replacingOccurrences(of: "make", with: "e") + String(k_sayData)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func willImage(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(dataCenterRawText) + String(constStartValue)) + "\(htmlDomain)")
            //: let userID: String = NowInfoThen.share.loginUserMode.userID
            let userID: String = NowInfoThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(k_cancelLabText) + String(noti_keyUrl)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(data_contentName)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(k_tableId)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.rooms()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.upFormat(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func upFormat(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(dataCenterRawText) + String(constStartValue)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: OfDetailAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(k_stopMsg) + String(user_tapRemindMsg)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (constAppearZoneStr.replacingOccurrences(of: "need", with: "U")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(k_tableId))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(data_contentName)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(data_contentName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.rooms()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func totalimateFinish() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(constScreenWithViewData) + String(userLengthValue.prefix(6)))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(constScreenWithViewData) + String(userLengthValue.prefix(6))))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(data_disappearContent) + String(k_toSizeActionKey))) {
                //: let str = getUserAgentString()
                let str = playerSnapLine()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.timeVideo { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(const_equalUrl.prefix(7)) + noti_faceText.lowercased() + "ce_id/") + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func playerSnapLine() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(notiLayerStr.prefix(6)) + mainWillMakeId.replacingOccurrences(of: "back", with: "ip") + String(dataViewButtonStr)) + "\(appFileMsg)" + (String(user_convertPath.suffix(4)) + "wu_p" + String(user_sizeData.prefix(7))) + "\(main_maxTitle)" + (String(noti_someoneMessage) + String(show_managerPath)) + "\(kBottomStr)" + (String(constCollectionName.prefix(5)) + String(main_addData.prefix(5)))
    }
}
