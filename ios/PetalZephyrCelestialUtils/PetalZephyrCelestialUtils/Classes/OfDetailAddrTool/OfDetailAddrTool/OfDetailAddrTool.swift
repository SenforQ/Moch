
//: Declare String Begin

/*: "http://" :*/
fileprivate let dataMoreMessage:String = "http:data m stream touch"
fileprivate let show_intimateValue:String = "faceface"

/*: "https://" :*/
fileprivate let noti_priceId:String = "https://transform to to"

/*: "http:// :*/
fileprivate let user_toKey:String = "hmain"
fileprivate let show_titleId:String = "forward fortp://"

/*: "serverSelctIndex =  :*/
fileprivate let appMainName:String = "namerve"
fileprivate let showToUrl:String = "rSelcview var color"
fileprivate let user_normalFormat:String = "icon extension number shareex = "

/*: "serverUrlStr =  :*/
fileprivate let app_offPath:String = "sercentere"
fileprivate let notiBottomData:String = "tab view text menu actiontr = "

/*: "app. :*/
fileprivate let app_countValue:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let userMakeValue:String = "type last view tab.com"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let main_viewMessage:String = "mf/chavalue return"
fileprivate let dataOffFormat:String = "layer leadingm/se"
fileprivate let const_sourceIndexValue:String = "ndGiftpath row model"

/*: "recharge/applePayNotify" :*/
fileprivate let userStartImageFormat:String = "reca"
fileprivate let constModelFormat:[Character] = ["/","a"]
fileprivate let data_transitionPath:String = "pprelease"

/*: "user/appleLogin" :*/
fileprivate let user_effectTitle:[Character] = ["u","s","e","r","/","a","p","p","l","e","L","o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let app_equalText:[Character] = ["I","M","/","g","e","t","M","y","B","l","a","c","k","L","i"]
fileprivate let mainToolMsg:[Character] = ["s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let kStatusStr:String = "mf/bumake type make let"
fileprivate let showIndexStageValue:String = "siness/time item"
fileprivate let userAfterContent:[Character] = ["s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let app_makeMessage:String = "mf/bwill model user"
fileprivate let const_careFileId:[Character] = ["e","s","s","/"]
fileprivate let constViewId:[Character] = ["s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let constSizeName:String = "mf/chaplayer pop"
fileprivate let show_constraintHiddenStr:String = "/sendview color var"
fileprivate let k_blockText:String = "Msgicon to to path collection"

/*: "en" :*/
fileprivate let user_upValue:String = "eimage"

/*: "ar" :*/
fileprivate let constLiveText:String = "atarget"

/*: "es" :*/
fileprivate let mainCellName:String = "efile"

/*: "pt" :*/
fileprivate let const_frameMsg:String = "ptable"

/*: "GJ21FB" :*/
fileprivate let kSequenceTitle:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let k_yearData:String = "完成\u{4e86}检测"
fileprivate let appToItemFilterName:[Character] = ["\u{5e38}"]

/*: "接口请求失败" :*/
fileprivate let constValueKey:String = "接口请\u{6c42}失\u{8d25}"

/*: "完成了检测,需要更换server url" :*/
fileprivate let appInsideMsg:String = "完成了检测,"
fileprivate let data_sharedId:[Character] = ["s","e","r","v","e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let kColorStr:[Character] = ["H","T","T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let data_marginMsg:[Character] = ["_","_","S","C","O","P","E","D"]
fileprivate let show_modelId:String = "__"

/*: "tap" :*/
fileprivate let dataMakeStatusModelStr:String = "TAP"

/*: "tun" :*/
fileprivate let mainCellErrorStatusMessage:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let appImageColorMsg:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let notiMakeMinData:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let notiVideoFormat:String = "func_last color mode equal"
fileprivate let constWithFormat:String = "colorful male pathateTa"
fileprivate let show_containerUrl:String = "ktext"
fileprivate let const_videoValue:String = "es = user block square label data"

/*: "https:// :*/
fileprivate let constButtonStr:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let show_valuePath:[UInt8] = [0x59,0x68,0x61,0x56,0x52,0x52,0x57,0x58,0x5f,0x58,0x67,0x58,0x46,0x54,0x60,0x58,0x36,0x62,0x62,0x5e,0x5c,0x58,0x66,0x4a,0x5c,0x67,0x5b,0x48,0x65,0x5f,0x13,0x30,0x13]

fileprivate func managerMake(background num: UInt8) -> UInt8 {
    let value = Int(num) + 13
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let show_userKey:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let notiErrorId:[Character] = ["P","H","P"]
fileprivate let kSumeractionContent:[Character] = ["S","E","S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let show_endFormat:String = "target selffunc__"
fileprivate let k_scaleGirlMsg:String = "view labAllCo"
fileprivate let app_arrayData:[Character] = ["="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let data_loadStr:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i","e","s"," "]
fileprivate let main_layerUrl:String = "true white manager make close error"

/*: "🍪 删除 :*/
fileprivate let showFormatId:[Character] = ["\u{0001f36a}"," ","\u{5220}","除"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OfDetailAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum MusicScalarLiteral: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class OfDetailAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = OfDetailAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        before()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/"))) || newValue.hasPrefix((String(noti_priceId.prefix(8)))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if main_closedTitle && !dataAgeVideoMessage {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (user_toKey.replacingOccurrences(of: "main", with: "t") + String(show_titleId.suffix(5))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (user_toKey.replacingOccurrences(of: "main", with: "t") + String(show_titleId.suffix(5))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    modeCookies(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func before() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = main_clickSenseKey.integer(forKey: app_failCheckSenseKey)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (appMainName.replacingOccurrences(of: "name", with: "se") + String(showToUrl.prefix(5)) + "tInd" + String(user_normalFormat.suffix(5))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = accumulationUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/"))
        //: if Environment_Formal && !Encryption_Request {
        if main_closedTitle && !dataAgeVideoMessage {
            //: serverPrefix = "http://"
            serverPrefix = (String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/"))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (app_offPath.replacingOccurrences(of: "center", with: "v") + "rUrlS" + String(notiBottomData.suffix(5))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func accumulationUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func assemblageStr(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(app_countValue)) + "\(main_bottomStr)" + (String(userMakeValue.suffix(4))), (String(app_countValue)) + "\(main_bottomStr)" + (String(userMakeValue.suffix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(main_viewMessage.prefix(6)) + "tRoo" + String(dataOffFormat.suffix(4)) + String(const_sourceIndexValue.prefix(6))),
         //: "recharge/applePayNotify",
         (userStartImageFormat.replacingOccurrences(of: "a", with: "h") + "arge" + String(constModelFormat) + data_transitionPath.replacingOccurrences(of: "release", with: "le") + "PayNotify"),
         //: "user/appleLogin"] + slowPathArr1
         (String(user_effectTitle))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(app_equalText) + String(mainToolMsg)),
         //: "mf/business/sendMsg",
         (String(kStatusStr.prefix(5)) + String(showIndexStageValue.prefix(7)) + String(userAfterContent)),
         //: "mf/business/sendGift",
         (String(app_makeMessage.prefix(4)) + "usin" + String(const_careFileId) + String(constViewId)),
         //: "mf/chatRoom/sendMsg"]
         (String(constSizeName.prefix(6)) + "tRoom" + String(show_constraintHiddenStr.prefix(5)) + String(k_blockText.prefix(3)))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = LineThen.getSystemLangCode()
        let lang = LineThen.can()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .randomLength(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(kSequenceTitle)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension OfDetailAddrTool {
    //: func checkServerUrl() {
    func loadEnable() {
        //: if NowInfoThen.share.networkStatus != .Unavailable {
        if NowInfoThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            computerFileContentChange()
            //: } else {
        } else {
            //: func_starCodeTime()
            nearCell()
        }
    }

    //: func func_starCodeTime() {
    func nearCell() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.computerFileContentChange()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func overEvent() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func computerFileContentChange() {
        //: if NowInfoThen.share.networkStatus == .Unavailable {
        if NowInfoThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        overEvent()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ScreenIngatheringManagerRequest.sendReceive { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (k_yearData + ",请求状态正" + String(appToItemFilterName)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (constValueKey))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.nearCell()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (appInsideMsg.capitalized + "需要更换" + String(data_sharedId)))
                    //: self.func__changeHostUrl()
                    self.playerRequest()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func playerRequest() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        overEvent()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        main_clickSenseKey.set(serverSelctIndex, forKey: app_failCheckSenseKey)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/"))
        //: if Environment_Formal && !Encryption_Request {
        if main_closedTitle, !dataAgeVideoMessage {
            //: serverPrefix = "http://"
            serverPrefix = (String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/"))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension OfDetailAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func utilisedRequest() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(kColorStr))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func withPushStart() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(data_marginMsg) + show_modelId.capitalized)] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((dataMakeStatusModelStr.lowercased())) || keyStr.contains((String(mainCellErrorStatusMessage))) || keyStr.contains((String(appImageColorMsg))) || keyStr.contains((String(notiMakeMinData))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension OfDetailAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func modeCookies(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(notiVideoFormat.prefix(5)) + "_upd" + String(constWithFormat.suffix(5)) + "lkingCoo" + show_containerUrl.replacingOccurrences(of: "text", with: "i") + String(const_videoValue.prefix(5))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = errorByCapacity()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/")), with: (String(noti_priceId.prefix(8))))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(noti_priceId.prefix(8)))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(constButtonStr)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            underside(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        every()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func underside(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: show_valuePath.map{managerMake(background: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func errorByCapacity() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dataMoreMessage.prefix(5)) + show_intimateValue.replacingOccurrences(of: "face", with: "/"))
        //: if Environment_Formal && !Encryption_Request {
        if main_closedTitle, !dataAgeVideoMessage {
            //: serverPrefix = "https://"
            serverPrefix = (String(noti_priceId.prefix(8)))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(app_countValue)) + "\(main_bottomStr)" + (String(userMakeValue.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func barModel(note _: Notification) {
        //: if NowInfoThen.share.loginUid.isEmptyString {
        if NowInfoThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            packageAcross()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            viewerAt()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func packageAcross() {
        //: var htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(show_userKey)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(show_userKey))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(notiErrorId) + String(kSumeractionContent)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(notiErrorId) + String(kSumeractionContent))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(show_userKey)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(show_userKey))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(notiErrorId) + String(kSumeractionContent)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(notiErrorId) + String(kSumeractionContent))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(show_userKey))), h5Cookie.keys.contains((String(show_userKey))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(show_userKey))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(show_userKey))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(notiErrorId) + String(kSumeractionContent))), h5Cookie.keys.contains((String(notiErrorId) + String(kSumeractionContent))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(notiErrorId) + String(kSumeractionContent))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(notiErrorId) + String(kSumeractionContent))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func viewerAt() {
        //: let htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = NowInfoThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == NowInfoThen.share.loginUserMode.userID {
            if cookie.name == (String(show_userKey)), cookie.value == NowInfoThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(notiErrorId) + String(kSumeractionContent)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(show_userKey)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(show_userKey))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(notiErrorId) + String(kSumeractionContent)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(notiErrorId) + String(kSumeractionContent))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(show_userKey))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(show_userKey))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(notiErrorId) + String(kSumeractionContent))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(notiErrorId) + String(kSumeractionContent))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func every() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(show_endFormat.suffix(6)) + "save" + String(k_scaleGirlMsg.suffix(5)) + "okies " + String(app_arrayData)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            main_clickSenseKey.set(cookieData, forKey: data_senseMsg)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(data_loadStr) + String(main_layerUrl.suffix(6))))
        }
    }

    //: func func__setAppCookies() {
    func rubricCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = main_clickSenseKey.object(forKey: data_senseMsg)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func colorAt() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        main_clickSenseKey.removeObject(forKey: data_senseMsg)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
