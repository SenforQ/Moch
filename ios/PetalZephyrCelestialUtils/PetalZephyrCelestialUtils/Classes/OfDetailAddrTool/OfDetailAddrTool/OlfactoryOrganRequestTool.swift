
//: Declare String Begin

/*: / :*/
fileprivate let show_lineTitle:[Character] = ["/"]

/*:  ( :*/
fileprivate let main_dataName:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let mainAtName:String = "make index; build:"

/*: ; iOS  :*/
fileprivate let mainPartyStr:String = "; iOStarget aspect style"
fileprivate let user_firstMsg:String = " "

/*: ;  :*/
fileprivate let userLabelMessage:String = "; "

/*: )" :*/
fileprivate let main_viewData:String = "domain"

/*: "audio" :*/
fileprivate let noti_filterAtId:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let userSaveIconMessage:String = "+"

/*: .wav" :*/
fileprivate let show_labelFormat:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let data_regularMsg:String = "auuser"
fileprivate let const_topMessage:String = "io/wavtype self image"

/*: .jpg" :*/
fileprivate let main_bottomKey:String = "whole video height in succeed.jpg"

/*: "image/jpeg" :*/
fileprivate let mainAddBlackMsg:[Character] = ["i","m","a","g","e","/","j","p"]
fileprivate let main_carrierValue:[Character] = ["e","g"]

/*: "://app." :*/
fileprivate let userCheckedTaskFalseTitle:String = "text self://app."

/*: "data" :*/
fileprivate let user_nowDataUrl:String = "DATA"

/*: ":null" :*/
fileprivate let dataQueryViewTitle:String = ":nullarray hidden pic"

/*: ":{}" :*/
fileprivate let k_objectValue:String = ":{}"

/*: "Request failed, Try again later" :*/
fileprivate let noti_sharedFormat:[UInt8] = [0x22,0x15,0x1,0x5,0x15,0x3,0x4,0x50,0x16,0x11,0x19,0x1c,0x15,0x14,0x5c,0x50,0x24,0x2,0x9,0x50,0x11,0x17,0x11,0x19,0x1e,0x50,0x1c,0x11,0x4,0x15,0x2]

/*: "json error" :*/
fileprivate let showSearchedKey:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let showNameStr:[Character] = ["p","l","a","t","f"]
fileprivate let userIconToValue:String = "range equal m insert toorm="
fileprivate let const_titleName:String = "IPHON"
fileprivate let userNoseKey:String = "rsion=extension time wrap version route"

/*: &packageId= :*/
fileprivate let kModeEqualFalseValue:[Character] = ["&","p","a","c","k","a"]
fileprivate let dataThumbPathTitle:String = "demonstrate data corner at togeId="

/*: &bundleId= :*/
fileprivate let appFatalValue:String = "info let lab&bund"

/*: &lang= :*/
fileprivate let dataMinMsg:String = "lab index adjust within&lang="

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let k_rankPlayPath:String = "?!@wrap$"
fileprivate let user_quitMessage:String = ";=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let const_netColorStr:String = "`<>()[]"
fileprivate let const_toUrl:String = "{}/\\| "

/*: "/route/slowindex" :*/
fileprivate let userThenEqualPath:[Character] = ["/","r","o","u","t"]
fileprivate let dataAtContent:String = "data table launch add statuse/sl"
fileprivate let const_sourcePath:String = "owinmessagex"

/*: "/route/index" :*/
fileprivate let userSearchedPath:String = "/route/import collect photo view awake"
fileprivate let data_popMsg:String = "imanagerex"

/*: "?path= :*/
fileprivate let app_layerMsg:[Character] = ["?","p","a","t","h","="]

/*: "&GJNonceStr= :*/
fileprivate let app_equalName:String = "index to&GJN"
fileprivate let userRegardData:String = "push giftStr="

/*: "& :*/
fileprivate let dataTempMessage:[Character] = ["&"]

/*: = :*/
fileprivate let kMakeTitle:[Character] = ["="]

/*: "&params= :*/
fileprivate let user_textName:String = "up array count self image&pa"
fileprivate let k_styleValue:[Character] = ["r","a","m","s","="]

/*: "/ :*/
fileprivate let kPicPath:String = "card"

/*: "? :*/
fileprivate let const_frameStr:String = "?"

/*: "token" :*/
fileprivate let k_sizeLogId:String = "toketable"

/*: "请求成功 :*/
fileprivate let userSendText:String = "请求\u{6210}功"

/*: "AAAAAAAA" :*/
fileprivate let mainEqualData:String = "pushpush"

/*: "AA" :*/
fileprivate let userPlayLabName:String = "error"

/*: "BB" :*/
fileprivate let const_componentId:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let app_cornerTalkMsg:String = "下"
fileprivate let constBackgroundStr:[Character] = ["\u{8f7d}","\u{5b8c}","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let show_tableRequestStr:String = "/Docueffect path event of view"

/*: ".mp4" :*/
fileprivate let userAwakePath:String = ".mp4cell level true"

/*: "移动地址: :*/
fileprivate let appBeyondName:String = "移动地\u{5740}:"

/*: "后台任务下载回来" :*/
fileprivate let appCustomUrl:String = "后台任务temp"
fileprivate let dataTargetText:String = "载回来"

/*: "下载成功 : :*/
fileprivate let appMainFormat:[Character] = ["下","载","\u{6210}","功"," ",":"]

/*: "Net Error" :*/
fileprivate let app_labelLeadingValue:String = "Net result float image near"

/*: "下载失败 : :*/
fileprivate let appUserName:String = "\u{4e0b}载失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OlfactoryOrganRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let noti_screenValue = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let mainLiveName = "\(k_noMessage)/\(notiErrTurnUrl) (\(kBottomStr)" + (String(mainAtName.suffix(8))) + "\(mainFailFormat)" + (String(mainPartyStr.prefix(5)) + user_firstMsg.capitalized) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: WhiteErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let dataVoiceKey = OlfactoryOrganRequestTool()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class OlfactoryOrganRequestTool: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func titleCompletion(model: FrameRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.currentNameObject() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.executeResume(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.itemModel(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func executeResume(model: FrameRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.constraintModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.inmate(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    sequence(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: mainTurnMsg))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        addMsg(showMsg: mainTurnMsg)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(noti_filterAtId)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.stamp())+\(main_maxTitle)" + (String(show_labelFormat)), mimeType: (data_regularMsg.replacingOccurrences(of: "user", with: "d") + String(const_topMessage.prefix(6))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(main_bottomKey.suffix(4))), mimeType: (String(mainAddBlackMsg) + String(main_carrierValue)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    sequence(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: mainTurnMsg))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        addMsg(showMsg: mainTurnMsg)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func itemModel(model: FrameRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.constraintModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.inmate(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: dataAgeVideoMessage ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = noti_screenValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    sequence(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: mainTurnMsg))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        addMsg(showMsg: mainTurnMsg)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = noti_screenValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    sequence(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: mainTurnMsg))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        addMsg(showMsg: mainTurnMsg)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func sequence(model: FrameRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            OfDetailAddrTool.share.modeCookies(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if dataAgeVideoMessage, model.requestServer.contains((String(userCheckedTaskFalseTitle.suffix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.messageKey(key: OfDetailAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (user_nowDataUrl.lowercased()) + "\"" + (String(dataQueryViewTitle.prefix(5))), with: "" + "\"" + (user_nowDataUrl.lowercased()) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !dataAgeVideoMessage {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(dataQueryViewTitle.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<CellHandyJSON>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == HeritageSendable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, WhiteErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    addMsg(showMsg: responseModel.msg ?? String(bytes: noti_sharedFormat.map{$0^112}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case HeritageSendable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: mainAddTitle, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: (String(showSearchedKey)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func constraintModel(model: FrameRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(showNameStr) + String(userIconToValue.suffix(4)) + const_titleName.lowercased() + "e&ve" + String(userNoseKey.prefix(6))) + "\(appFileMsg)" + (String(kModeEqualFalseValue) + String(dataThumbPathTitle.suffix(5))) + "\(main_maxTitle)" + (String(appFatalValue.suffix(5)) + "leId=") + "\(kBottomStr)" + (String(dataMinMsg.suffix(6))) + "\(OfDetailAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if dataAgeVideoMessage, serverUrl.contains((String(userCheckedTaskFalseTitle.suffix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if OfDetailAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(userThenEqualPath) + String(dataAtContent.suffix(4)) + const_sourcePath.replacingOccurrences(of: "message", with: "de")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(userSearchedPath.prefix(7)) + data_popMsg.replacingOccurrences(of: "manager", with: "nd"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(app_layerMsg)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(app_equalName.suffix(4)) + "once" + String(userRegardData.suffix(4))) + "\(OfDetailAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.popularKey(key: OfDetailAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(user_textName.suffix(3)) + String(k_styleValue)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func inmate(model: FrameRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(mainLiveName)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (k_sizeLogId.replacingOccurrences(of: "table", with: "n")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func subTarget(model: FrameRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.constraintModel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (userSendText) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (mainEqualData.replacingOccurrences(of: "push", with: "AA") + "AAAA")) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (app_cornerTalkMsg.capitalized + String(constBackgroundStr)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(show_tableRequestStr.prefix(5)) + "ments/") + NSDate.stamp() + (String(userAwakePath.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (appBeyondName) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (appCustomUrl.replacingOccurrences(of: "temp", with: "下") + dataTargetText.capitalized))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = CellThen.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func product(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: dataSessionUrl.increase(), with: main_bottomStr)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(appMainFormat)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, WhiteErrorResponse(errorCode: HeritageSendable.Normal.rawValue, errorMsg: (String(app_labelLeadingValue.prefix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: (String(app_labelLeadingValue.prefix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (appUserName) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func pathRemote(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: dataSessionUrl.increase(), with: main_bottomStr)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(appMainFormat)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, WhiteErrorResponse(errorCode: HeritageSendable.Normal.rawValue, errorMsg: (String(app_labelLeadingValue.prefix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, WhiteErrorResponse(errorCode: HeritageSendable.NetError.rawValue, errorMsg: (String(app_labelLeadingValue.prefix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (appUserName) + "\(errMsg)")
            }
        }
    }
}
