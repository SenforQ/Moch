
//: Declare String Begin

/*: "cmd" :*/
fileprivate let app_ofKey:[UInt8] = [0x93,0x9d,0x94]

private func dataButton(content num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "cancelCall" :*/
fileprivate let user_equipmentTitle:String = "camodelel"

/*: "data" :*/
fileprivate let k_backgroundName:[UInt8] = [0x1b,0x18,0x2b,0x18]

fileprivate func modelView(manager num: UInt8) -> UInt8 {
    let value = Int(num) + 73
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let userContentTitle:String = "result tologId"

/*: "#322870" :*/
fileprivate let k_playFormat:[Character] = ["#","3","2","2","8","7"]
fileprivate let kLastContent:String = "0"

/*: "Tap to start video chatting" :*/
fileprivate let noti_statusUrl:String = "if return data videoTap to"
fileprivate let data_addMsg:String = "t videclick user"
fileprivate let appModelMessage:[Character] = ["o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let app_femaleContentKey:[Character] = ["i","c","o","n","_"]
fileprivate let dataTextUrl:String = "cloby"

/*: "icon_views_number" :*/
fileprivate let k_enableValue:String = "ICON"
fileprivate let notiViewStr:String = "ws_nstring location let video"
fileprivate let mainTopMsg:String = "umbnormalr"

/*: "%@ chatting" :*/
fileprivate let dataNeedFatalId:String = "%@ cacross tab false color"
fileprivate let userSourceStr:[Character] = ["h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let notiConstraintData:String = "the cornericon_"
fileprivate let userChannelMsg:String = "lounpath"

/*: "icon_rank_coin" :*/
fileprivate let noti_viewKey:[Character] = ["i","c","o","n","_","r","a","n","k","_","c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let dataRawTitle:String = "case count selfvip_"

/*: "btn_nearby_settings_nor" :*/
fileprivate let noti_nameValue:String = "maintn"
fileprivate let dataMomentName:String = "_nearuser status color frame print"
fileprivate let kToMinPathMessage:String = "ttnormalg"

/*: "%@ Coins/min" :*/
fileprivate let userTheMessage:[Character] = ["%","@"," "]
fileprivate let show_userPlayerMsg:String = "Coincenter image self"

/*: "get json error" :*/
fileprivate let userManagerName:String = "get japp return"
fileprivate let kUserMoreTitle:String = "eequalor"

/*: "matchId" :*/
fileprivate let user_editItemMessage:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let showFormalColorText:[UInt8] = [0x7b,0x88,0x88,0x84,0x85]

fileprivate func viewDo(re num: UInt8) -> UInt8 {
    let value = Int(num) + 234
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let const_makeEndKey:String = "name in touch case varVIPFee"

/*: "Matching now" :*/
fileprivate let data_attributePlayerMessage:String = "Matchimixture var be"

/*: "userTouch" :*/
fileprivate let k_infoPath:String = "userTouclength touch data self"
fileprivate let dataLevelTitle:[Character] = ["h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let show_dataKey:String = "mf/frview back data array date"
fileprivate let main_actionUrl:String = "make make security valuell/get"
fileprivate let data_modelEqualFormatMessage:String = "VideoInfoview false"

/*: "fromType" :*/
fileprivate let userTurnBottomData:[UInt8] = [0x88,0x9c,0x81,0x83,0xba,0x97,0x9e,0x8b]

private func fromVideo(mode num: UInt8) -> UInt8 {
    return num ^ 238
}

/*: "You've been barred from receiving calls" :*/
fileprivate let show_colorUrl:[UInt8] = [0x7b,0x91,0x97,0x49,0x98,0x87,0x42,0x84,0x87,0x87,0x90,0x42,0x84,0x83,0x94,0x94,0x87,0x86,0x42,0x88,0x94,0x91,0x8f,0x42,0x94,0x87,0x85,0x87,0x8b,0x98,0x8b,0x90,0x89,0x42,0x85,0x83,0x8e,0x8e,0x95]

fileprivate func userView(component num: UInt8) -> UInt8 {
    let value = Int(num) - 34
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let showTagData:[UInt8] = [0xbf,0xbb,0xc1,0xbe,0xaf,0xb1]

fileprivate func randomOn(leading num: UInt8) -> UInt8 {
    let value = Int(num) - 76
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let app_pathTopMessage:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let showMakeLocationText:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let mainLeadingKey:[Character] = ["f","r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let noti_viewName:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let notiFrameContent:String = "onViddata size"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let show_mName:[UInt8] = [0x69,0x73,0x47,0x46,0x5d,0x5f,0x53,0x46,0x5b,0x51,0x12,0x44,0x5b,0x56,0x57,0x5d,0x6f,0x12,0x60,0x57,0x51,0x57,0x5b,0x44,0x57,0x12,0x44,0x5b,0x56,0x57,0x5d,0x12,0x51,0x53,0x5e,0x5e,0x12,0x5b,0x5c,0x51,0x5d,0x5f,0x5b,0x5c,0x55]

/*: "refuseCall" :*/
fileprivate let app_emptyMessage:String = "imageefuse"

/*: "onCancelCall" :*/
fileprivate let k_giftTitle:String = "self center effect addonCa"
fileprivate let appChangeLessMsg:String = "label"

/*: "retryAfter" :*/
fileprivate let app_marginId:[Character] = ["r","e","t","r","y","A","f","t"]
fileprivate let kSightPath:[Character] = ["e","r"]

/*: "onRequestCall" :*/
fileprivate let mainDataMsg:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let main_shareAtPath:[Character] = ["f","a","i","l","T","y"]
fileprivate let appStatusFormat:[Character] = ["p","e"]

/*: "callWaitingTime" :*/
fileprivate let k_phoneValue:[Character] = ["c","a","l","l","W"]
fileprivate let show_arrayFormat:[Character] = ["a"]
fileprivate let constRawUrl:String = "itingTimeclass let table"

/*: "toUid" :*/
fileprivate let noti_failContent:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let noti_dataKey:String = "add user equal action typeFree"

/*: "%@/min" :*/
fileprivate let mainMenuMakeMessage:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MusicViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class MusicViewController: TailRecognizerDelegate {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.makeCost()
            }
        }
        //: self.resetCamera()
        self.equalName()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        beOf()
        //: setupSubViewsConstraint()
        views()
        //: bindInteraction()
        ignore()
        //: videoManager.startPreview(isMatch: true)
        videoManager.cause(isMatch: true)
        //: setPlayer(isRandon: false)
        thumb(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            rectangle()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: app_ofKey.map{dataButton(content: $0)}, encoding: .utf8)!: (user_equipmentTitle.replacingOccurrences(of: "model", with: "nc") + "Call"), String(bytes: k_backgroundName.map{modelView(manager: $0)}, encoding: .utf8)!: [(String(userContentTitle.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        valid()
        //: if TalkingSocketManager.shared.isTarget == false {
        if BankManagerSocketDelegate.shared.isTarget == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            BankManagerSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            BankManagerSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            BankManagerSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            BankManagerSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: TextThen = {
        //: let m = TalkingVideoChatManager()
        let m = TextThen()
        //: let info = TalkingVideoChatModel()
        let info = NameChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .towardOf()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(rectangle), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: constExplainViewMsg + 1, height: user_deviceId + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(k_playFormat) + kLastContent.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(noti_statusUrl.suffix(6)) + " star" + String(data_addMsg.prefix(6)) + String(appModelMessage)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.atomicCount85(name: (String(app_femaleContentKey) + dataTextUrl.replacingOccurrences(of: "by", with: "s") + "e_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(buttonClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: BlockButton = {
        //: let btn = TalkingButton()
        let btn = BlockButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.atomicCount85(name: (k_enableValue.lowercased() + "_vie" + String(notiViewStr.prefix(4)) + mainTopMsg.replacingOccurrences(of: "normal", with: "e"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.atomicCount85(name: (k_enableValue.lowercased() + "_vie" + String(notiViewStr.prefix(4)) + mainTopMsg.replacingOccurrences(of: "normal", with: "e"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.photographicEquipment(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(dataNeedFatalId.prefix(4)) + String(userSourceStr)).toAppear(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.atomicCount85(name: (String(notiConstraintData.suffix(5)) + userChannelMsg.replacingOccurrences(of: "path", with: "ge")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.atomicCount85(name: (String(noti_viewKey)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.photographicEquipment(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.atomicCount85(name: (String(dataRawTitle.suffix(4)) + "btn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: BlockButton = {
        //: let btn = TalkingButton()
        let btn = BlockButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(accenting), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.atomicCount85(name: (noti_nameValue.replacingOccurrences(of: "main", with: "b") + String(dataMomentName.prefix(5)) + "by_se" + kToMinPathMessage.replacingOccurrences(of: "normal", with: "in") + "s_nor")), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.atomicCount85(name: (String(noti_viewKey)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .equalColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(userTheMessage) + String(show_userPlayerMsg.prefix(4)) + "s/min").toAppear(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.photographicEquipment(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: CharacteristicRootOfASquareMatrixView = //: return CharacteristicRootOfASquareMatrixView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension MusicViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func thumb(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ProduceActionReactiveCompatible.default.distaffType(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(userManagerName.prefix(5)) + "son " + kUserMoreTitle.replacingOccurrences(of: "equal", with: "rr")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func transaction(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(user_editItemMessage)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: app_ofKey.map{dataButton(content: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: showFormalColorText.map{viewDo(re: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        DistrictManagerThen.thenCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.actionRank()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(user_editItemMessage))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func allRoute() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        DistrictManagerThen.tvCover { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(const_makeEndKey.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.makeCost()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension MusicViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func accenting() {
        //: TotalReactiveCompatible.share.func__pushToSubscribeAlert()
        TotalReactiveCompatible.share.subscribe()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func buttonClick() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        sharedSearched()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        dataMatchStr.isometric(eventID: noti_ratePath)

        //: if NowInfoThen.share.appConfigMode.displayMatchV4PopUp, NowInfoThen.share.loginUserMode.freeCallTimes > 0,
        if NowInfoThen.share.appConfigMode.displayMatchV4PopUp, NowInfoThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.drogue()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        buttonController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func rectangle() {
        //: guard TalkingSocketManager.shared.isTarget == false else {
        guard BankManagerSocketDelegate.shared.isTarget == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.addMsg(showMsg: dataMessageName)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        enableVoice()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(data_attributePlayerMessage.prefix(6)) + "ng now").localized
        //: setPlayer(isRandon: true)
        thumb(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if NowInfoThen.share.loginUserMode.randomVideoType == 2 {
        if NowInfoThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            dataMatchStr.isometric(eventID: notiUserKey)
            //: req_getVideoInfo()
            errorInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            dataMatchStr.isometric(eventID: user_screenAvailableId)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            BankManagerSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            BankManagerSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            BankManagerSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            BankManagerSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            restartWithinSimulationErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func restartWithinSimulationErrno(cmd: String = (String(k_infoPath.prefix(8)) + String(dataLevelTitle)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        valid()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        transaction(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func actionRank() {
        // 停止定时器
        //: destroyWaitingTimer()
        valid()
        //: showCloseBtn()
        sharedSearched()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(noti_statusUrl.suffix(6)) + " star" + String(data_addMsg.prefix(6)) + String(appModelMessage)).localized
        //: setPlayer(isRandon: false)
        thumb(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTarget == false {
        if BankManagerSocketDelegate.shared.isTarget == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            BankManagerSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            BankManagerSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            BankManagerSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            BankManagerSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        makeCost()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func valid() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func errorInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = FrameRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(show_dataKey.prefix(5)) + "eeCa" + String(main_actionUrl.suffix(6)) + String(data_modelEqualFormatMessage.prefix(9)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: userTurnBottomData.map{fromVideo(mode: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        dataVoiceKey.titleCompletion(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.actionRank()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: NowInfoThen.share.loginUserMode.randomVideoType = 1
                    NowInfoThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.rectangle()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.addMsg(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = EnableMeasurable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = VideoTitlePlayListener()
                //: player.setMute(bEnable: true)
                player.muteSample(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.showWorldView(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                LoadUpThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                LengthPermissionTool.nearGift { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.done()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func done() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        dataMatchStr.isometric(eventID: mainPointPath)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard LoadUpThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.addMsg(showMsg: String(bytes: show_colorUrl.map{userView(component: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            LoadUpThen.shared.utilizer(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.actionRank()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = CenterPlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.buttonController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.actionRank()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func enableVoice() {
        //: if NowInfoThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if NowInfoThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(NowInfoThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(NowInfoThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func sharedSearched() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func equalName() {
        //: guard TalkingSocketManager.shared.isTarget == false else {
        guard BankManagerSocketDelegate.shared.isTarget == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.cause()
        }
    }
}

// MARK: - CommentObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension MusicViewController: CommentObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func locationData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = BlockSaveResultModel.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(user_editItemMessage)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: showTagData.map{randomOn(leading: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: app_pathTopMessage.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: showMakeLocationText.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = EnableThreadTypeQuickLookable.randow
        //: if NowInfoThen.share.loginUserMode.freeCallTimes > 0 && NowInfoThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if NowInfoThen.share.loginUserMode.freeCallTimes > 0 && NowInfoThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(mainLeadingKey)))
            //: TalkingSocketManager.shared.isFreeCall = true
            BankManagerSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: app_ofKey.map{dataButton(content: $0)}, encoding: .utf8)!: (String(noti_viewName)), String(bytes: k_backgroundName.map{modelView(manager: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        BankManagerSocketDelegate.shared.appFrom(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            BankManagerSocketDelegate.shared.appFrom(info: [String(bytes: app_ofKey.map{dataButton(content: $0)}, encoding: .utf8)!: (user_equipmentTitle.replacingOccurrences(of: "model", with: "nc") + "Call"), String(bytes: k_backgroundName.map{modelView(manager: $0)}, encoding: .utf8)!: [(String(userContentTitle.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.restartWithinSimulationErrno(cmd: (String(notiFrameContent.prefix(5)) + "eoMatch"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func beTitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(userContentTitle.suffix(5)))].intValue
    }
}

// MARK: - NameObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension MusicViewController: NameObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func eigenvalueOfASquareMatrix(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func modelPhone(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func filterOfRequest(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func session(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            CellThen.view(body: String(bytes: show_mName.map{$0^50}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = BlockSaveResultModel.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            digitiserSumUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        actionRank()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.exceptCotta()
        //: NowInfoThen.share.start1v1TalkCall(info: data, situation: .Calling)
        NowInfoThen.share.asShow(info: data, situation: .Calling)
    }
}

// MARK: - ActionVideoPartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MusicViewController: ActionVideoPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func doTarget(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        restartWithinSimulationErrno(cmd: (app_emptyMessage.replacingOccurrences(of: "image", with: "r") + "Call"))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func sprechgesang(data _: [String: Any]) {}
}

// MARK: - BotchErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension MusicViewController: BotchErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func startAccountingData(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(k_giftTitle.suffix(4)) + "ncelCal" + appChangeLessMsg.replacingOccurrences(of: "label", with: "l")) { // 取消通话异常
            //: stop_randomVideo()
            actionRank()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(notiFrameContent.prefix(5)) + "eoMatch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            actionRank()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(user_editItemMessage))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.addMsg(showMsg: msg)
            //: uploadFailTime(type: 2)
            penumbra(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(notiFrameContent.prefix(5)) + "eoMatch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(app_marginId) + String(kSightPath))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(user_editItemMessage))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.restartWithinSimulationErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(mainDataMsg)), errorNo == PriceWidthCount.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            actionRank()
            //: guard NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue else { return }
            //: TotalReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            TotalReactiveCompatible.share.musculusSphincterPupillae(clickEvent: app_screenMoviePath, sufficient: false)
            //: uploadFailTime(type: 1)
            penumbra(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        restartWithinSimulationErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension MusicViewController {
    //: func uploadFailTime(type: Int) {
    func penumbra(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(main_shareAtPath) + String(appStatusFormat))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(k_phoneValue) + String(show_arrayFormat) + String(constRawUrl.prefix(9)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        dataMatchStr.dismissEnable(eventID: data_viewId, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func digitiserSumUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(k_phoneValue) + String(show_arrayFormat) + String(constRawUrl.prefix(9)))] = time
        //: parm["toUid"] = toUid
        parm[(String(noti_failContent))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        dataMatchStr.dismissEnable(eventID: constAppUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension MusicViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func makeCost() {
        //: guard NowInfoThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard NowInfoThen.share.appStatus == PointRelatableTarget.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !NowInfoThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !NowInfoThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = EnableThreadTypeQuickLookable.randow
        //: if NowInfoThen.share.loginUserMode.freeCallTimes > 0 && NowInfoThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if NowInfoThen.share.loginUserMode.freeCallTimes > 0, NowInfoThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if NowInfoThen.share.appUserConfigMode.callBtnStyle == 1 {
            if NowInfoThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(noti_dataKey.suffix(4)) + " 1 min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = NowInfoThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = NowInfoThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(mainMenuMakeMessage)).toAppear(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(mainMenuMakeMessage)).toAppear(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.outView(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = NowInfoThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = NowInfoThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = NowInfoThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = NowInfoThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !NowInfoThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !NowInfoThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func beOf() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func views() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(appImageLimitPath + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func ignore() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(makeCost),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: const_viewCancelName,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(allRoute),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: show_tabKey,
                                               //: object: nil)
                                               object: nil)
    }
}
